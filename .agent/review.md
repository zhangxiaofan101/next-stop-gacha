# Review — 下一站，去哪玩

> 本文件由 reviewer（与实现者不同模型家族，默认 Codex/GPT）拥有。发现项在此存活直到被处置：
> ① 代码修复后删除 ② 移入 state.md 成为待办（带署名）③ 升级为 design.md 持久变更 ④ 驳回并留一行理由。

## Reviewer 提示词模板

审阅本仓库最近的变更（`git log` 定位范围），从八个轴逐项检查，发现写入下方 Active findings：

1. **goal ↔ design 对齐**：设计是否偏离 goal.md 的意图与硬约束（单文件/CSP/浅色卡通）？
2. **design ↔ code 对齐**：index.html 实现是否与 design.md 的机制描述一致？
3. **数据完整性**：data/*.json 与补丁是否通过 tools/build.py 校验？枚举、坐标范围、id 唯一性？地名/菜名/酒店有无明显编造？
4. **错误处理**：空筛选结果、空行程、localStorage 损坏、剪贴板失败等边界是否处理？
5. **测试/验证覆盖**：构建脚本校验之外，关键交互（扭蛋、路书装配、顺路排序）有无验证记录？
6. **可维护性**：单文件 JS 是否结构清晰？数据 schema 变更的影响面是否可控？
7. **估算的诚实性**：交通时长/预算是否始终明示为估算？有无伪装精确的文案？
8. **遗漏**：goal 中承诺但未实现、或实现了但 state.md 未记录的内容？

## Active findings

### F1 — [P1] M12 未清除公开设计文档中的品牌偏好

`goal.md` 明确要求具体住宿品牌偏好不出现在公开文案中，`state.md` 又将 M12 标为完成并声称页面/README/goal 已同步；但 `design.md` 的 M2 仍写有“住宿信息按用户偏好（万豪系）定制”。本仓库及其 `.agent/` 文件都是公开项目内容，这既与 goal 的隐私约束冲突，也说明 M12 的完成声明不完整。

建议处置：→ **design.md durable change**，将 M2 改为不含品牌的“按用户住宿档位与位置偏好定制”；随后补充 M12 的 `Verified:`，记录对整个仓库（排除 `.git`）的品牌词扫描结果。

### F2 — [P1] S2/S3 完成项缺少可复核的验收与验证证据

`state.md` 将 M1–M5、M7、M8、M12 等 S2/S3 项标为完成，但文件中没有任何 `Acceptance:` 或 `Verified:` 行。按本项目 workflow，S2/S3 的完成必须记录预期证明和实际执行的检查；当前只有摘要式“校验全过”“独立复核”等描述，无法判断筛选、对比、行程、路书等关键交互是否真实走查，也无法复现 M12 的零残留结论。本次 reviewer 仅能确认 `python3 tools/build.py` 通过（144 条数据成功合并注入、无工作树差异）；JS 语法检查因本机无 `node` 未执行。

建议处置：→ **state.md actionable item**，先为已完成的 S2/S3 模块补齐具体 `Acceptance:` / `Verified:`；没有历史证据的不要倒推写“已验证”，应新增一项聚焦 QA，至少覆盖筛选 AND/天数桶、损坏 localStorage、对比上下限、行程增删/清空/排序、路书生成与复制失败回退，并记录实际环境和结果。M19 完成前也应定义并记录入口显性化与一键清空的验收证据。
