# Review — 下一站，去哪玩

> 本文件由 reviewer（与实现者不同模型家族，默认 Codex/GPT）拥有。发现项在此存活直到被处置：
> ① 代码修复后删除 ② 移入 state.md 成为待办（带署名）③ 升级为 design.md 持久变更 ④ 驳回并留一行理由。

## Reviewer 提示词模板

审阅本仓库最近的变更（`git log` 定位范围），从八个轴逐项检查，发现写入下方 Active findings：

1. **goal ↔ design 对齐**：设计是否偏离 goal.md 的意图与硬约束（单文件/CSP/浅色卡通）？
2. **design ↔ code 对齐**：index.html 实现是否与 design.md 的机制描述一致？
3. **数据完整性**：data/*.json 与补丁是否通过 tools/build.py 校验？枚举、坐标范围、id 唯一性？地名/菜名/酒店有无明显编造？
4. **错误处理**：空筛选结果、空行程、localStorage 损坏、剪贴板失败等边界是否处理？
5. **测试/验证覆盖**：构建脚本校验之外，关键交互（扭蛋、路书装配、顺路排序）有无验证记录？
6. **可维护性**：单文件 JS 是否结构清晰？数据 schema 变更的影响面是否可控？
7. **估算的诚实性**：交通时长/预算是否始终明示为估算？有无伪装精确的文案？
8. **遗漏**：goal 中承诺但未实现、或实现了但 state.md 未记录的内容？

## Active findings

> Review baseline: `7cc7a0c..c57a67a`，Codex/GPT reviewer，2026-07-15。本轮覆盖 cc 对 F14–F17 的 triage 修复与 M31 处置执行。F14–F17 已定向复核：`trip` 限城市 id 且校验天数、江浙沪 42 张城市卡 `transit` 全部显式含“自驾”、JSON 导入拒绝 `null`/数字/数组、绕路增量改用未取整 haversine 且全量枚举无负值，四项按协议删除。`python3 tools/build.py` 全量通过且幂等（248 城 + 37 线），`git diff --check` 通过。

### F18 — [P1] 独库线路卡装入行程后丢失线路方案，5 天路书实际拼成 9 天城市方案

`addRouteToTrip()` 只把 `stops` 写入 `state.trip`，不保留线路 id 或线路 `plans`（`index.html:1757-1765`）；`roadbookModel()` 随后仅对每个城市 stop 调用 `pickPlan()`（`:2144-2162`）。新 `route-duku-highway` 装入为 `yili:2 / bayanbulak:1 / kuqa:2`（`data/routes.json:359-371`），但伊犁最短只有 5 天 plan（`data/data-d.json:683-725`），巴音布鲁克最短只有 2 天 plan（`:1189-1228`）。因此点“整条装入”后，路书显示“伊犁 2 天·按 5 天版改编”并原样输出完整 5 天伊犁方案，再把巴音布鲁克 2 天方案塞入 1 天，加上库车 2 天；线路卡自身的 5 天“纵贯版”完全不会进入路书。M31 这次删除原 `duku-highway` 城市 stop 后，暴露了 M18 “线路 plan 只用于卡面、路书只用城市 plan”的结构断点。

建议处置：→ **code/schema fix + route-to-roadbook regression**。线路装入后必须保留路线身份，让路书能消费该线路的对应天数 plan/逐日骨架；或为 stop 提供线路专用分段文案，不能继续用城市独立游 plan 生硬压缩。回归至少断言点击「独库公路自驾」后，导出的 5 天路书包含独库驾驶段，且不出现“2 天按 5 天版改编”/“1 天按 2 天版改编”。

### F19 — [P1] 独库新 5 日文案把哈希勒根达坂放在那拉提→巴音布鲁克段，路线方向错误

`route-duku-highway` 新 5 日方案写“D2 经那拉提上独库，翻哈希勒根达坂至巴音布鲁克”（`data/routes.json:336-345`）。但新疆交通运输厅的 2025 出行指南明确分段为“独山子 → 乔尔玛 → 那拉提”北段（哈希勒根达坂在该段）、“那拉提 → 巴音布鲁克”中段；自那拉提向南去巴音布鲁克不会翻越位于北段的哈希勒根。官方口径也将独库全线定义为独山子起、途经乔尔玛/那拉提/巴音布鲁克、库车终。当前 7 日方案同样从伊宁/那拉提开始，完全略过独山子–乔尔玛北段，却仍保留“纵贯天山”、乔尔玛和哈希勒根亮点（`:307,328-345`）。证据：[新疆维吾尔自治区交通运输厅《2025 年端午节公路交通出行服务指南》](https://jtyst.xinjiang.gov.cn/xjjtysj/zwgg/202505/1bc0833e1b754ae98576021d349ca627.shtml)、[新疆维吾尔自治区文化和旅游厅对独库公路线位的答复](https://wlt.xinjiang.gov.cn/wlt/rdxyta/201910/0f1303e3fa8e4493b47cc89a9956a4eb.shtml)。

建议处置：→ **data fix + factual route review**。二选一：若仍叫「独库公路自驾」/“纵贯版”，恢复独山子→乔尔玛→那拉提→巴音布鲁克→库车的南下顺序；若产品意图是伊宁起、从那拉提只走中南段，则收窄名称/卖点并删除乔尔玛、哈希勒根等北段承诺。

### F20 — [P2] 「西宁」仍有整日跨市州青海湖方案，未达到 M31 拍板的“纯城市卡”

M31 处置记录明确写 `xining-qinghai-lake` 收窄为「西宁」纯城市卡、环湖玩法交给 `route-qinghai-loop`（`.agent/m31-containment.md:18`）；但当前 3 天 plan 仍名为“加青海湖一日”，D3 完整走日月山–倒淌河–二郎剑（`data/data-d.json:486-496`）。二郎剑位于海南州共和县，不属西宁；这不是市内景点的命名细节，而是一个仍在替环线卡承载跨地区一日游的独立方案。证据：[共和县政府的青海湖二郎剑景区公告](https://www.gonghe.gov.cn/xwdt/tzgg/content_1013653405)、[青海省政府行政区划](https://www.qinghai.gov.cn/dmqh/system/2020/10/10/010368322.shtml)。

建议处置：→ **data fix + M31 scope assertion**。按已拍板方向，把 3 日方案改为西宁市域内容（青藏文化博物院/湟中区城市漫游/大通或湟中市域内选项），青海湖只保留“可接《青海湖祁连环线》”的导流提示。
