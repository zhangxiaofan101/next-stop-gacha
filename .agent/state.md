# State — 下一站，去哪玩

> 更新纪律：每个工作会话结束时本文件必须与代码现实一致；每条新增/修改行带作者署名 [cc] 或 [codex]。spec 一律指 design.md，本文件只记时间线，不复述规格。

## 🎯 Status snapshot

**线上**：`lab.medspiral.com/next-stop-gacha/`——Vite+TS 工程化版，267 城 + 53 线全链路（筛选/扭蛋/对比/行程/路书/足迹地图/天气）；后端短链分享（KV）+ 同步码云同步（Durable Object）已闭环并过跨家族 review；主题皮肤机制落地，奶油/山水双肤可切（山水已过 M52 形态精修并生产复验），DOM 结构与 token 体系处冻结态（再动结构须用户拍板）。四期代码侧（M37/M38/M40/M41/M42 管线）与五期代码轨道（M47→M45→M50→M46→M52）全部落地，明细见 ✅ 与 git 历史。 [cc]

**在飞**：①review：codex 对 /loop 七项批次跨家族复核（baseline 883655a）新提 F61（P1，M55 季节解析误判「春节」+ 漏识别带旬跨年区间）/F62（P2，M57 工艺槽位归一化撞名静默覆盖），**cc 已逐条修复入库并带回归测试**（见 ✅ M55/M57·review 响应），review.md 尚未改——按既有惯例关闭权留给 codex 下轮复核；此前 M45/M46/M52 三个跨家族 gate 仍维持正式通过。②M44 目的地共享集：**191/267 城母版已终审入库**，`dest-fuzhou.webp` 超预算问题已由 codex/M44 track 修复（commit f5a83fd，用户已确认）；③**/loop 批七项全部收工**：[interrupt]→M53→M54→M58→M55→M51→M57 逐项落地，见 ✅（M58/M57 各自实现期顺带修复/发现一个真实问题——前者是详情头图 lazy 恒不加载的生产 bug，已随 review 响应一并复验）；④**内容批（fable orchestrator 会话）三模块收工且拍板全回收**（2026-07-20，见 📋 拍板档案）：三工单（`.agent/m48-findings.md`/`m56-findings.md`/`m49-findings.md`）头部已注拍板结果，实施批A（M48+M56 修复落库）与实施批B（M49 入库 15 新卡+3 tag+回标 51 条，267→282 城）**均已于 2026-07-21 内容 /loop 会话落库**（见 ✅ 两条目；三份 m*-findings 工单全部消费归档删除），**挂「codex 跨家族抽查」gate（见 📥）**；M22/M11 仍候排（P1/P2）。 [cc]

**近期顺序**：/loop 批（本会话，用户指定顺序）**全部完成**：[interrupt] 两条线上小修✅ → M53✅ → M54✅ → M58✅ → M55✅ → M51✅ → M57✅，模块边界 compact；随后 codex 跨家族复核提两条发现，**cc 已修复入库**（见 ✅ M55/M57·review 响应，本节顶部）。M56 机制段本批不动，留 🔜；用户侧待办：M44 母版挑版/终审、山水终审 + 默认皮肤拍板、三条内容工单 triage（均见 🟡）。2026-07-21 用户九条视觉反馈立案 M59（十二项均带根因取证）+ M60 九区题头晋升共享题头层，拍定执行顺序先 M60 后 M59；**M60、M59 均已实现落地并生产复验**（见 ✅ M60、✅ M59）。M56 机制段仍留 🔜（transport.ts 文件域与 M59 重合，建议同会话顺手接续，见 🔜 条目）。**内容批 /loop（cc 另一会话）已全部收工**：2026-07-21 实施批A（M48+M56 修复落库）✅ + 实施批B（M49 入库 15 新卡+3 tag+回标批）✅，已挂「codex 跨家族抽查」gate 停批（gate 详见 📥）。 [cc]

## ✅ Implemented

（Phase 1 已封板 → 见 🪦 墓碑；M1–M25 全部条目与 Verified 证据明细在 git 历史（至 176512d）；新模块自 M26 起编号） [cc]

- M59 — 视觉修缮批（十二项）[R1 · S2] → sonnet · high · cc in-session（2026-07-21）｜spec 见 design M59 与本节 M60 之后已删除的原始十二条发现（根因取证详见 git 历史）。逐条落地：
  ① **山图裁头**：`.deco-hill` 从「fixed 全宽 + 固定 140px/90px 高 + object-fit:cover」（960×480 母版在常见视口只显 23~30% 纵向带，山峰被切）改为 `aspect-ratio:2/1; height:auto; max-height:200px（移动端110px）; object-fit:contain`——窄视口下容器天然维持 2:1 零裁切，宽视口超出上限后 contain 保证母版整幅可见（左右留白）。同族自查揪出 `.deco-bamboo` 也是假 2:1（190×130，比例 1.46）：桌面改 190×95、移动端 120×66→120×60，配平真实母版比例；`.deco-willow` 本就 220×110 精确 2:1，未动。
  ② **工艺框笔触校准**：frame 母版 1024² 只画一圈发丝细墨线，`border-image-slice:42` 配旧 `width:8/9px` 压缩比达 5.25x/4.7x，笔触被压到约 1px 与普通实线无差。放大 `border-image-width/outset`——`.console`/`.paper` 8px→18px（outset 3→6px）、`.machine`/`.share-sheet` 9px→20px（outset 4→7px），压缩比收到约 2.3x/2.1x，笔触糙边清晰可辨（浏览器实测长边缘可见明显手绘波动，非直线）。参数校准已达标，未触发 A8 返工分支。
  ③ **图标尺寸**：`.icon-btn .ic` 17px→20px。**实现期抓到真实回归**：`.icon-btn` 选择器是单类，与后文同样单类的 `.btn`（`padding: 7px 14px`）specificity 相同，源码序在后的 `.btn` 赢——`.icon-btn` 的 `padding: 0` 自 M50 落地起从未真正生效，内容盒被压到约 3~5px 宽，线形 SVG 图标被横向挤扁到几不可见（emoji 版本因文字溢出盒子未被裁切、长期未被肉眼揪出）；生产环境验证同样受影响（`svgW:"5px"`）。改选择器为 `.btn.icon-btn`（复合，匹配 HTML 实际用法）解决，padding:0 才真正生效，图标恢复完整方形展示。
  ④ **卡片交通图标解析**：`logic/transport.ts` 新增 `parseTransitIcon(transit)`——按「文案中最先出现」的方式关键词取图标（直飞/飞机/航班/转机→✈️，高铁/动车/城际/铁路/普速/火车→🚄，地铁/轻轨→🚇，自驾/包车/公路/用车/车辆→🚐，大巴/客车/班车→🚌，游轮/邮轮/渡轮/快艇/乘船/坐船→🚢，轮渡→⛴，索道/缆车→🚡，兜底🧭）；`cards.ts` 条带与名下行共用同一次解析结果，替换此前硬编码「✈」/「🚄」。267+53 条真实 transit 跑通关键词表：仅 2 条纯模糊转乘描述落兜底图标，不猜测。detail.ts 的详情页元信息行仍硬编码 🚄——finding 明确只点名 cards.ts 两处，未扩大范围（已 spawn_task 记录为独立小尾巴）。
  ⑤ **收藏钮线形化**：`icons.ts` 新增 `heart`（stroke 线形，与 scale/suitcase 同族），`cards.ts` 收藏按钮从 `♥` 字符换成该图标；`.act.fav.on` 既有「变红」背景色逻辑不变。
  ⑥ **详情区段视觉系统**：chip 家族三成员（美食/博物馆/古建古迹）同构格式、各自一色——古建古迹此前无差别沿用博物馆的蓝（两同一异），新增 `.dt-chip.arch` + `--arch-chip-*` token（cream 铜绿、ink 低饱和铜绿灰）独立成第三色；「特色体验」是完整语句不适合 chip 格式，改用 `::marker` 项目符号色（`--highlight-accent`，玫红/ink 收敛版）区分，文字仍走中性正文色。住宿（棕）/当地交通（绿）/行程方案（蓝卡）三个既有格式各自已经自成一色，未动。
  ⑦ **扭蛋弹层 hero 化**：`.g-illust .illust` 92px→190px，`.g-window` padding 30/14→18/14，`.g-city` 字号 `clamp(34,8vw,52)`→`clamp(24,5.5vw,36)`——机器画面变主角，结果窗降级配合；DOM 冻结点按用户本人点名视为已授权修订。
  ⑧ **路书分隔线/leg 行轻量化**：divider craft 层三处消费点（`.leg-line .dash`/`.rb-sk-row`/`.console-foot::before`/`.rb-cover::after`）统一从 `100% 3px`→`100% 1.5px`（源图左侧笔触浓重处压到 3px 高时近乎实心，与②同口径但方向相反——frame 太弱要放大、divider 太重要收细）；`.rb-leg`（路书 `stop.leg` 交通行，非行程单的 `.leg-line`）新增 ink 覆写——去底色/去边框，改 `border-bottom` 虚线 + 文字色降为 `--text-subtle`，从「蓝底实线框」降级为与宣纸纸感一致的轻样式。
  ⑨ **卡位共享集展示皮肤声明开关**：`SkinDeclaration` 新增 `cardPhotos: boolean`（cream:false / ink:true），`illustrations.ts` 新增 `cardPhotosEnabled()`；`cards.ts` 的 `photo` 槽位整体受此开关控制，票券走同一 `cardHTML` 路径自动随开关（不需要票券侧单独判断）。
  ⑩ **线路卡卡位启用大区题头**：开图皮肤下线路卡 `.c-photo` 主源直接是 `regionHeaderSrc(d.region)`（M60 共享层，2:1 原生不换帧比，无需回退链）。
  ⑪ **票券氛围带重做**：`#gachaTicket` 从「整券 `background-image:cover+center` + ink 专属 90px 顶部 padding 露缝」（城市券卡片占满券高完全遮住、线路券勉强可见位置不可控）改为 `roll()` 生成的独立 `<div class="ticket-ambience">`（自身 90px 高、`.card` 同款描边 token、居于卡片上方），随 ⑨ 同一个 `cardPhotosEnabled()` 判断生成与否（cream 下整条不产出，不留空盒）。
  ⑫ **无个图城市卡位退大区题头**：开图皮肤下城市卡 `.c-photo` 复用详情头图同款回退链——主源 `destPhotoSrc(d.id)`、`data-fallback-src=regionHeaderSrc(d.region)`，两者都缺时（cream 下）槽位整体不生成。
  **红线执行**：①②⑧ 仅 ink 生效（cream 无对应资产/规则，天然零回归）；④⑤⑥⑦ 双皮肤同改；⑨–⑫ 落地后 cream 的可见变化被严格限定在「卡位无图 + 图标/区段统一族」——票券氛围带虽然 URL 现在对 cream 也能 200（M60 共享层不设皮肤覆盖），但 `#gachaTicket`/`.card` 结构上完全重合（零 padding），氛围带背景图被卡片完全遮挡，cream 肉眼零可见变化（浏览器实测 `getBoundingClientRect` 两者坐标完全一致）。
  ｜Verified：`bun run verify` 204/204（12 项新增测试：`transport.test.ts` +7、`cards.test.ts` 新文件 7 条、`detail-sections.test.ts` 新文件 4 条、`gacha-ambience.test.ts` 新文件 2 条、`gacha-ticket-eager.test.ts` 改 1 条前置条件）+ 45 workerd 无回归；`bun run build` 干净。**双皮肤浏览器复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome）：cream——分享图标 20px 完整可辨（padding bug 修复实测 `getComputedStyle` 从 `{w:"3px",h:"20px"}` 变为 `{w:"20px",h:"20px"}`）、卡片网格与详情页均确认零 `.c-photo`（`cardPhotosEnabled()===false`）、无个图城市（chengdu）详情兜底显示大区题头（M60 联动，此前必 404）、扭蛋票券零氛围带背景但卡片/图标/交通图标正常、控制台零 error；ink——deco-hill 滚动到页底完整可见山头（无裁切，两侧留白）、console/paper/machine 边框肉眼可辨手绘糙边、路书 leg 行降级为轻量虚线下划、路书逐日分隔线变细、扭蛋机 hero 化（190px）+结果窗缩小、票券氛围带作为独立框体正确显示在卡片上方（浙东南海岸渔村→柳枝题头）、详情页古建古迹 chip 与博物馆 chip 呈现可辨的不同色调、特色体验列表项目符号带色、控制台零 error。真实窗口无法收窄到移动端宽度复验 680px 断点（环境已知限制，同 M47/M50/M52 记录）——mobile 数值改动（deco-hill/bamboo 尺寸）仅按算术推导验证比例正确，未做像素级实拍。**生产复验（push 13c1870 后，Cloudflare 自动部署完成）**：`index-DeYtzZom.js`/`index-DTJH9Irq.css` 与本地过闸构建**字节级一致**（`diff` 通过）；真机 `lab.medspiral.com/next-stop-gacha/` 复验——cream 分享图标 `{padding:"0px", svgW:"20px", svgH:"20px"}`（padding 撞车修复确认生效）、杭州卡片零 `.c-photo`+交通图标正确解析为 🚄+收藏钮为 SVG；ink 杭州卡片 `.c-photo` 正确加载 `illustrations/dest/hangzhou.webp`、页面滚到底部 deco-hill 完整可见山头（两侧留白，无裁切）、线路卡「江南水乡」题头图正确显示；两皮肤控制台全程零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M59 ④·收尾：详情页交通行图标同源解析（2026-07-21，cc planning 会话续用当日代码例外授权，任务卡=M59 会话遗留 spawn 提示、用户转交执行）｜M59 ④ 修卡片时详情页 `dt-meta` 同病一行按「只改用户点名范围」刻意未动：`🚄 ${d.transit}` 恒高铁图标（澳门类飞机城照显 🚄）。本次 `src/ui/detail.ts` 改为 `parseTransitIcon(d.transit)` 同源解析；**线路卡不豁免**——与 cards.ts 的「线路不显示 transit 故无图标」不同，详情页真的展示线路 transit（首末大交通文案、方式词齐全），一并解析（「上海直飞乌鲁木齐」类线路卡自此显 ✈️）。新增 `src/ui/__tests__/detail-transit-icon.test.ts` 4 条：飞机城 ✈️（革硬编码）、高铁城 🚄（零变化）、线路卡解析、模糊文案 🧭 中性兜底｜Verified：`bun run verify` 208/208（+4 新增）+ 45 workerd 全绿；`bun run build` 干净；本地生产构建双皮肤复验（http.server 正确 `/next-stop-gacha/` base）：cream 澳门=✈️ 杭州=🚄 广深港澳线路卡=✈️、ink 澳门=✈️，控制台双皮肤零 error；生产复验见本条追记 [cc]

- M56·slowrail 数据段（2026-07-21，cc **planning 会话按用户指示开代码例外**——「顺手修一下」授权；只动无人占用的 data/build.py，transport.ts 守卫消费留给机制段避免撞 M59 在飞会话）｜起因：用户问「普通火车还多吗」→ 池内筛查发现 4~6 张非 FLY_PROV 卡有轨道但仅普速，启发式 <950km 档会标「高铁」（哈尔滨→漠河标「高铁约4.8h」vs 现实 K 车 13h）。落地：①`tools/build.py` 守卫字段组 `(noair, norail)` 扩为 `(noair, norail, slowrail)`（布尔校验+线路卡禁标沿用）；②6 城落 `slowrail:true`（紧跟 transit 行、与批A noair 落位同风格）：**漠河/五大连池/抚远**（transit 文案自证「转火车/普速/夜车」，批A 体检核过）、**阿尔山**（网查：白阿线现役全 K/普快 4346/K2121/K7565 等，动车化仅规划、无 G——维基白阿铁路/携程时刻表）、**呼伦贝尔**（网查：2026-07 哈铁调图海拉尔方向仍 K7090/K7193 普速，滨洲线电气化未开动车——哈铁调图公告；cc 原「海拉尔有动车」印象经查证伪）、**额济纳**（网查：现役客运=K7911/2 呼市—额济纳普速约 15h，2026-05 冠名胡杨专列——中国铁路官网/新华网）；嫌疑洗清：延吉/齐齐哈尔/西昌/敦煌/伊春/龙泉/德化均有现役 G/D（transit 文案即证）；③规则回写：checklist 二节增 slowrail 查验项、design M56 增第三守卫 spec（降「火车」档 ~65km/h+0.5h，标定=哈漠 K 车；与 TRANSPORT_META 显式火车 120 参数不混用）、复卡清单 +2 触发（白阿动车化→aershan、滨洲开动车→hulunbuir；黑河高铁既列项捎带 wudalianchi）｜Verified：`python3 tools/build.py` 过闸（282+53 全绿，chunk-3/7 重哈希）；**守卫消费未落地**——slowrail 字段此刻与 noair/norail 一样躺在数据里等机制段，无行为变化、无回归面 [cc]

- 内容批·实施批B — M49 入库：15 新卡 + 3 tag + 回标批 [R2 · S2] → fable 编排 · opus 分片×4 + cc 复核（2026-07-21，内容 /loop 第二轮；spec=m49-findings 拍板终稿+五之二附录，工单消费后归档删除，全文见 git 历史 `.agent/m49-findings.md`）｜落地五块：
  ① **tag schema**：TAGS 加 实景演艺/观星/工业遗产（`tools/build.py` + `src/logic/constants.ts` 筛选 chip 同步；「亲子」维持 chip 隐藏先例不变）
  ② **15 新卡入库（267→282 城）**，全部 transit/通达网查核实后落笔：诸暨·山下湖珍珠/义乌/龙泉（江浙沪）、德化（华东——屈斗宫窑址核实属泉州 22 世遗点，打 世界遗产）、唐山·开滦/查干湖/大庆（华北/东北）、冷湖（西北——noair+norail+alt+折腾，观星+工业遗产双标）、文昌·龙楼/黄石·大冶（华南/华中）、澄江·抚仙湖/榕江·村超/荣昌/六盘水/攀枝花（西南）。新卡 noair 标 7（诸暨/龙泉/德化/文昌龙楼/澄江/榕江/荣昌，每标带网查依据句）+冷湖双标；「凡判先搜」纪律四处真实回报：兴泉铁路德化站(2022)、云桂线阳宗站(2026-01 开客运·低频)、松原查干湖机场现役、鄂州花湖机场距黄石 15km（按 <30km 近场口径不标 noair、客运收缩挂复卡清单）；强季节卡（查干湖冬捕 12月中-春节/文昌发射窗口/六盘水夏避暑/攀枝花冬康养）seasonNote 均写窗口月份+淡季落差+「以官方公告/当期实查为准」对冲
  ③ **回标批 51 条**：实景演艺 17（宋城/印象刘三姐/又见平遥/又见敦煌/文成公主/魅力湘西/东京梦华/长恨歌/康熙大典·2025-04 复演核实/知音号/丽江·三亚千古情/只有河南/梦幻谷/金声玉振/乌镇戏剧节·10月/大唐倚梦，全部 2025-26 在营逐一核验）+ 观星 2（茶卡「暗夜星空保护地」/中卫沙坡头星星酒店）——**连同冷湖观星共 3 张，低于拍板预估 5-7**，按「终名单逐卡核验不硬拍」授权收缩（札达跳过：阿里暗夜公园实属噶尔县、距札达约 260km；稻城/大柴旦/额济纳核验未过，理由随工单归档）+ 工业遗产 7（首钢园/燊海井/陶溪川/晋华宫/青啤博物馆/沈阳中国工业博物馆/茂新面粉厂）+ 补文案 25 条（盱眙龙虾节 6 月、阳澄湖蟹季 9 月下旬开湖落苏州+周庄锦溪两卡、东风航天城注明政审预约、善卷洞卡补紫砂文案·陶二厂+黄龙山=拍板③）；拈花湾/美丽西江等 12 项 skip 留因
  ④ **cc 复核 diff 改 8 处**：大庆/唐山 difficulty 直达→一次中转（<5对/日细则 + xichang/huaian 库内先例交叉）、5 处「判直达/判折腾」spec 语从 transit 文案清除、榕江 patch 补 companions、阳宗站线名归属修正、两卡名点号风格归一
  ⑤ 数据现实钉子更新：`filter.test.ts` 全量 320→335、江浙沪 46→49
  ｜Verified：`python3 tools/build.py` 过闸（282 城+53 线，9 chunk 零 error）；vitest 204/204 + 45 workerd 全绿；`bunx vite build` 干净。**环境警示（非本批引入）**：`bun run verify`/`build` 的 tsc 前置当前红——同树前端 /loop 会话 M59 未提交改动（SkinDeclaration.cardPhotos）自身 mid-module，错误全限于其 `src/skins/__tests__/illustrations.test.ts`，本批文件 tsc 干净（bunx tsc 过滤核实），其收工提交后 verify 自然转绿。**批B 落库后挂「codex 跨家族抽查」gate（见 📥），内容批 /loop 到此停批** [cc]

- 内容批·实施批A — M48+M56 修复落库 [R2 · S2] → fable 编排 · opus 分片×2 + cc 复核（2026-07-21，内容 /loop 第一轮；spec=两工单头部拍板结果+正文清单，工单消费后归档删除，全文见 git 历史 `.agent/m48-findings.md`/`m56-findings.md`）｜四块落地：
  ① **noair/norail 111 城字段落库**（双标 31／单 norail 15／单 noair 65；脚本化落库+全量断言复核：判例城 wutaishan/chaozhou/shantou/chongwu/anyang 零标记、非清单城零误标；D 档按三档粒度口径裁定——qinhu-wetland/zhouzhuang-jinxi 门户 ≤1h 班次密不标，tiaozini/xinghua/jiabang-terrace 标 norail；jiabang 的 noair 未经反方复核不追标，宁缺毋编）；`tools/build.py` 校验随字段落（布尔型+线路卡禁标，通过后消费端机制段见 design M56 另排）
  ② **M48 落库（routes.json）**：difficulty 逐线核定 4（guilin→一次中转/siguniang→折腾/bashang→一次中转/jiaodong→直达）+ 阻塞 B1（晋北 transit 主推直飞约2.5h+高铁如实约9h）+ 警告 W1-W4（茶卡旅游专列措辞/海拉尔直飞约3.5h/洛阳约5.5h/晋北 plan 改「可接《浑源 · 恒山悬空寺》《朔州 · 应县木塔》」并 architecture 摘除两项）+ 建议 23 条一揽子（含 three-gorges chongqing leg 补 transport:游轮、duku seasons+秋、menyuan 末日 stays 门源→西宁、greater-bay 跨桥表意落金巴段、minnan 世遗→非遗簪花+崇武改可接）
  ③ **M56 落库（data-*.json/patch）**：29 条 transit 文案（硬伤 6/过时 15/用词 8——天目湖站→溧阳站、镇远走三穗站、神农架站锚定无「南站」、呼市盛乐机场公路约40km、五处普速/D级线「高铁」→「动车」等）+ 12 城习惯区组合标签（包车/自驾主选项+确有航线并列飞机）+ difficulty 3（xinghua/huaian→一次中转按 <5对/日细则、yixing 沪苏湖后→直达）；工单「可更新提示」类 5 项未拍板未入批
  ④ **口径成文**：difficulty=整程复杂度+直达班次 <5对/日 慎标细则 → design 属性打标原则 + checklist 三节；noair/norail 本体粒度三档口径（含品牌门户/共享机场/不稳定态处置）→ design M56 + checklist 二节
  委派纪律：两 opus 分片（routes.json 与 data-*.json 各一，带工单原文+checklist 节原文）并行，cc 逐条复核 diff 后修正 6 处——「并列」spec 语泄漏进文案 ×2、川青铁路误写「高铁」、五大连池丢「转飞」选项、shigatse 语序、漏派建议21（menyuan stays）补做；agent 侧两处诚实偏差采纳（chu-culture 目标句实在 local 非 transit；route-dali-lijiang 无铁路措辞可改）｜Verified：`python3 tools/build.py` 过闸（320 条含新字段校验，零 error）；`bun run verify` 184/184 + 45 workerd 全绿（本批零 src 改动，184 含同树在飞前端会话的未提交测试增量）；`bun run build` 干净、CSS 哈希未变；可接指针卡名逐一与 data name 字段核对命中 [cc]

- M60 — 九区题头晋升共享题头层 [R1 · S2] → sonnet · high · cc in-session（2026-07-21）｜spec 见 design M60。落地：
  ① **资产转档**：九张区域题头母版 `git mv` 出 `assets/illustrations/picked/ink/ink-region-<slug>.webp` → `assets/illustrations/picked/dest/region-<slug>.webp`（仅去掉皮肤前缀，画面内容不变——共享版=原 A6 水墨九张，illustration-brief 已注记此口径）；同时清理 `public/illustrations/ink/region-*.webp` 九个已失效的旧产物（`git rm`，无代码再引用这条路径，留着只是死重）。
  ② **`tools/build_illustrations.py`**：`classify()` 移除 `region-` 分支——皮肤目录内 `<skin>-region-*` 命名从此落进「未知类别」违规（九区彻底退出皮肤素材维度，新皮肤成套不必再画这 9 个版位）；`process_dest()` 扩为识别两族母版——`region-<slug>.webp` 按题头 2:1（960×480，`BUDGET_DECOR`）校验、`dest-<cityid>.webp` 仍按个图 3:2（640×427，`BUDGET_DEST`）校验，同目录并存互不误伤，沿用既有 F60 硬闸（比例超 2% 容差/质量下限仍超预算/命名不合规均计入 violations、非零退出）。
  ③ **`src/skins/illustrations.ts`** 新增 `regionHeaderSrc(region) = destPhotoSrc(regionSlot(region))`——复用两个既有 helper 直接复合，不另起拼接逻辑；不经 `assetDirFor`/`currentSkinId`，与当前皮肤无关，恒解析到 `illustrations/dest/region-<slug>.webp`。`ui/detail.ts`（详情头图兜底/线路卡头图）与 `ui/gacha.ts`（票券氛围带）两个既有消费点改调此 helper（各自移除对 `assetDirFor`/`currentSkinId`/`illustSrc`/`regionSlot` 的直接拼接）；第三个消费点「卡位题头」尚未存在（M59 ⑨⑩⑫ 的工作），届时同样消费这个 helper。
  **展示 gating 未变**（design 明确要求，URL 解析与是否可见是两件事）：详情兜底皮肤无关常显——**验证到位**，奶油 chengdu（无个图母版）详情页浏览器实测确认题头兜底首次在奶油下真实出现（此前 404 空白），符合「详情所有皮肤常显」拍板；票券氛围带肉眼零变化——**结构性验证**，`#gachaTicket` 与其内部 `.card` 子元素 `getBoundingClientRect()` 完全重合（零 padding），氛围带背景图虽然现在对奶油也能 200 加载，但被子卡片完全遮挡，视觉上仍不可见（`[data-theme="ink"] #gachaTicket.show { padding: 90px 0 0 }` 才是让山水皮肤露出氛围带的唯一机制，此规则未改），故奶油零可见回归；山水下同一氛围带位置/露出高度肉眼与改动前一致。
  ｜Verified：`bun run verify` 184/184（181+3 新增，`src/skins/__tests__/illustrations.test.ts` 新增 `regionHeaderSrc` 探针：拼路径正确/与 `currentSkinId`/`data-theme` 无关/等价于 `destPhotoSrc(regionSlot(...))`）+ 45 workerd 无回归；`bun run test:build-assets` 14/14（11+3 新增：picked/dest 内 region 与 dest 两族并存互不误伤、region 槽位一样受 2:1 硬闸拒绝 3:2 冒充、皮肤目录内 `<skin>-region-*` 不再被识别为合法槽位；另把首条比例校验测试的探针母版从即将被搬走的 `ink-region-jzh.webp` 换成同为真实 2:1 的 `ink-decor-hill.webp`，避免因资产搬家改变无关测试的前提）；`bun run build` 干净，CSS 哈希未变（本模块零样式改动），JS 哈希变化（detail.ts/gacha.ts/illustrations.ts 改动的正常联动）。真实全量运行 `python3 tools/build_illustrations.py`——九张 region 全部 2:1 校验通过且预算内（15.3KB~51.0KB，预算 60KB），267 城 dest 个图零回归，退出码 0。**双皮肤 + 缺图路径浏览器复验**（`python3 -m http.server` 服务生产构建于 `/next-stop-gacha/` 前缀，claude-in-chrome）：① 奶油 chengdu（无个图城市）详情页——头图确认加载 `illustrations/dest/region-xinan.webp`（`complete:true naturalWidth:960`，无 `.photo` 类，2:1），此前此路径必 404 空白，本次验证「奶油详情兜底自此有图」拍板真实生效；② 奶油线路卡「稻城亚丁·川西高原」详情——同一西南题头正确显示；③ 奶油扭蛋票券——氛围带 URL 确认解析到共享层（`region-xibei.webp`），但因 `#gachaTicket`/`.card` 完全重合无露出，肉眼与改动前一致（零回归）；④ 山水皮肤下 chengdu 详情、线路卡详情、扭蛋票券三处逐一复验——头图渲染与皮肤化边框/字体正常叠加，票券氛围带在 90px 顶部露出正确的水墨题头画（浙东南海岸渔村→`region-jzh.webp` 柳枝小景），与改动前视觉一致。四组场景控制台全程零 error。**生产复验（push cacd68c 后，Cloudflare 自动部署完成，首次探测命中过期边缘缓存报旧哈希、重试即稳定转新——同 M38/M47 记录的分钟级边缘传播窗口，非本次改动引入）**：`index-DV8WtG6u.js` 与本地过闸构建**字节级一致**（`diff` 通过，CSS 哈希未变）；真机 `lab.medspiral.com/next-stop-gacha/` 搜「成都」打开详情，头图确认加载 `illustrations/dest/region-xinan.webp`（`complete:true naturalWidth:960`），与本地复验结论一致；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M55/M57·review 响应（F61/F62，2026-07-20 cc in-session）— codex 对本轮 /loop 七项批次跨家族复核（baseline 883655a，见 review.md）新提两条，逐条处置：
  ① **F61**（P1，`extractMonths` 把「春节」误连带扩成 3-5 月春季，且跨年区间不认「上/中/下旬」修饰）——两处独立缺陷、同一函数：（a）季节/节庆两套关键词表各自独立按子串匹配，「春节」本身包含「春」，先前实现对两表都无差别扫，命中即并集，导致「春节」被误当成同时命中节庆[1,2]和季节[3,4,5]；改为节庆表先扫、命中后把该节庆子串从句子副本里挖掉再扫季节表——只挖掉命中的确切子串（如「春节」两字），句中真实独立出现的「春天」「春季」不受影响，不是无差别削弱季节匹配。（b）跨年区间正则此前只认紧凑写法「12-2月」（单个末尾「月」），真实数据「12月中旬-3月上旬」「12月下旬-2月」两侧各自带「月」+可选「上/中/下旬」，旧正则测不出这种格式、退化成两个孤立单月，丢失中间月份；新增 `kwRangeRe`（两侧「月」+可选旬修饰，`[-至]`分隔）与既有 `rangeRe`/`wrapRe` 互不重叠（前者要求数字紧邻「月」才能匹配、后者要求数字紧邻分隔符，两种格式的字符相邻关系正好互斥）。原测试 `冬季尤其春节前后最热闹`一条断言把误判并集（`{12,1,2,3,4,5}`）直接写成期望值，review 精确指出这是「从实现复制结论而非独立语义断言」——改断言为正确值 `{12,1,2}` 并显式钉住 3/4/5 不出现。新增两条真实数据回归：`langzhong`（阆中）真实 seasonNote「春节期间'阆中过大年'」在此修复后独立解析出 `{1,2}`（不含 3/4/5），5 月出发的 `filterSeasonNote` 确认该句被正确滤掉；`zhangjiakou-chongli`（崇礼）真实 seasonNote「12月中旬-3月上旬雪道状态较稳，7-8月林海避暑，9月山谷转金适合徒步骑行。」全句只有一个句号、逗号不切句，`filterSeasonNote` 按整句处理——旧正则测不出「12月中旬-3月上旬」这种带旬两侧写法，只能拆出两个孤立单月 12、3，加上 7-8月区间与单月 9，解析出的集合是 `{12,3,7,8,9}`，恰好不含 1、2；1 月出发的崇礼行程因此会把整条核心雪季提示（连同后面 7-8月避暑、9月徒步两句）一起滤成空行——与 review 描述的生产场景一致。修复后集合含 1、2，1 月出发时整句正确保留。
  ② **F62**（P2，M57 单例工艺槽位归一化撞名静默覆盖）——`tools/build_illustrations.py` 的 `process_skin_dir()` 原本对每个源文件独立调用 `classify()` 拿归一化输出名直接编码，texture/frame/divider/placeholder 四类允许任意描述性后缀、归一化后可能撞名（如 `ink-frame-a.webp`/`ink-frame-b.webp` 都归一化成 `frame.webp`），发生时脚本连续两次报告 "OK frame.webp"、只留下按文件名排序更晚的产物、退出码仍是 0——文件名排序变化即可悄悄改变上线素材。改为 `process_skin_dir()` 维护一个 `claimed: dict[out_name → 源文件名]`，同一皮肤内第二个映射到相同输出名的源文件直接计入 `violations`（非零退出）并跳过、不覆盖已产出的产物；按 `sorted(glob)` 顺序取第一个文件名（字典序）为准。未按 slot 类型分支特判——seal/region/decor 等多实例槽位输出名本身带 name 正常不会撞，用同一套「out_name 去重」兜底即可，不需要为它们单独分支（同 review 建议「继续按完整输出名去重即可」）。
  **两条均带回归测试**：`src/logic/__tests__/roadbook.test.ts` 新增 2 条真实数据回归（langzhong 春节独立解析、chongli 旬区间解析）+ 改写 1 条误导性断言；`tests/build-illustrations.test.mjs` 新增 1 条撞名回归（`probe-frame-first.webp`/`probe-frame-second.webp` 双源同撞 `frame.webp`，断言非零退出+错误信息含「撞名」+先出现的产物仍正常产出）。**未动 review.md**：按本项目既有惯例（同 M45/M46 F58–F60 处置先例）该文件由 reviewer（codex）拥有，findings 的关闭权在 reviewer 复核后，本轮只负责代码修复入库，F61/F62 仍待 codex 下轮复核确认后从 review.md 删除｜Verified：`bun run verify` 181/181（179+2 新增）+ 45 workerd 无回归；`bun run test:build-assets`（`PATH="/usr/bin:$PATH"` 规避本机 Homebrew Python 3.14 动态库签名策略拦截，同上轮已记录的环境问题）8/8（7+1 新增）；`bun run build` 干净，CSS 哈希未变（本轮零 CSS/HTML 改动，纯逻辑+管线脚本）；真实全量运行 `python3 tools/build_illustrations.py`（191 dest + ink 全部槽位）退出码 0、`git status` 确认产物字节级无漂移，证明撞名检测对真实资产零假阳性。**浏览器真实数据端到端复验**（`python3 -m http.server` 服务生产构建于 `/next-stop-gacha/` 前缀路径，claude-in-chrome，注入 `localStorage.nextstop_v2` 直接装入单站行程绕开扭蛋随机性）：langzhong 单站行程、2026-05-01 出发——生成路书后 `.rb-facts` 季节行确认只剩「3-5月与9-11月气候宜人；」，「春节」句已被滤掉（页面其他位置的「春节」字样来自该城 tagline 文案，非 seasonNote，不受本次改动影响、也不应受影响）；zhangjiakou-chongli 单站行程、2026-01-15 出发——季节行确认完整保留「12月中旬-3月上旬雪道状态较稳，7-8月林海避暑，9月山谷转金适合徒步骑行。」整句（此前会被滤成空行，此为 review 描述的确切生产场景）。两次复验控制台全程零 error。**fuzhou 图超预算问题**（此前 spawn_task 交后续处理的 P2 内容质量项）：codex/M44 track 已在本会话开始前修复（commit f5a83fd，母版重画降至 460KB/产物 35.8KB），用户确认修好，本轮顺手推送该悬空提交，随后 `spawn_task` 对应工单可视为已解决。**生产复验（push 43d87d5 后，Cloudflare 自动部署完成）**：`index-Dmiy4Thg.js` 与本地过闸构建**字节级一致**（`diff` 通过，CSS 哈希未变）；真机 `lab.medspiral.com/next-stop-gacha/` 重跑上述两个真实场景——langzhong 5 月出发路书季节行确认只剩「3-5月与9-11月气候宜人；」、chongli 1 月出发确认完整保留「12月中旬-3月上旬雪道状态较稳，7-8月林海避暑，9月山谷转金适合徒步骑行。」整句，与本地复验结论一致；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归 [cc]

- M49 — 特色主题游扩容 + 筛选标签扩充·调研段 [R2 · S2 · 🌫️] → fable 编排 · worker opus · high ×4（2026-07-20，内容批第三模块；spec 见 design M49；🌫️=收录口径与标签集变更即拍板项，调研段本身无自拍）｜4 条主题赛道并行网查（产业工艺/影视文娱/市集美食/新奇主题）。产出 `.agent/m49-findings.md` 工单：新卡候选 23 个三档（T1 强推 8：诸暨珍珠种子/义乌/文昌航天/澄江化石世遗/榕江村超/龙泉剑瓷/查干湖冬捕/荣昌；T2 备选 8；T3 存疑交拍 7 含单体幻城口径与宜兴丁蜀查重定性）+ tag 提案 5（`实景演艺` 强推 15-25 张回标、`手作体验`/`市集`/`星空航天` 条件推、`影视基地`/`工业遗产` 暂缓）+ 既有卡补 tag/文案清单约 25 项 + 落选记录约 45 条（防重复调研）+ 拍板项 5。**停在拍板 gate，零入库**；拍板通过部分按 E 路径续做（checklist 自检→build 过闸→codex 跨家族抽查 gate）。checklist 回写 2 条新规（注 M49）｜Verified：fable 收口抽验——21 个新候选池内查重 21/21 无重名、11 条「已收录」断言 11/11 命中、跨赛道重复（义乌）合并、tag 提案跨赛道收口（市集/淘货合并）；worker 原文 4 份存会话 scratchpad（m49-results/） [cc]

- M55 — 路书语境化：全局日序 + 当季文案 [R2 · S2] → sonnet · high · cc in-session（2026-07-20，/loop 第五轮）｜spec 见 design M55。落地两处渲染层整改（`logic/roadbook.ts` 新增纯函数 + `logic/roadbook.ts`/`ui/roadbook.ts` 两处消费，city 详情/对比页从不经过这些函数、零改动）：
  ① **日序统一**——`remapDayCodes(route, start)` 按站 `start` 偏移正则替换 `D<n>`/`D<n>-<m>`（`offset=start-1`，首站天然 no-op）；**implementer 拍定**（design 未明说是否受 `tripStart` 门控）：日序重映射与是否设了出发日期**无关，永远生效**——`skelDayLabel` 的全局 D 序号本就无条件显示（现状代码，非本轮新增），城市段文案不跟它对齐的话，不设日期时 bug 依旧存在，「未设日期展示零变化」这条验收只适用于详情/对比页（它们从不过这条渲染路径）与当季过滤本身，不适用于日序重映射。
  ② **当季过滤**——`extractMonths(sentence)` 解析单月/`N-M月`/`N至M月`/`N月至次年M月`（跨年环绕）/春夏秋冬/春节五一暑假国庆，无时间词返回 `null`（=无条件保留，非"无法判断"的兜底）；`filterSeasonNote(note, stayMonths)` 按「；。」切句（顿号/逗号不切句——这是 hangzhou 类"3-4月…、9-10月…"单句多时段文案在真实数据里天然保留双时段的原因，不是 bug）；`stayMonths(start, end, tripStart)` 按站实际停留日逐日取月份集合（跨月停留天然落两个月）。**只在设了出发日期时启用**（未设=全文展示，现状不变）。HTML 端：`.rb-facts` 里 `🌤 季节` 从固定展示改条件展示，全滤则整个 `<span>` 省略；文本导出新增「当季提示」独立行（原来没有），仅过滤后有内容才追加。
  ③ **content-checklist 四节离线审计**：`looksTimeSpecific(sentence)` 关键词启发式（旺季/淡季/花期/雨季/干季/汛期/黄金周/巅峰期/高峰期/节假日）+ `sentencesOf()` 切句复用，扫真实 267 城 seasonNote 抓"像时间词但 extractMonths 解析不出月份"的句子——非阻塞（不断言数量，只断言扫描本身跑得通+每条真解析不出），跑出 8 句真实清单（`putuoshan`「避开农历初一十五」等，均为设计意料内的漏网案例：农历/无数字节气/纯定性词）供后续内容补写月份。｜Verified：`bun run verify` 166/166（148+18 新增，`src/logic/__tests__/roadbook.test.ts`：`remapDayCodes` 4 条（首站 no-op/偏移/降配无关性/无编码原样）+ `extractMonths` 3 条（单月区间跨年至次年/季节节庆并集/无时间词 null）+ `filterSeasonNote` 5 条（**真实数据用 design 验收原句 linzhi 实测**/分隔符带出/全滤空串/空 note/单句无分隔符）+ `stayMonths` 2 条（跨月/单月）+ 渲染层集成 2 条（**真实数据 hangzhou+linzhi 两站行程，9 月出发滤桃花季保留海拔、3 月出发桃花季保留**——验证的是"真按当季过滤在起作用"不是"恒定滤掉"；未设日期零"当季提示"行）+ 审计 2 条；均手动 revert 验证过会红）+ 45 workerd 无回归；`bun run build` 干净，CSS 哈希未变（纯逻辑改动零样式改动）。**浏览器全链路复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome；本模块无 CSS 改动，主验证 cream + ink 零回归抽查）：hangzhou(D1-2)+linzhi(D3-6) 两站行程、2026-09-05 出发——路书 HTML 逐日速览与站段标题 D 序连续（D1→D6 无跳号缺号）；linzhi 段方案文本原始 `D1 抵林芝；D2…D3…D4 鲁朗林海` 正确重映射为 `D3 抵林芝；D4…D5…D6 鲁朗林海`（与该站 `start=3` 精确对应）；季节行只显示「海拔约3000m，是进藏适应的好起点」，「3月底桃花季」确认不出现；换成 2026-03-20 出发重验同一站——「3月底桃花季」正确重新出现（证明过滤方向依赖真实日期，非写死单向剔除）；拦截 `navigator.clipboard.writeText` 捕获「复制路书文本」真实产出的文本串，`当季提示：海拔约3000m` 行确认存在、「桃花季」确认不在文本导出里、D 序同 HTML 端一致重映射；未设出发日期时 hangzhou 季节行显示完整原文（含 3-4 月与 9-10 月两段，顿号不切句的预期行为，非回归）；ink 皮肤下同一行程渲染截图确认手绘描边/宿地色标无恙。控制台全程零 error。**生产复验（push f54a2f5 后，Cloudflare 自动部署完成）**：`index-XJObt2R0.js` 与本地过闸构建**字节级一致**（`diff` 通过，CSS 哈希本轮未变）；真机 `lab.medspiral.com/next-stop-gacha/` 同一 hangzhou+linzhi 9 月行程——`.rb-plan` 文本确认 `D3 抵林芝；D4…D5…D6 鲁朗林海`（全局重映射）、`.rb-facts` 季节行确认只剩「海拔约3000m，是进藏适应的好起点」（桃花季句已滤）；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M51 — 共享集皮肤适配滤镜 [R1 · S1] → sonnet · medium · cc in-session（2026-07-20，/loop 第六轮）｜spec 见 design M51「主题皮肤系统·共享集皮肤适配滤镜」。落地：`--dest-filter` CSS 自定义属性（cream.css=`none`、ink.css=`saturate(.72) sepia(.1)`——目的地共享集本身已是低饱和墨线淡彩速写，再收一档饱和度+一点向 `--paper`（`#faf3e3` 暖纸色）靠拢的暖调，让白框贴画在水墨语境下的残余违和更小）；`style.css` 新增 `.c-photo .illust, .dt-banner.photo .illust { filter: var(--dest-filter, none); }`——**只挑 `.dt-banner.photo` 而非全部 `.dt-banner`**（M58 刚引入的修饰类，标记「当前显示的是目的地共享集个图」）：题头兜底/线路卡走的是各皮肤自有的原生题头插画，不该被这条滤镜链调和（那本来就是该皮肤自己的画风，调和了反而奇怪）——M58 的 `.photo` class 在这里被直接复用为「该不该吃滤镜」的判据，两个模块的机制无缝衔接。`--dest-filter` 缺失或未来某皮肤的值引用了不存在的 SVG 滤镜 id 时，浏览器原生按 `none` 处理，「滤镜失败优雅回退原图」不需要额外代码兜底。青花/doodle(画报) 两皮肤design 段落里描述的滤镜配方（feComponentTransfer 灰度→钴蓝映射、灰度线稿化）**尚未落地**——这两个皮肤本身还没进 `SKINS` 注册表（P2 backlog），token 机制已经为它们预留好扩展点（未来皮肤只需在自己的 css 里声明一行 `--dest-filter`，不改任何 TS/HTML），validate 留给它们各自的落地模块。｜Verified：`bun run verify` 170/170（166+4 新增：`src/skins/__tests__/dest-filter.test.ts` 覆盖奶油零滤镜/山水吃滤镜/`.photo` 态与非 `.photo` 态分野/两文件都显式声明该 token 防漂移，均手动 revert 验证过会红——首版 revert 后 3/4 失败、`.photo`/非 `.photo` 分野那条因断言写成 `not.toBe("none")` 被 happy-dom「无规则命中时算空串非字面量 none」的环境特性意外放过（空串 !== "none" 恒真，测不出滤镜真被删掉），改断言为 `toContain("saturate")` 后复测 4/4 全红，修复后 4/4 全绿）+ 45 workerd 无回归；`bun run build` 干净，CSS/JS 哈希均变化（本模块唯一改动 CSS，JS 哈希联动变化系 Vite 构建产物哈希机制正常行为，非代码改动泄漏）。**双皮肤浏览器复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome）：cream 皮肤卡片网格目的地图色彩原样；切 ink 皮肤同一批卡片实测 `getComputedStyle(img).filter === "saturate(0.72) sepia(0.1)"`，目检色调明显更柔和、与宣纸底色/水墨氛围融合度提升（对比同一批卡片 cream/ink 截图，杭州西湖图从鲜绿转为更接近画面里墨线淡彩基调的暖调灰绿）；详情页个图（`.photo` 态，hangzhou）同款滤镜生效，画面完整可辨认、未过曝或过灰；详情页线路卡（无 `.photo`，用大区题头 `region-jzh.webp`）实测 `filter:"none"`——题头插画本来就是水墨原生画风，未被二次调和，与设计预期一致。控制台全程零 error。**生产复验（push 4d50fe3 后，Cloudflare 自动部署完成）**：`index-C0IJ0Vpu.css` 与本地过闸构建**字节级一致**（`diff` 通过）；真机 `lab.medspiral.com/next-stop-gacha/` 切 ink 皮肤实测 `getComputedStyle(img).filter === "saturate(0.72) sepia(0.1)"`；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M57 — 皮肤工艺件接入 [R1 · S2] → sonnet · medium · cc in-session（2026-07-20，/loop 第七轮，本轮批最后一项）｜spec 见 design M57（含本轮新增「消费机制的具体接线」段）。落地：
  ① **`tools/build_illustrations.py`**：新增 TEXTURE(512²)/FRAME(1024²)/DIVIDER(1024×32)/SEAL(256²) 四个尺寸预算元组（placeholder 复用既有 BANNER 960×480，同题头位画布契约）；`classify()` 扩五类槽位——texture/frame/divider/placeholder 描述性后缀（如 `ink-texture-paper.webp`）归一化裁成裸槽位名（`texture.webp`），因这四类皮肤内单例、后缀只是这批母版的自选说明不是稳定标识符；seal 例外保留 `seal-<name>` 完整名（同 region-/decor- 的多实例先例，两枚印章各自不同用途不能合并）。跑真实管线：ink 六槽位全部预算内产出（divider 8.1KB/frame 32.5KB/placeholder 5.0KB/seal-nextstop 13.5KB/seal-wheretoplay 12.2KB/texture 1.6KB，预算均 60KB）。
  ② **`src/skins/illustrations.ts`** 新增 `applyCraftAssets(dir)`：texture/frame/divider/placeholder 四槽位算好 URL 写成 `--craft-<slot>` CSS 自定义属性挂 `documentElement`，`applySkinVisuals` 无条件调用（不进 `SkinDeclaration`——皮肤是否真消费由该皮肤自己的 CSS 决定，cream.css 零引用故零请求，浏览器实测确认：`read_network_requests` 过滤 `cream/(texture|frame|divider|placeholder)` 零命中）。seal 不在这份列表——两枚印章走既有 `[data-illust]`+`wireIllustFallbacks` 机制（见④），复用度更高、不必为保留带 token 引用的 SVG 文字内容再造一遍 data-URI 方案。
  ③ **`src/style.css` 四类消费点**：**frame**——`.console`/`.paper`（border-image-width 8px/outset 3px）与 `.machine`/`.share-sheet`（9px/4px）border-image-source 指向 `--craft-frame`，border-color 从 M52 时期的 `transparent` 改回 `var(--ink)`（缺图回退基线）；**移除**原「原边框透明+`#inkWobble`滤镜伪元素」整套机制（不再需要间接层，border-image 直接画在真实边框盒上；`#inkWobble` 滤镜定义随之从 index.html 清除，`#sealRough` 仍在用保留）。**texture**——`body::after` 两层 background-image：`--craft-texture` 真图在前，M52 时期 feTurbulence data-URI 程序噪点原样留作第二层兜底。**divider**——四处消费点（`.leg-line .dash`/`.rb-sk-row`/`.console-foot::before`/`.rb-cover::after`）均加 `--craft-divider` 图层（整条一笔非等比拉伸铺满宽度，同 frame 中段拉伸是「工艺件画布契约」明确接受的代价）叠在原 repeating-gradient 之上。**placeholder**——`.c-photo`/`.dt-banner` 加 `--craft-placeholder` 图层叠在 M52 远山剪影渐变之上。**实现期发现**：`.c-photo`/`.rb-sk-row` 原用 `background:` 简写多层语法，happy-dom 不支持简写的 computed style 展开（真实浏览器没问题，纯测试环境限定），为了可测试性顺手拆成 `background-image`/`-position`/`-size`/`-repeat` 分写属性——视觉结果不变，只是写法更明确。
  ④ **印章**（index.html）：两枚 `.ink-seal` 从「就是那枚 SVG」改造成小容器——`<span class="ink-seal"><svg class="ink-seal-svg">…</svg><img class="illust ink-seal-img" data-illust="seal-nextstop|seal-wheretoplay" data-fallback="hide"></span>`；`.ink-seal-img` 绝对定位铺满、叠在 SVG 之上（DOM 顺序决定层叠）；**关键实现决定**：`<img>` 刻意不标 `data-illust-frame`——`wireIllustFallbacks` 的「隐藏」分支在找不到 frame 祖先时退化为「只隐藏 img 自身」，SVG 代码版因此天然透出当兜底，不需要新造一种回退分支。｜Verified：`bun run verify` 179/179（170+9 新增，`src/skins/__tests__/craft-assets.test.ts`：`applyCraftAssets` 四槽位+确认 seal 不在列表 1 条、frame border-image-source 四容器+缺图回退纯色 2 条、texture 两层顺序（源码断言）1 条、divider 四消费点（2 条计算样式+2 条源码断言，`console-foot::before`/`rb-cover::after` 是伪元素 happy-dom 不支持计算样式改断源码）3 条、seal DOM 结构+叠层 2 条；均手动 revert 验证过会红）+ 45 workerd 无回归；`bun run test:build-assets` 7/7（原 5 条 + 新增 2 条：四类单例槽位归一化命名、seal 多实例保留全名，均用仓库里真实 A8 母版跑，未合成图片）；`bun run build` 干净，`dist/illustrations/ink/` 确认含六个新槽位产物；CSS/JS 双哈希均变化（本模块以 CSS 为主但也改了 illustrations.ts，JS 联动变化合理，非泄漏）。**双皮肤 + 缺图回退三态浏览器复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome）：山水下筛选台/扭蛋机/分享弹层三个大容器 border-image 手绘边线目检确认——**首版 `border-image-width: var(--bw-thick)`（1.5px）渲染出的边线太薄看不出笔触**，改成 8~9px 固定值后笔触清晰可辨（截图前后对比确认，此为实现期迭代非缺陷）；两枚印章截图确认为带糙边质感的真实手绘图（非 M52 时期的平涂 CSS 印章块）；`read_network_requests` 确认真实生产构建下无浪费请求（cream 皮肤零个 craft 请求）。**缺图回退实测**（`document.documentElement.style.setProperty` 手动把四个 `--craft-*` 指向不存在文件模拟真实 404，非仅 happy-dom 断言）：console/machine 边框实测视觉肉眼回退为 M52 前的纯色细边框，无破图标、无空白间隙；控制台全程零 error（404 本身不产生 console.error，只是网络层记录）。**实现期顺带发现一处内容问题**（跑 M42 管线为生成 ink/ 六槽位而触发对 191 城 dest 母版的全量重编码时抓到，非本模块引入）：`dest-fuzhou.webp` 母版在 cwebp q40 最低质量档仍编码出 43.3KB，超过目的地卡位 40KB 预算——`build_illustrations.py` 按 F60 校验规则计入违规非零退出（产物仍写出供人工核对）；已 `spawn_task` 交后续会话处理，不阻塞本模块｜产物随手同步（跑管线的自然副作用，非本模块范围但顺带入库）：`public/illustrations/dest/` 从 64 张追平到 191 张（codex 并行铺量批的产物，母版已提交、产物此前一直缺失，本次运行一并补齐）。**生产复验（push fc0c86c 后，Cloudflare 自动部署完成）**：`index-B1Sj-nO9.css` 与本地过闸构建**字节级一致**（`diff` 通过）；真机 `lab.medspiral.com/next-stop-gacha/` 切 ink 皮肤实测 `#console` `border-image-source` 指向生产 `frame.webp`、`border-color` 回退值正确；两枚印章 `naturalWidth:256`/`complete:true` 均加载成功；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。**本轮 /loop 七项批次至此全部落地并生产验证通过**。 [cc]

- M56·数据体检段（通达性网查）[R2 · S2] → fable 编排 · worker sonnet/opus ×15 + 反方复核 opus ×3 + 补查 opus ×1（2026-07-20，内容批第二模块；spec 见 design M56 数据体检段，机制段另行）｜267 城 15 片并行（西部习惯区 6 片 opus·high：新疆/青甘/川渝/藏区/云南/内蒙，东中部 9 片 sonnet·medium 扫盲）。产出 `.agent/m56-findings.md` 工单：noair/norail 候选按档呈现（双标 31/单 norail 12/单 noair 约 60/镇景区级 5 随粒度拍板）+ transit 矛盾 29 条（硬伤 6/过时 15/用词 8）+ 习惯区组合标签改法 12 城 + 高时效复卡清单 18 项 + 拍板项 3（本体粒度三档口径/状态不稳定独立态/落库形式）。**反方复核翻案 3 例**（安阳红旗渠机场 2023 通航、忻州五台山机场 2015 通航、阿里普兰机场 2023 通航——共同失败模式=被忽略的既有低频支线机场，教训入 checklist）；「在建转通航」零翻车。补查 15 项定案 14（神农架站名裁定：无「神农架南站」，M48 分片10 说法纠正；抚远夜车实存；黑河/北安「高铁」硬伤确认；绥芬河已有 D 动车；义乌站 2026-01 已恢复无需改）。执行插曲：首轮耗尽会话 WebSearch 配额（200）致后到分片留存疑不硬拍，收口改道 WebFetch/tavily 补查定案；两个复核 agent 首派异常速退重派后正常；E1/E2 分片曾漏写盘被东部复核代理抓出、补盘补核（16 组全维持）。checklist 回写 9 条新规+1 条范式（注 M56）。存量只读纪律执行：零数据改动，清单交用户过目后才落库｜Verified：fable 抽查 37/37 摘录与 data 原文命中零幻觉；候选 92 组全部过换视角反方复核（维持 84/翻案 3/口径挂起 5）；现存疑仅 3 项（武威 noair 单源、偏关 norail 单源、额济纳旺季加密）均标注「倾向维持不硬拍」；worker/复核原文 18 份暂存会话 scratchpad（m56-results/） [cc]

- M58 — 目的地图展示语义：卡位扁条、详情位全图 [R1 · S1] → sonnet · low · cc in-session（2026-07-20，/loop 第四轮）｜spec 见 design M58「目的地图展示语义」。落地：①`src/style.css` `.c-photo`（卡片流）3:2→2:1；`.dt-banner`（详情头图）新增 `.photo` 修饰类=3:2，不带该类的默认态维持 2:1（题头兜底/线路卡）；②`src/ui/detail.ts` `headerBannerHTML` 按 `isRoute` 在渲染时决定要不要打 `.photo`（城市卡默认打，线路卡不打）；③`src/skins/illustrations.ts` `wireIllustFallbacks` 新增通用能力——img 若带 `data-fallback-frame-toggle=<class>`，第一级 fallback（换 src 重试）触发时同步从最近的 `[data-illust-frame]` 摘掉该 class，不写死"photo"字符串，`detail.ts` 声明 `data-fallback-frame-toggle="photo"` 消费；换源（个图→题头图）与换帧比（3:2→2:1）保证同步，不会拿 3:2 框硬撑一张 2:1 题头图。**实现期浏览器复验顺带抓到一个真实生产 bug（[interrupt] 并入本轮）**：`headerBannerHTML` 沿用至今的 `loading="lazy"` 与 [interrupt] 一轮修过的票券卡图是同一个坑——img 插进 `detailBody.innerHTML` 时 `#detailOverlay` 还是 `display:none`，紧接着同步加 `.show`，懒加载可视观察永远等不到"从隐藏切到可见"这一刻；本地复测证实：未被浏览器缓存过的目的地个图/题头图在详情页里恒不加载（`complete:false`，`read_network_requests` 确认连请求都没发出），只是先前测试都恰好先在别处（卡片网格/扭蛋票券）预热过同一张图的缓存才没露馅。两处 `<img>`（城市卡个图、线路卡题头图）`loading` 均改 `eager`——card 网格自身的 `.c-photo` 不受影响（图片在正常文档流里随滚动进出，懒加载语义本来成立，未改）｜Verified：`bun run verify` 148/148（142+6 新增：`src/skins/__tests__/illustrations.test.ts` 2 条覆盖 `data-fallback-frame-toggle` 存在/不存在两分支，`src/ui/__tests__/detail-banner-ratio.test.ts` 4 条覆盖城市卡/线路卡的 `.photo` 归属与两处 `loading=eager`，均手动 revert 验证过会红）+ 45 workerd 无回归；`bun run build` 干净。**双皮肤浏览器复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome）：卡片网格 `.c-photo` 计算样式 `aspect-ratio:2/1` 实测比例 2.000；城市详情（hangzhou）头图 `aspect-ratio:3/2`、`.photo` 存在、图片 `complete:true naturalWidth:640`，截图确认西湖水墨画完整无裁切；线路卡详情头图 `aspect-ratio:2/1`、无 `.photo`，图片正常加载；**缺图两级回退双路径验证**：cream 皮肤下阆中（无个图母版）打开详情——个图 404 触发 fallback 换源到题头图、题头图在 cream 下同样 404（cream 无题头图资产）→ 整个 `.dt-banner` 正确隐藏（"两者都缺才整块不占位"，非 bug）；切 ink 皮肤同一操作——个图 404 换源到题头图、题头图在 ink 下**有资产**成功加载（`naturalWidth:960`），`.photo` 类被正确摘掉、容器帧比落回 2:1，截图确认西南大区题头山水画完整显示无破图。两皮肤下控制台全程零 error。**生产复验（push e1589df 后，Cloudflare 自动部署完成）**：`index-Bo0_iylB.js`/`index-Cp4tsC8I.css` 与本地过闸构建**字节级一致**（`diff` 通过）；真机 `lab.medspiral.com/next-stop-gacha/` cream 皮肤 hangzhou 详情——`.photo` 存在、`aspect-ratio:3/2`、`loading=eager`、`complete:true naturalWidth:640`；切 ink 皮肤搜「阆中」（无个图母版）——个图 404 换源到 `region-xinan.webp` 成功（`naturalWidth:960`），`.photo` 类正确摘掉、容器回落 `2/1`，与本地复验结论一致，证明 eager 修复与换源换帧比联动在真实边缘节点/真实网络条件下同样成立（非本地 http.server 特有）；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M48 — 线路全量体检·体检段 [R2 · S2] → fable 编排 · worker opus · high ×10 + 反方复核 opus ×1（2026-07-20，内容批第一模块；spec 见 design M48）｜53 线按地理走廊分 10 片并行网查体检（checklist 三节+一节+二节联查逐项过、逐项留依据）；机械项（leg 成组/stays/天数契约/stop id/alt 传播/门户 id）离线脚本先行清零，20 条 plan 疑漏站软提示 worker 人审 20/20 判分词误报。产出 `.agent/m48-findings.md` 工单：**阻塞1**（晋北线沪太高铁 5.5h 实为约 9h）/**警告6**（茶卡「动车」实为 Y 字头旅游专列、呼伦贝尔线路卡与城市卡直飞矛盾、洛阳 4.5h 低估、晋北并入木塔悬空寺两独立卡、桂林与四姑娘丹巴 difficulty 口径冲突）/**建议23**/**拍板项3**（difficulty 全库口径、晋北重叠、太原修法→🟡）。存量只读纪律执行：零数据改动。checklist 回写 8 条新规（注 M48）：两端门户自述、旅游专列≠动车、时长实测口径、线路卡↔城市卡交叉自洽、独立卡「可接」指引、无 leg 线披露口径、易变信息对冲、体检编排范式｜Verified：fable 抽查——全部驱动改动的摘录逐条与 data 原文核对为真（含 4 组 difficulty 冲突、2 处线路卡↔城市卡矛盾直接实证），无幻觉摘录；6 条事实性阻塞/警告级结论 opus 换视角反方复核（只验依据链）6/6 成立零推翻（沪太 6 班直达 8h41m-9h42m 无 6h 内班次、沪洛最快 5h26m、茶卡站无 D/C 常态班次、沪海拉尔直飞每周约 23 班约 3.5h、荆州→武当山西同实体车汉口改号须分段购票、蟳埔=国家级非遗且泉州 22 世遗点逐点比对不含）；worker 交付原文 10 片全量暂存会话 scratchpad（m48-results/），工单消费前可追溯 [cc]

- M54 — 行程站数上限放宽 [R1 · S1] → sonnet · low · cc in-session（2026-07-20，/loop 第三轮）｜spec 见 design M54。落地：`TRIP_MAX` 6→10（`src/logic/constants.ts` 单点）；三处 cap 检查各自持有的 toast 文案原先硬编码「6 站」（`src/ui/trip.ts` `insertOnWay` 一处、`src/ui/actions.ts` `toggleTrip`/`addRouteToTrip` 两处），改用模板字符串插值 `${TRIP_MAX}`，此后再改上限不会漏改文案。边界复核（design「已核」结论现场验证一致）：`persist.ts` 的 `sanitizeTripItems` 只按 `days∈[1,14]`+城市 id 过滤，无数组长度上限；`cloudflare/worker.mjs` 分享 API 无逐项计数校验，只有 payload 体量与单站天数约束，10 项 JSON 远小于 8KB｜Verified：`bun run verify` 142/142（139+3 新增：`src/ui/__tests__/trip-max.test.ts` 覆盖 `toggleTrip`/`addRouteToTrip`/`insertOnWay` 三条装入路径的 cap 行为与 toast 文案，`insertOnWay` 一例利用其 cap 检查先于顺路几何计算的实现顺序、无需构造真实顺路场景即可测早退；均手动 revert 验证过会红：单独回退常量与单独回退文案两种缺陷分别单测复现）+ 45 workerd 无回归；`bun run build` 干净，CSS 产物哈希与 M53 相同（本模块零 CSS 改动）。**浏览器全链路复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome；本模块无视觉改动，主验证在 cream，ink 做零回归抽查）：cream 皮肤连续点击 11 张卡片「行程」按钮，第 11 次被拒绝、`localStorage.trip.length===10`、toast 精确为「一次行程最多 10 站，贪多嚼不烂～」；打开行程单显示全部 10 站，点「自动排序」（顺路排序）无报错、按最近邻结果重排（苏州→乌镇西塘→莫干山→杭州→绍兴→宁波→…）；生成路书成功，标题栏「10 站 · 总里程约 1589km」，逐日速览 D1–D21+ 完整、宿地/交通逐段正确；ink 皮肤下同一 10 站行程单渲染无破版（手绘描边/宿地色标正常）。控制台全程零 error。**生产复验（push 344a15d 后，Cloudflare 自动部署完成）**：`index-XiZDHy2a.js` 与本地过闸构建**字节级一致**（`diff` 通过，CSS 哈希本轮未变）；真机 `lab.medspiral.com/next-stop-gacha/` 实点 10 张卡「行程」全部装入；打开路书后点「分享」→ `read_network_requests` 抓到 `POST https://lab.medspiral.com/next-stop-gacha/api/share` 返回 **200**（真实携带 10 站 trip payload 过 Worker 短链创建，design「边界已核」的 8KB/单站天数校验现场验证未被 10 站触发拒绝）；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M53 — 定向扭蛋：对比池抽签 [R1 · S2] → sonnet · medium · cc in-session（2026-07-20，/loop 第二轮）｜spec 见 design M53。落地：①`src/logic/constants.ts` 新增 `CMP_MAX = 6`（原硬编码 4），`src/ui/actions.ts` `toggleCmp` 改用该常量、toast 文案同步为「最多对比 6 个哦」；②`src/ui/gacha.ts` 新增模块级 `cmpPoolOverride`，`openGacha(cmpPool?: Destination[])` 与内部 `currentPool()` helper——传入池覆盖当次会话直到下次 `openGacha()`；`roll()` 改用 `currentPool()`；scope 行按来源分叉：对比池显示「对比池 · 共 N 颗」，全量池维持原「扭蛋池：<筛选条件>｜共 N 颗蛋」；对比池不触发「放宽建议」（`relaxCandidates()` 语义是筛选收窄，套对比池会文不对题）；③两个入口——dock 对比行与对比表弹层各加一个「就从这几个里抽」按钮，`events.ts` 分别接线 `openGacha(state.cmp.map(byId).filter(非空))`；表格入口额外先关 `cmpOverlay`（与 `gachaOverlay` 同为默认 z-index:50，不关闸门更晚的 DOM 序会盖住新开的扭蛋层）。**实现期修正一处 spec 字面理解**：design 原话「对比 dock 行…加「就从这几个里抽」入口」隐含文字按钮，首版真的加了个 `.dock-go` 文字按钮（🎰 扭这几个），浏览器实测 `cmpBox` 自然宽度 618px（6 城 chips 320px 上限 + label + 比一比 + 新按钮 100.6px + ✕ + gaps/padding）在移动端 366px 预算下会溢出——本环境拖不动真实窗口到 390px（M47/M50 已知限制），改用 `getBoundingClientRect` 直接测量各子项自然宽度核实溢出风险，随后把 dock 入口换成 M50 同款 `.icon-btn`（35px 圆钮 + `::after` 外扩热区 ≥44px，`aria-label`/`title`="就从这几个里抽"），非移动端不受影响的 dock-go 保留给「比一比」；对比表弹层空间充裕，维持原文字按钮「🎰 就从这几个里抽」。`--orange-deep` 复用既有 token（无新增色值，过 no-hardcoded-visuals 闸）｜Verified：`bun run verify` 139/139（136+3 新增：`src/ui/__tests__/gacha-compare-pool.test.ts` 覆盖对比池覆盖/roll 限定池内/`openGacha()`不传参回落全量池不残留/`CMP_MAX` 上限拒绝第 7 个，均手动 revert 验证过会红）+ 45 workerd 无回归；`bun run build` 干净。**双皮肤浏览器复验**（`python3 -m http.server` 服务生产构建，claude-in-chrome；CSSOM 改写 680px 断点手法同 M47/M50/M52 走移动端布局路径，真实窗口无法收窄到 390px 的环境限制下改用 `getBoundingClientRect` 测量替代肉眼窄屏截图）：真实点击 6 张卡片的「对比」按钮（发现 `Array.from(...).slice().forEach(click)` 的陈旧引用在首次 `render()` 重建 `#grid` 后失效——`toggleCmp`→`render()` 替换整个 grid innerHTML，脱离文档树的旧节点点击不再冒泡到 `document` 委托监听，改为逐次重新查询未 `.on` 的按钮再点，一次一个）；第 7 次点击被 `CMP_MAX` 正确拒绝（toast「最多对比 6 个哦」，`localStorage.cmp` 长度维持 6）；cream/ink 两皮肤下 dock 行 🎰 图标钮与对比表「🎰 就从这几个里抽」文字按钮均正确开启扭蛋层，scope 行「对比池 · 共 6 颗」；roll() 抽中目标（南京/绍兴/杭州/苏州/扬州等，多次独立验证）全部落在 6 城对比池内，未见池外目的地；表格入口点击后 `cmpOverlay.classList.show===false && gachaOverlay.classList.show===true`（两层同 z-index 的正确切换，未双层重叠）；票券图沿用 [interrupt] 修复的 eager 加载，图片当场完整显示；控制台全程零 error。**生产复验（push 7981e6b 后，Cloudflare 自动部署完成）**：`index-C4v9VoYu.js`/`index-BGPprB4C.css` 与本地过闸构建**字节级一致**（`diff` 通过）；生产 CSS 已含 `.icon-btn.cmp-gacha` 规则；真机 `lab.medspiral.com/next-stop-gacha/` 实点 6 张卡「对比」→ 点 dock 🎰 图标钮 → scope 显示「对比池 · 共 6 颗」→ 扭中 shaoxing（池内目的地，`loading=eager/complete=true`）；控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- [interrupt] 两条线上小修：票券卡图 eager + `.illust[hidden]` 布局盒守卫 [R1 · S2] → sonnet · medium · cc in-session（2026-07-20，/loop 第一轮）｜此前两次 commit（a2e5bf5/25bf162）标题读起来像修复，实际只改了 state.md 记issue，代码从未真的改过——本轮补上真实代码修复：①`src/ui/gacha.ts` roll() 里 `ticketEl.innerHTML = cardHTML(pick, 0)` 之后补一行 `querySelector(".illust")?.setAttribute("loading", "eager")`——只改票券这一条路径，不动 `cards.ts` 共享模板（网格 300+ 卡仍要 lazy，票券容器是唯一「从 display:none 切入导致懒加载可视观察永不触发」的调用点）；②`src/style.css` 补 `.illust[hidden] { display: none; }`（`.illust { display: block }` 是普通作者规则、优先级高于 `[hidden]` 的 UA 默认样式，两条规则同特异度时更具体的属性选择器胜出）。新增两个回归测试文件（happy-dom）：`src/ui/__tests__/gacha-ticket-eager.test.ts`（mkCity 造池、fake timers 跑完 roll() 动画、断言票券 img `loading=eager`；revert 修复后手动验证会红）、`src/ui/__tests__/illust-hidden-guard.test.ts`（`readFileSync` 加载**真实** `src/style.css`（非手抄片段）注入 happy-dom `<style>`，断言 hidden 态 `.illust` 计算 `display:none`、非 hidden 态仍 `display:block`；同样手动 revert 验证过会红）｜Verified：`bun run verify` 136/136（133+3 新增）+ 45 workerd 无回归；`bun run build` 干净。**双皮肤浏览器复验**（`python3 -m http.server` 服务生产构建于正确 `/next-stop-gacha/` 前缀路径，claude-in-chrome）：ink 皮肤搜「杭州」扭中后 `img.loading=eager/complete=true/naturalWidth=640/naturalHeight=427/hidden=false`（票券即时显示水墨西湖插画，无需二次触发）；切 cream 皮肤重验同一断言同样通过；两皮肤搜「阆中」（无 dest 母版）扭中后 `.c-photo` 计算 `display:none`、`.illust.hidden=true`，票券截图确认无破图框、卡片从「上海→四川·西南」条带直接接城市名行，无空隙；控制台全程零 error。**生产复验（push ef91751 后，Cloudflare 自动部署完成）**：`index-v5wghxHD.js`/`index-QIqaJrr5.css` 与本地过闸构建**字节级一致**（`diff` 通过）；生产 CSS 已含 `.illust[hidden]` 守卫规则；真机 `lab.medspiral.com/next-stop-gacha/` 搜「杭州」扭中后 `img.loading=eager/complete=true/naturalWidth=640/naturalHeight=427`，控制台零 error；Lab 首页 `/` 与游戏路径均 200 零回归。 [cc]

- M45/M46·review 响应（F58–F60，2026-07-20 cc in-session）— codex 一轮 gate review 审毕（baseline 533a431，见 review.md）新提三条，逐条处置：
  ① **F58**（P1，`assetDir`/字体声明未被运行时真实消费）——`src/skins/illustrations.ts` 新增 `assetDirFor(skinId, skins = SKINS)`（skins 参数可注入，测试无需碰全局注册表即可造 `id !== assetDir` 探针）；`illustSrc` 的参数语义改为「已解析好的 assetDir」而非 skinId，`applySkinVisuals`/`ui/detail.ts`/`ui/gacha.ts` 三处调用点全部先过 `assetDirFor()` 再拼 URL——运行路径现在真的解析 `SkinDeclaration` 才拿到目录名，不是拿 skinId 字面量硬拼。`registry.ts` 头注纠偏：`fonts` 字段明确「不参与任何运行时选择」，CSS token（`--round`/`--sans`）才是唯一真相源与懒加载/失败回退机制，`fonts` 唯一用途是被 drift-pin 测试核对，不应再被记作「被消费」——「有测试盯着」与「被运行时消费」是两件事，此前的注释混为一谈，design/M46 验收条文本身未改（其表述「字体管线有可执行证据」并未过度声明，过度声明只在代码注释里）。新增 3 条 `assetDirFor` 探针测试（自定义 skins 数组证明解耦、真实 SKINS 下仍按字段取值、未注册 skinId 兜底）。
  ② **F59**（P1，缺图回退不可逆删除插画槽位，双皮肤往返切换永久丢 mascot/gacha/empty）——`wireIllustFallbacks` 终态从「替换/移除 DOM 节点」改为「隐藏但保留 `data-illust` 元数据」：emoji 兜底改为同级插入 `<span class="illust-fallback">`（img 本身只 `hidden=true`，不再 `replaceWith`）；`hide` 兜底改为隐藏最近的 `[data-illust-frame]` 容器（找不到则隐藏 img 自身），不再 `.remove()`。`applySkinVisuals` 相应补上「唤醒」逻辑：切换皮肤时对每个静态槽位清 `fallbackTried`、摘掉遗留的 `.illust-fallback` 兜底 span、把 frame/img 的 `hidden` 复位为 `false`，再重新赋值 `src` 触发重试——哪怕新皮肤仍缺图，重试只是再走一遍回退（不会更差）；但只要目标皮肤真的有资产，之前因为切到过无资产皮肤而被隐藏的槽位就能重新出现。新增回归测试：mascot/gacha/empty 三槽位完整走一遍「山水成功 → 切奶油 404 触发三级回退各分支 → 切回山水验证 src/可见性/回退残留全部恢复」，并更新两条旧测试的断言（此前断言「节点被删除」，现在断言「节点仍在只是隐藏」）。
  ③ **F60**（P2，`tools/build_illustrations.py` 对比例/预算/命名/编码违规只打印不拦截、恒零退出）——`classify()`/`encode()` 现在把命名不合规、槽位未知、`webpinfo` 读取失败、cwebp 编码失败、质量下限 q40 仍超预算这五类违规全部计入一个 `violations` 列表，`main()` 结束时非空即 `sys.exit(1)`。新增编码前校验：用 `webpinfo` 读源图真实宽高（只读 RIFF header，不整图解码），与槽位期望比例比对，超出 2% 容差直接拒绝、不再无条件 resize 拉伸——旧版脚本注释自称「按母版实际比例校验」是假的，resize 从不检查比例。为支持临时目录测试，`PICKED_DIR`/`OUT_DIR`/`BUDGET_DECOR`/`BUDGET_DEST` 均可用环境变量覆盖（默认路径/预算不变）。新增 `tests/build-illustrations.test.mjs`（4 条：比例不符拒绝且不产出拉伸产物、质量下限超预算非零退出但产物仍保留供核对、合规母版 happy path 零退出、未知槽位命名非零退出），复用仓库里已有的 `picked/ink` 真实母版做输入避免合成图片的不确定性；接入 `test:build-assets` 脚本。**过程中抓到一个真实回归**：新测试文件按 glob `tests/**/*.test.mjs` 会被顺带扫进 `vitest.workers.config.ts` 的 workerd 沙箱池（它起子进程调 python3/cwebp，workerd 沙箱跑不了，同 `build-assets.test.mjs` 当年被排除的原因）——`vitest.workers.config.ts` 的 `exclude` 补上这一条，否则 `bun run verify` 会在沙箱里对着这个文件报 segfault。
  **收尾**：F60 修好的管线拿去真实执行了一遍——`a8095d2`（M44 挑版收尾）已把 44 城 dest 母版转入 `picked/dest/` 但明确把产物生成「让位在途的 F58–F60 修复会话」；本次 `python3 tools/build_illustrations.py` real run 对 15 张山水母版 + 44 张 dest 母版全部编码，ink 产物与修复前字节级一致（脚本改动只加校验，不改编码路径），44 张 dest 产物全部 640×427、≤40KB 预算内，退出码 0，随本次一并 commit 上线——**目的地共享集自此 44/267 城真实产物在线**。**未动 `review.md`**：该文件按其自身约定由 reviewer（codex）拥有，findings 的关闭权在 reviewer 复核后，本轮只负责代码修复入库，M45/M46 gate 仍待 codex 复核关闭｜Verified：`bun run verify` 178/178（133 决策/UI 层，+4 新增：3 条 `assetDirFor` 探针 + 1 条 F59 双皮肤往返回归；+45 workerd 无回归）；`bun run test:build-assets` 5/5（1 条既有 + 4 条新增管线契约测试）；`python3 tools/build_illustrations.py` 对真实资产 real run 退出码 0，15 张 ink + 44 张 dest 全部在预算内。**生产复验（push 30c2cb6 后，Cloudflare 自动部署完成）**：`index-CtqpeRp5.js` 上线（200）；`index-C-GBXldd.css` 与本地构建**字节级一致**（CSS 本轮零改动，哈希未变）；44 张 dest 产物抽样（hangzhou/anji/zhouzhuang-jinxi/dunhuang/sanya/nanjing）逐一 200（首次探测 hangzhou 一度 404，重试即稳定转 200——同 M38/M47 记录的分钟级边缘传播窗口，非本次改动引入）；Lab 首页 `/` 仍 200 零回归。 [cc]

- M45/M46·review 响应二轮（F58 收窄，2026-07-20 cc in-session）— codex 复核 baseline 880e51f：F59/F60 连同回归测试均判定成立、**关闭**；F58 的 `assetDir` 运行时消费部分同样判定成立，但指出**代码与 design 口径仍互斥**——`design.md:93`「字体对/资产目录/装饰开关由渲染路径按当前皮肤声明消费」与已经实现（且 reviewer 上一轮认可）的「CSS token 是字体运行时唯一真相源、`fonts` 只是 drift-pin 静态元数据」直接矛盾，`:114` 部件清单表也把字体对无差别列进「被消费」的皮肤声明里；F58 由 P1 降级为 P2，**范围收窄为纯文档**、reviewer 明确「无需再改运行时代码」。处置：`design.md:93` 拆开表述——资产目录/装饰开关维持「渲染路径真实消费」（点名 `assetDirFor()`/`applySkinVisuals()`），字体对单独成句「不走这条路」，说明运行时机制=scoped CSS token（`--round`/`--sans`）+ 浏览器原生按需取 `@font-face`，`fonts` 字段唯一用途=被测试核对与 CSS 字面量一致，不是运行时输入；`:114` 部件清单表「字体对」一格加注同一口径，与「资产目录/装饰开关位」的「渲染路径真实消费」并列对照，不再混写。未改运行时代码（按 reviewer 建议，`registry.ts`/`illustrations.ts` 上一轮已经改对）；`review.md` 仍未动，关闭权留给 codex 下一轮复核｜Verified：本轮为纯文档变更（`design.md` 零代码文件改动），仍重跑确认 `bun run verify` 178/178 无回归 [cc]

- M52 — 山水皮肤一致性精修 [R2 · S2] → fable · high · cc in-session（2026-07-20 晚，用户单开会话按工单甲→庚逐层执行）｜spec 见 design M52，工单 `.agent/m52-ink-polish-brief.md`（已按其自述消费后归档删除，git 历史为档案）。落地五层：
  ① **乙·投影/按压形态 token 化**——25 个形态 token（`--shadow-logo/-pill/-panel/-chip/-chip-on/-btn/-act/-card/-card-hover/-fab/-dock/-go/-close/-sheet/-machine/-knob/-cmp/-mini/-bigbtn/-bigbtn-active/-bigbtn-ghost/-share` + `--press-shift/-sm`）+ 6 个选中态 token（`--chip-on-bg/-text/-border`、`--btn-on-bg/-text/-border`），**cream 值=改造前 style.css 字面量逐处原样搬入**（按形态语义逐位立 token 不按值合并——同值的 chip/btn 分立、count-pill/map-stat 同值同语义合一，未来皮肤可分别表达）；style.css 全部 box-shadow/text-shadow/active-transform 字面量替换为 var()（唯 `.sky` 族的太阳/云投影按工单豁免——ink 下整块 display:none）；ink 值=mock 语言（贴纸硬投影全部 none，卡片/弹层/浮坞只留极淡无偏移墨色晕散，按压位移改 `[data-theme="ink"]` 的 opacity .8 反馈）。**过程中抓出并修复一个 token 化首版引入的真实奶油回归**：`--shadow-card` 内含逐卡片元素级变量 `var(--rc)`（区域色投影），而自定义属性的 var() 在定义作用域（:root）替换、元素继承已解析值——token 里拿到的永远是兜底灰而非各卡区域色（计算样式核验抓出：`.card` 投影从 `rgb(205,233,255)`（江浙沪）退成灰 .18，非目测能稳定发现）。修法=cream 值设 `initial`（guaranteed-invalid）+ style.css use-site `var(--shadow-card, <奶油原字面形态含 --rc>)` fallback，ink 实值整条顶掉 fallback——**`--rc` 区域色投影在 ink 下由此结构性失效**（工单红线）。
  ② **丙·色块去平涂**——ink 的 `--region-*` 九色+fallback 全部=宣纸色（mock 无区域色概念，区域归属交给条带文字，卡条带/卡 emoji 方块/详情头/行程站点四处消费点一并退场）；chip 选中态=朱红描边+暖纸浅底+墨字（对照 mock「江浙沪/夏」，`.btn.on`（筛选入口按压态/皮肤选择器高亮）同语言）；`--grad-fab/-knob/-btn-primary/-blue/-green` ink 值改扁平色（朱红/朱红/朱红/黛蓝/深苔绿 `#5f7d54`——`--green` 对纸白字对比度只有 3.05，另立深值保 5.0）；`.c-photo`/`.dt-banner` 补「宣纸+极淡墨色山影渐变」加载期垫底（纯 CSS 零资产）。
  ③ **丁A·功能图标 SVG 化**——新建 `src/ui/icons.ts`（stroke=currentColor 线形，viewBox 24、1.8 笔宽圆头）：footprints/scale/suitcase/share/palette/printer 六枚，替换 👣（足迹胶囊/地图统计/详情打卡钮）、⚖️（卡片/详情/dock）、🧳（卡片/详情/trip 空态文案引语）、📤（页头备份钮）、🎨（页脚换皮肤）、🖨（路书打印）；index.html 四处静态按钮为免首帧闪空**内联同款 SVG**（与 icons.ts 双源，两处注释互指同步义务）；`footPill` 由 textContent 改 innerHTML 携带图标。**这是全皮肤共享的结构升级（奶油同样换新）**——工单明示「奶油下也更精致」，属 intended change，不算奶油回归；奶油零回归红线适用于 token 搬运部分（见 Verified ㋑）。icons.ts 已补进 no-hardcoded-visuals 审计文件清单。
  ④ **戊·印章+手绘边线**——index.html 内联手绘 SVG 白文印**两枚**（副标题尾「下一站」+页脚「去哪玩」，2×2 排布 rect+text 走 `var(--red)`/`var(--paper)`，`#sealRough` feTurbulence 高频位移做刀刻糙边，微旋转 ±、opacity .85；应用没有 mock 的「区段标题」位，不为凑 3 硬造第三处），替换被用户点名丑的 CSS「印」字块；全站共享 SVG 滤镜 defs 一块进 index.html（`#inkWobble` 低频/`#sealRough` 高频，仅 ink 规则引用）。手绘边线两轨：**小件（chip/btn/search/sort/act/dock-go/card/dock-box/g-window/fab/big-btn）=不对称圆角 trick**（ink 专属 override，零资产零滤镜成本；卡片 300+ 张刻意不进滤镜避免手机滚动开销；g-window 有 overflow:hidden 会裁外扩伪元素故同走圆角）、**大容器（console/paper/machine/share-sheet）=原边框转透明保布局+伪元素同位叠 `#inkWobble` 位移描边**（只有线在抖、文字不受滤镜影响；伪元素定位基准是 padding box，外扩量=各容器自身边框宽，M50 padding box 教训复用）；抖动虚线口径推广到剩余 `--divider-*` 消费点（`.leg-line .dash`/`.rb-sk-row`）。**FAB「扭一个」=横版朱印**：扁平朱红+纸白字+糙边滤镜+不对称圆角，墨描边退场；文案维持「扭一个」不改 mock 的「抽一个」（DOM 跨皮肤共享，改文案会动奶油，文案不是皮肤维度）。
  ⑤ **丁B+己·装饰处置与细扫**——`[data-theme="ink"]` 下：✈️ `.plane` 隐藏（mock 标题旁无物件，留白）；🎰 包 `.fab-emoji` span 后隐藏（奶油原样）；🎯 扭蛋旋钮藏 emoji 改白「扭」朱印（`font-size:0`+伪元素，零 DOM）；mascot 头像圈退硬边框（母版自带圆墨圈构图，双重圈框）；**宣纸纤维噪点**=body::after fixed 合成层平铺 256px feTurbulence data-URI（feColorMatrix 常量映射无色值字面量，opacity .05，print 媒体关闭）；毛笔字体下数字实测正常渲染（路书 D 序/预算「¥1,200~1,800」均出毛笔数字，语料含数字无需补兜底）；⛰️♥👣 偏好 chip、语义文案 emoji（toast/引语）、`d.emoji`（用户豁免）、详情区段标题 emoji 按工单口径保留。
  ｜Verified：`bun run verify` 174/174（+2：**皮肤 token 双文件漂移钉子测试系本轮新造**——工单红线默认它存在、实际 M46 只做过一次性脚本比对，现固化为 registry.test.ts 常驻测试：cream/ink token 名集合完全一致，漏一边即红；icons.ts 纳入审计闸门）；`bun run build` 干净。**浏览器全链路复验**（claude-in-chrome + `python3 -m http.server` 生产构建，606px 视口=手机路径 + M47 同款 CSSOM 断点改写走桌面路径）：㋐ 山水逐视图目检——首页（宣纸噪点/柳桥竹枝远山落位/朱红双印/手描墨框含拐角起笔感/FAB 朱印）、筛选台展开（chip 描边+计数、选中态朱红描边浅底与 mock 逐位对照）、详情（华南题头图/纸张 wobble/扁平朱红+黛蓝 CTA）、扭蛋（机器横幅/朱印旋钮「扭」/票券顶部氛围带出该区题头画）、对比表（暖纸表头+茶色格线的老账本感）、行程单（靛蓝站点卡/抖动虚线 leg）、路书（毛笔 D 序与数字/抖动分隔线/打印机线形图标）、足迹地图（茶色省界/朱红心标）、分享/皮肤选择器（wobble 框/朱红高亮）、空态（咔啦 180px 正常）——**与 style-ref-mock 并排气质一致**；㋑ **奶油等价性程序化核验（非目测）**：24 项计算样式逐一断言等于改造前字面量（chip/console/logo/count-pill/fab/card/act/dock-box/dock-go/knob 投影、999px/20px/12px 圆角、sky/hills/plane/fab-emoji 照常显示、印章 display:none、body::after 噪点不存在、console::after wobble 不存在、knob 字号 26px），其中卡片投影经①ⓑ修复后实测恢复 `rgb(205,233,255) 6px 7px 0`（区域着色原样）；㋒ 缺图回退契约无恙（263 帧按滚动就近 404 后逐个正确移除，`.c-photo` 山影渐变只在加载窗口可见、缺图不硬占的 M46 拍板未破）；控制台全程零 error。**两个已知瞬态确认非 bug**：后台标签页 transition 冻结导致计算样式读数滞后（M50 已记录的同款竞态，禁过渡后读数即正确）；懒加载 404 前的占位闪现为 M46 既有行为非本轮引入。**生产复验（push 743280b 后，Cloudflare 自动部署完成）**：`https://lab.medspiral.com/next-stop-gacha/` 引用 `index-C-GBXldd.css`/`index-D5kXODKz.js`，CSS 与本地过闸构建**字节级一致**（`diff` 通过），JS 200；生产 HTML 已含 `#inkWobble`/`#sealRough` 滤镜 defs 与两枚 `ink-seal` 印章标记；Lab 首页 `/` 仍 200 零回归。 [cc]

- M46 — 皮肤：山水（水墨游记）[R2 · S2] → sonnet · high + 用户终审 · cc in-session（2026-07-20，spec/资产/token 三线高度耦合、拆子代理需要复述的上下文比直接做还贵，未委派）｜spec 见 design「主题皮肤系统」山水行 + M46 + 「已冻结风格锁」。M42 插画接入管线、字体子集管线均随本模块首落地，两条管线自身皮肤无关、后续皮肤直接复用。落地：
  ① **字体管线**（`tools/build_fonts.py` + `fonts/README.md`）——马善政毛笔体（标题）/霞鹜文楷 Regular（正文）均 SIL OFL 1.1 可商用开源，源文件下载进 `fonts/source/`（体积大不进 git，同 raw 插画候选的先例）；语料分两档：正文用 UI 文案+data 全量文本（`--sans` 覆盖数据里一切字段，必须全量），标题只用 UI 文案+`name`/`province`（逐个核对 `var(--round)` 选择器实际覆盖的数据字段后收窄——马善政笔画点数远高于文楷，按全量语料算会撑破 1MB 预算，实测 3043 字→1.19MB；收窄到 1444 字后 570KB）；`pyftsubset` 产物落 **`src/skins/fonts/ink/`（不进 `public/`）**——Vite 的 base 前缀改写只对模块图内资产生效，CSS `url()` 指向 `public/` 时不会被改写（已知限制），留在 `src/` 下才能让 Vite 按模块资产处理（哈希文件名+正确带 base 拷进 `dist/assets/`），否则会重演 F39 那次「base 配错致资产 404」的事故形状——`bun run build` 后实测 `dist/assets/*.woff2` 引用路径确认正确带 `/next-stop-gacha/` 前缀。**本地管线、不进 Cloudflare 远端构建**：`pyftsubset` 依赖 `fonttools`+`brotli` 两个第三方 Python 包，给远端构建加 pip 安装步骤等于把部署管线绑在 PyPI 可用性上，产物提交 git（同图片资产管线先例）；已知代价=日后数据/UI 文案新增大量新字符不会自动跟上，需人工重跑。懒加载/失败回退**不需要额外 JS**——`--round`/`--sans` 只有 `data-theme="ink"` 时才解析到 "Ink Title"/"Ink Body"，浏览器天然只在这时才发起 woff2 请求（`font-display:swap` + 系统字体兜底栈保排版不塌）。
  ② **插画接入管线**（`tools/build_illustrations.py`，M42 首落地）——同样本地管线不进远端构建（用 `cwebp` resize+重编码，Cloudflare 镜像不保证有）；`picked/<皮肤id>/*.webp` 母版（1024×1024 方图 / 1024×512 题头+装饰）按画布契约等比缩放（方图→640×640、题头/装饰→960×480，物理上不裁切，交给前端 `aspect-ratio` 撑比例）+ `cwebp` 质量搜索（85 起步降到 40）保 ≤60KB；同一脚本顺带处理 `picked/dest/`（M44 目的地共享集，3:2→640×427，≤40KB）——两套命名规则不同（皮肤资产去 `<皮肤id>-` 前缀，dest 资产去 `dest-` 前缀，命中过一次真实 bug：脚本首版没剥 dest- 前缀，产物错误命名成 `dest-hangzhou.webp` 而不是 `hangzhou.webp`，导致 `destPhotoSrc()` 拼不出正确 URL，浏览器复验时发现修的）。产物：`public/illustrations/ink/`（16 张母版出 15 个槽位，style-ref-mock 不算 UI 资产跳过）+ `public/illustrations/dest/`（皮肤无关目的地共享集，脚本对空目录静默跳过——**本模块开工期间 `picked/dest/` 有过一段短暂窗口**：M44 batch 0 转档的三样张 hangzhou/dunhuang/sanya 一度落过档，cc 据此验证过「个图优先、退题头图兜底」整条链路；同一会话内另一支线随后拍板画风 A/B 比稿、回滚了这批转档（commit 7ea9652），`picked/dest/` 回到空——本模块 push 时是空的，产物目录随之为空，机制本身零改动仍然成立：cards.ts/detail.ts 对「个图不存在」的处理路径就是常态路径，不是这次回滚才补的特例）。
  ③ **`src/skins/illustrations.ts`**（新）——`illustSrc(skinId, slot)`/`destPhotoSrc(id)`/`regionSlot(region)`（复用 `logic/constants.ts` 新增的 `REGION_SLUG`，从 `REGION_COLOR` 的 `var(--region-<slug>)` 正则抠 slug，不另开一份映射）三个纯函数负责拼 URL——`assetDir` 字段字面拼进路径，是「assetDir 真实被消费」的直接证据；`applySkinVisuals(skinId)` 一次性做完两件事：给全部 `[data-illust]` 静态槽位（吉祥物/扭蛋机/空态/三个自由装饰件）按当前皮肤写 `src`，**且**对带 `data-deco` 的槽位查 `SKINS.find(skinId).decorations[key]`——不是 true 就整个 `hidden=true` 并 `removeAttribute("src")`（不只是隐藏，是压根不发请求），是「装饰开关全链路验真」的直接证据；`wireIllustFallbacks()` 用文档级捕获阶段监听 `error`（error 不冒泡，同 click 委托是同一习语只是挂载阶段不同）实现三级缺图回退：`data-fallback-src` 存在且没试过 → 换一次 src 重试（详情页头图的「个图→题头图」两级链就是这么接的）→ `data-fallback` 是 emoji 字符 → 就地换成等大 `<span>`（空态唯一用到，必须原样退回同一个 emoji 才叫零回归）→ 其余（含默认 "hide"）→ 移除最近的 `[data-illust-frame]` 容器，找不到就移除自身。
  ④ **`src/skins/ink.css`**——与 `cream.css` **128 个 token 逐一对应**（脚本比对过两文件的 token 名集合完全一致，无遗漏无多余），色板取自风格锁四色（宣纸 `#faf3e3`/暖炭黑墨 `#2b2621`/淡靛 `#7ba7c9` 系/朱红 `#e85d3d` 系/赭黄茶绿），描边宽/圆角族整体收细收窄（水墨线条比奶油卡通克制，1/1.25/1.5/2px 与 12 档圆角普遍降 30~40%，胶囊类 999px 不变）；`--round`/`--sans` 挂 `@font-face`（`src: url("./fonts/ink/*.woff2")`，相对路径走 Vite 资产管线，见①）。**不改 `cream.css` 一个字符**。
  ⑤ **`src/style.css`**——新增 `@import "./skins/ink.css"`；插画接入通用块（`.illust`/`.illust-fallback`/`.mascot-decor`/`.deco-*`/`.g-illust`/`.c-photo`/`.dt-banner`/`#gachaTicket` 氛围带，均 token 驱动零硬编码色值，过审计闸门）；山水专属装饰块（`[data-theme="ink"]` 隐藏 `.sky`/`.hills`——云朵山丘是奶油卡通专属氛围；`.subtitle::after` 纯 CSS 朱红印章；`.console-foot`/`.rb-cover` 的抖动分隔线用 `repeating-linear-gradient` 模拟不规则笔触，替代原生 `dashed`，颜色仍走 `var(--ink-soft)`）。
  ⑥ **接入四处**：`cards.ts` 卡顶目的地照片（`.c-photo`，M44 共享集，线路卡不画个图跳过）；`detail.ts` 详情页头图（`headerBannerHTML`——线路卡直接用大区题头，城市卡个图优先、缺个图退题头图、两者皆缺整块消失，"缺图不硬占"的字面实现）；`gacha.ts` 票券氛围带（`#gachaTicket` 的 `--ticket-ambience` 背景变量，取扭中目的地所属大区的题头图；重扭时先清掉旧值防串图）；`index.html` 静态槽位（mascot 头像圆框、gacha 机器横幅、empty 状态图、三个自由装饰件）+ 防闪烁数组 `S=["cream","ink"]`。
  **Implementer 拍定 5 条**（均无先例、design 未钉死具体位置，记录理由）：ⓐ mascot/gacha 两个「固定小件插画」在本模块前从未有 DOM 位置——mascot 放页头 title-row（复用 mock 里「圆框头像」的取景逻辑但简化实现，未采用 mock 的绝对定位悬浮画法，避免引入新的定位脆弱性）、gacha 机器横幅放 `.machine` 面板顶部，均纯装饰、手机端隐藏 mascot（M50 同款「低频纯氛围入口让位」纪律）；ⓑ decorations 记录**只管自由浮动装饰件**（柳桥/竹枝/远山）不管 `.sky`/`.hills`——后者是 FOUC 敏感的首帧背景（必须在 JS 跑之前就确定，纯 `[data-theme]` CSS 选择器更安全），前者是本模块新引入的可选装饰、天然适合走「组件已渲染后由 JS 决定要不要」的声明式开关，两者机制不同不是随意二选一；ⓒ 详情页头图与卡片照片统一用 `object-fit:cover` 而非题头位契约要求的「不浅裁」——契约字面只约束「题头位」本身，个图（比例 3:2）落进 2:1 题头框属于「个图借用同一展示位」的合理裁切，不是题头图被裁；ⓓ 扭蛧票券氛围带首版用「四周留白窄边掀开一角」实现，实测 90% 情况这条窄边刚好落在题头图的留白区（水墨构图负空间大），几乎看不出来——改成顶部单条 90px 高的实条带，读出来才叫「氛围」；ⓔ 空态插画首版忘了给 `<img>` 显式尺寸，按原始 640×640 分辨率铺满全屏——浏览器复验时肉眼抓到，补 `180×180` 显式框。｜Verified：`bun run verify` 172/172（新增 16 条：`illustrations.test.ts` 14 条覆盖 URL 拼接/字体产物存在性&体积/registry-CSS drift-pin/`applySkinVisuals` 消费/三级回退全链路 + `registry.test.ts` 补 2 条山水声明契约断言）；`bun run build` 干净（`title.woff2` 583.6KB / `body.woff2` 651.3KB，均 ≤1MB；15 张山水插画全部 ≤60KB 预算内，实测 12.6~51.0KB 区间）。**对比度实测（cc 亲自算，非目测）**：body/paper 对比度 ink 8.95 : cream 8.88，chip 未选中文字/paper ink 13.55 : cream 9.39，chip 选中态 on-color/bg ink 3.00 : cream 2.22，placeholder/paper ink 2.47 : cream 2.43——**全部四项 ink ≥ cream**，验收「正文/chip 对比度不低于奶油皮肤」过闸有数据支撑而非肉眼印象。**浏览器全链路复验**（claude-in-chrome，`python3 -m http.server` 而非 `vite preview`——踩过一个坑：`vite preview` 对不存在的资产路径有 SPA fallback、返回 200+index.html 而非 404，会把「缺图该 404」的测试场景悄悄测成「缺图变成整站首页」，同 M45 用 `http.server` 的先例）：桌面态（606px 视口用 M47 同款 CSSOM 手法把 680px 断点临时改窄到 200px）逐视图目检——首页（sky/hills 消失、willow/bamboo 装饰件、朱红印章、mascot 头像圆框、抖动分隔线全部到位）/筛选台全展开（10 组 chip 含新「偏好」组，对比度良好）/详情页（**验证过两种真实状态**：`picked/dest/` 短暂有三样张期间，hangzhou 个图直接显示、suzhou 无个图正确退到江浙沪题头图；上游回滚 batch 0 后重验，hangzhou 也正确退回题头图——两次结果都对，证明「个图优先、退题头图、两者皆缺才消失」这条链路对「有图」和「暂无图」两种输入都成立，不是只在有图时才 work）/扭蛋（机器横幅+票券顶部氛围带清晰可辨）/空态（补尺寸后插画大小正常，emoji 回退在奶油皮肤下逐字节复现原样）/足迹地图（省界/图例/心形标记配色协调）/路书（抖动分隔线、逐日速览、weather 卡片正常）；`localStorage.nextstop_skin_v1` 三态（"cream"/"ink"/"random"）reload 后 data-theme 解析全部正确；🎨 页脚按钮实时切换不刷新生效（弹层内「山水」选项即时高亮、toast 提示）；控制台全程零 error。**奶油零回归**：同一浏览器会话在 cream 下逐视图复验（首页/卡片/详情空banner/空态 emoji/筛选台/扭蛋）与 M46 改造前截图肉眼无差异——`picked/dest/` 现为空，卡片/详情页均不出现任何目的地照片，两皮肤下都是如此，与改造前逐像素一致；M44 batch 1 画风比稿定案、母版重新落地后，卡顶照片会作为皮肤无关的预期新增自动出现，不需要再动本模块代码。**生产复验（push d38f140 后，Cloudflare 自动部署完成）**：`https://lab.medspiral.com/next-stop-gacha/` 引用 `index-D3TjkAJL.js`/`index-DiO_gB-R.css`，与本地过闸构建**字节级一致**（`diff` 通过）；`assets/title-*.woff2`/`body-*.woff2` 200；`illustrations/ink/mascot.webp`/`region-jzh.webp` 200；`illustrations/dest/hangzhou.webp` 404（与 `picked/dest/` 当前为空的真实状态一致，非 bug）；Lab 首页 `/` 仍 200 零回归。 [cc]

- A6 — 山水皮肤固定资产批（插画轨道：codex 生成 → 用户挑版 → cc 转档）｜2026-07-20 收官：`picked/ink/` 16 张母版齐备——方图 3（mascot/gacha/empty）+ 题头 9（九区）+ 装饰 3（柳桥/竹枝/远山）+ style-ref 风格锚点；东北/港澳经一轮带意见补画至 v5 过审。挑版同日拍板：题头槽位原生 2:1 不浅裁、接入逐资产 focal 微调、装饰件移出槽位契约（已入 design「装饰位画布契约」）。批次生成与 QA 明细见 illustration-brief A6 节与 git 历史 [cc]

- M50·落位修订（2026-07-20 cc in-session，fable 直做——diff 小且与用户反馈迭代耦合紧，委派往返不划算）｜用户对下方首版落地提四条反馈，AskUserQuestion 三答拍板后重做落位（拍板档案 2026-07-20 有记，design「界面动作分区」同步改述）：①页头集中动作区整行撤销——`skinBtn` 改右上角 `absolute` 图标钮（`.corner-skin`，44px 触达不占布局流）；`footPill` 从纯统计 span 改 `<button>` 常显（零打卡时文案「👣 足迹地图 · 打卡第一站吧」引导+入口二合一，不再隐藏——隐藏会连地图入口一起丢）、点击即开足迹地图（events.ts 接 `openMap`，独立 🗺 按钮删除）；📤 备份钮挂胶囊旁，`.foot-group` 包裹保证两者作为整体换行不拆散（首版复验时抓到 📤 单独折行的缺陷，当轮修正）。②收纳条「清空」按钮（`barResetBtn`）删除——台底「清空筛选」为唯一清空入口，收纳条腾出的宽度归搜索框。③「筛选」入口加 `.fcaret` 折角（▾，`#console.open` 时 180° 翻转，`aria-hidden` 装饰件）暗示可展开。CSS 净变化：删 `.header-top`/`.header-actions`/`.header-act` 三规则族，增 `.icon-btn`/`.corner-skin`/`.foot-group`/`.fcaret`/`button.count-pill` 交互态，全部纯布局+既有 token，零新色值｜Verified：`bun run verify` 156/156、`bun run build` 干净；浏览器复验（vite preview 生产构建，claude-in-chrome）：桌面态 🎨 右上角清空阳装饰、footPill+📤 成组不拆行；三入口真实点击各开对弹层；`barResetBtn` 确认不存在、全站含「清空」按钮仅剩 resetConsoleBtn/emptyResetBtn（空态兜底）/clearTripBtn（行程域）三个各司其职；手机路径（同 M47 CSSOM 断点改写手法）收纳条=搜索+筛选▾ 两件、单行 40px、consoleBody 正确开合；折角 transform 经禁用过渡后直读验证 open=180°/closed=none（此前读到的"反转"是 0.15s 过渡与后台标签页样式重算的竞态，非 bug）；打卡 1 个后胶囊实时变「👣 去过 1 个目的地 · 点亮 1 个省份」、取消后回引导文案、localStorage 前后逐字节一致；整页 reload 控制台零 error。**生产地址复验（push d7a072a 后，Cloudflare 部署完成）**：`index-BHPONvvj.js`/`index-BdJtmHXQ.css` 与本地过闸构建**字节级一致**（`diff` 通过），生产 HTML 已含 `corner-skin`/`foot-group` 新结构；manifest 200、Lab 首页 200 零回归。**同日三轮微调（用户看过二轮后再提两条：📤 比胶囊大一截、🎨 悬浮右上角突兀）**：🎨 撤出角落并入 foot-group 簇（`.corner-skin` 删除，页脚方案用户前轮已排除、簇内收编为余下自然位），📤/🎨 双双收为与胶囊**等高 35px**（实测三件同高）；触达用 `::after` 外扩热区补足——**过程中抓到一个真实 bug**：absolute 伪元素的定位基准是 padding box 而非 border box，`inset:-4px` 被 2px 边框吃掉一半、实际热区只外扩 2px（`elementFromPoint` 逐像素探针坐实），改 `inset:-7px` 后实测 ±4px 外命中、有效触达 45px；`.foot-group` 间距 8→10px 防相邻热区互吞。design「界面动作分区」改述为「等高图标钮同簇随行、绝对定位角落按钮是异物」并记录 padding box 陷阱。Verified：156 测全绿、build 干净、浏览器实测三件等高/热区外扩/两钮开对弹层/整页 reload 零 error。**生产地址复验（push a395569 后）**：`index-vUme3eli.js`/`index-Dk0ZIMaZ.css` 与本地过闸构建字节级一致（`diff` 通过），HTML 含 foot-group、corner-skin 已消失；传播期再次观测到新 HTML 引用的 hashed CSS 短暂 404（同 M38/M47 记录的分钟级边缘混 serving），轮询至 200 后核对收敛；Lab 首页 200 零回归。**同日四轮（用户看过三轮后再提两条：🎨 挤得胶囊统计文案在手机折成两行、▾ 折角太小不明显）**：🎨 最终归**页脚 meta 区**（「🎨 换皮肤」小按钮，免责行下方）——换肤是站点级外观设置、与页面任何数据无关、设一次即忘（有「随机」），页脚是唯一的站点 meta 区，头部两轮尝试证明它在上面就是挤别人；📤 独自随胶囊，`#footPill` 加 `nowrap`（宁可整体换行不在胶囊内折行）；折角 ▾10px/.55 → **▼12px/.8**。浏览器实测：foot-group 引导态 261px/带统计 307px 均 <350px（390 屏可用宽），胶囊单行；页脚按钮开对皮肤弹层；156 测全绿。design「界面动作分区」增「皮肤入口归页脚 meta 区」条。**生产地址复验（push df59b67 后）**：`index-8Yi0uDlv.js`/`index-CxkVf817.css` 与本地过闸构建字节级一致（`diff` 通过），HTML 含 footer-tools；Lab 首页 200 零回归。DOM 结构随本修订重新冻结 [cc]

- M50 — 全局动作区：工具按钮按归属分家 [R2 · S2] → sonnet · high · sonnet 子代理实现 + cc 复核（2026-07-19，按拨盘委派；cc 亲自 review diff + 跑 verify/build + 双端浏览器复验，见 Verified）｜spec 见 design M50 与「决策机制·界面动作分区」。落地：①`index.html`——header 拆出 `.header-top`（`.title-row` + 新 `.header-actions`），三个全局入口（`skinBtn`/`shareBtn`/`mapBtn`，id 不变、原样复用既有 `openSkin`/`openShare`/`openMap`）从 console-foot 动态生成挪为页头静态标记，标签改两字（🎨「皮肤」不变、📤「分享/备份」→「备份」、🗺「足迹地图」→「足迹」），**分享面板自身标题「📤 带走我的记录」与内容零改动**；每个按钮补 `aria-label` 全名（切换皮肤/分享与备份/足迹地图，均包含可见两字标签，满足 label-in-name）供手机端图标化后读屏仍知道按钮是什么。②`src/ui/console.ts`——console-foot 移除三个全局按钮（连带 `openMap`/`openShare`/`openSkin` 三个 import 一并删除，wiring 转 events.ts）；避开高海拔/只看收藏/隐藏去过的三开关从 `.btn` 改 `.chip`、包一层新的第十个 `.fgroup`「偏好」，与九个筛选组同一视觉语言（点亮态）。三开关的 wrapper 用新 class `.chip-row`（刻意不用 `.chips`）——`buildConsole()` 里的通用 chip 点击委托与 `updateChipCounts()`/`syncChips()` 都靠 `document.querySelectorAll("#console .chips")` 选中筛选组，若「偏好」沿用 `.chips` 会被误接进 `simulateChipClick`（键值非 `GroupKey`，运行时读 `state[undefined]`）、且会被强行标上「剩几个」计数——两者都是 spec 明确禁止的（布尔开关不进 `simulateChipClick` 口径、不标计数）；`.chip-row` 结构性地把「偏好」隔离在这两套机制之外，不是靠约定人记得不要接错。三开关自身的 click handler（toggle `state.onlyFav`/`noAlt`/`hideVisited` + toggle `.on`）逐字不变。顺手清理：`resetFilters()` 里原本单独 `.classList.remove("on")` 三开关的三行，因三开关现在也是 `.chip` 而被同函数最后一行 `document.querySelectorAll(".chip.on")...` 覆盖变成死代码，一并删除。console-foot 收窄为排序+清空筛选+命中数三件（spec 规定的唯一三件）。③`src/ui/events.ts`——新增 `openMap`/`openShare`/`openSkin` import 与三行 `addEventListener`，紧跟其余静态元素（`tripClear` 等）之后；这三个按钮现在能在这里接线是因为它们已是 `index.html` 静态标记（`wireEvents()` 在数据异步加载/`buildConsole()` 之前同步跑），不再依赖 `buildConsole()` 生成后才存在。④`src/style.css`——新增 `.header-top`/`.header-actions`/`.header-act` 三条布局规则（纯 flex/gap，零新色值）；680px 媒体查询内追加 `.header-act` 收圆（44×44px、`border-radius:var(--radius-pill)` 复用既有 token）+ `.act-label{display:none}`；`.chips` 选择器扩为 `.chips, .chip-row` 使「偏好」继承同一套 flex-wrap 布局。按钮外观本身 100% 来自既有 `.btn`/`.chip` token 化样式，零新增 hex/rgba——`no-hardcoded-visuals` 审计闸门覆盖 `index.html`/`style.css`/`console.ts`/`events.ts` 全部四个改动文件均过闸。`state` 字段、过滤语义、`updateFilterBadge()` 计数口径（9 组维度+3 开关各计 1、`q` 不计）三处逐字未动｜Verified：`bun run verify` 156/156（111 决策+UI 层 + 45 workerd，无新增/无回归，本模块零触碰 `src/logic/`）；`bun run build` 干净（`dist/index.html` 9.11KB、CSS 29.39KB、JS 192.05KB）；`grep -rn "openMap\|openShare\|openSkin" src/ui/console.ts` 空，确认三处全局入口彻底搬离 console.ts。**浏览器双端复验**（claude-in-chrome，`vite preview` 生产构建于 `localhost:4321/next-stop-gacha/`；本环境真实窗口无法拖窄到 390px——同 M47 手法用 CSSOM 把打包产物内真实 680px 媒体查询临时改宽到 3000px，让当前窗口走手机代码路径，验证后即弃用该 tab）：collapsed 态 `.console-bar` 实测高度 40px=单行；页头三个圆形入口 `getBoundingClientRect` 实测 44×44px、`border-radius:999px`、`aria-label` 三值分别为「切换皮肤/分享与备份/足迹地图」、`.act-label` 计算 `display:none`（图标独存，标签由 aria-label 补全）；🎰扭一个 fab 首屏可见。展开筛选台后「偏好」组渲染为第十个 fgroup，三 chip 无数字徽章（与其余九组的「还剩几个」形成对照，即结构隔离生效的直接证据）；真实点击「避开高海拔」+「只看收藏」两 chip 均点亮（`.on`）、`命中` 从 320 正确收窄到 1（过滤语义与改造前等价）；收起筛选台后 `filterBadge` 显示「2」（9 组维度+3 开关口径不变的直接证据）；点「清空筛选」后两 chip 复位、badge 清空、命中恢复 320/320。三个页头按钮逐一真实点击：🎨 打开「选皮肤」弹层、📤 打开「带走我的记录」（标题/内容与改造前一致，未受按钮标签改「备份」影响）、🗺 打开足迹地图弹层，三者互斥切换正确。控制台全程零 error。**桌面态**（同一环境未做 CSSOM 干预的原生渲染）：页头三入口显示为「🎨皮肤/📤备份/🗺足迹」图标+两字胶囊按钮一簇，位于 title-row 右侧；console-foot 只剩排序/清空筛选/命中数；视觉与改造前一致，仅三处预期变化（全局入口搬出、多一个「偏好」行、console-foot 收窄）。**生产地址复验**：见下方 push 后追加记录。至此，M45 冻结点完成本次唯一获准的结构修订并重新冻结——后续皮肤/资产改动不得再动 DOM 结构，如需再动须走同等级别用户拍板。 [cc]

- M45·review 响应（F54–F57 triage，2026-07-19 cc in-session）— codex 首轮 gate review（baseline 757c8e2）提四条，逐条处置：**F54**（P1，皮肤声明契约未被消费、单皮肤无法证明切换成立）**用户拍板选「边界修订，M46 验真」**（codex 给出的两条出路之二）：design M45 验收明确「声明消费端验真不在本模块——单皮肤下无法诚实验证」，M46 验收补「皮肤声明契约首次验真」硬项（字体管线可执行证据+assetDir/装饰真实消费+双皮肤切换/首帧一致性），「皮肤声明」机制条补 token 文件=构建期 scoped block 的实现语义；**M45 跨家族 gate 保持开放，随 M46 该验收项一并关闭**——不造一次性测试皮肤。**F55**（P2，token 抽取漏网）三处全修：`index.html` 日期输入内联样式改 `var(--bw-md)/var(--radius-field)/var(--paper)`、`main.ts` 数据加载失败条 `#ef6461`→`var(--red)`、地图图例删「灰点/绿点」颜色词（色板换肤后文案不再说谎，色样 swatch 本身带色）；并按 codex 建议加**硬编码视觉值审计闸门**（`no-hardcoded-visuals.test.ts`：21 个视图层文件逐个扫 hex/裸 rgba，allowlist 三条各带理由——彩带兜底色、QR 黑白功能对比度、透明色）。**F56**（P2，脏 localStorage 高亮分叉）：registry 新增 `normalizeSkinChoice`（合法 id/"random" 通过、脏值/null 回退默认），`resolveSkinId`/`applySkinChoice`/选择器高亮三处共用同一条回退语义，脏值不再落盘；新增单测覆盖脏值/空串/null/random，另加 **storage key 漂移钉子**（index.html 内联脚本与 store.ts 各写一份 key 字符串，测试锁同步）。**F57**（P2，收纳无 disclosure 语义）：`filterToggle` 补 `aria-expanded`（初始 false，随切换同步）+ `aria-controls="consoleBody"`，body 补对应 id；装 happy-dom 写**项目首个 UI DOM 测试**（初始折叠/展开/再收起三态断言，真实 click 驱动）｜Verified：`bun run verify` 156/156（111 决策+UI 层——新增 29 条，+45 workerd）；`bun run build` 干净 [cc]

- M45 — 主题皮肤机制 [R2 · S3] → sonnet · xhigh + cc 复核 · sonnet 子代理实现（2026-07-19，按拨盘委派；cc 亲自跑 S3 等价性闸门，见 Verified）｜spec 见 design「主题皮肤系统」与 M45。落地：①`src/skins/cream.css`——奶油 token 集 128 个（旧 `:root` 30 var 原样搬入 + 98 个新 token 从视图 CSS 字面量等值收编：色板/文字色/描边宽 4 档/圆角 12 档/投影 rgb 三元组 10 个/渐变 10 个/区域九色+fallback/地图色 7 个），选择器 `:root, :root[data-theme="cream"]` 双挂——裸 `:root` 兜底是「默认皮肤首屏直出不闪烁」的机制保证；②`src/style.css` 首行 `@import`、所有可换肤字面量原位替换为 var()（规则零重排，Vite 构建仍单 CSS 产物）；③`src/skins/registry.ts` 皮肤声明契约（id/label/fonts/assetDir/decorations）+ `resolveSkinId`（random 抽取、脏值回退默认）+ `applySkinChoice`（持久化+即时切 data-theme 不刷新）；④`index.html` head 防闪烁内联脚本（同步先于样式表跑，数组/默认值与 registry 刻意重复、由漂移钉子测试锁死）+ `skinOverlay`；⑤选择器 UI：console-foot 新增 🎨 皮肤按钮（**位置临时**，最终入口随 🟡 默认皮肤拍板在 M46 终审定）+ `src/ui/skin.ts`（选项随 SKINS 增长、切换后弹层留开方便对比、events.ts data-skin 委托）；⑥`REGION_COLOR` 值改 `var(--region-<slug>)`（键/形状不变）、cards/detail fallback 改 `var(--region-fallback)`；⑦mapview 省份/装饰线改 class+CSS token（`--map-*` 7 个）、图例/点标 inline var()；⑧彩带 6 色改扭蛋时刻读 token 计算值（原 hex 为兜底）；⑨store 新增 `nextstop_skin_v1` 哑读写。**Implementer 拍定 3 条**：字体子集管线（pyftsubset+FontFace 懒加载）推迟至 M46 随首个真实字体文件落地——奶油用系统字体栈，管线现在无可验证对象，声明槽位（fonts:null）即契约，推迟已在 registry 注释与本条记录在案；徽章同 M47 维度计数口径不变；`--paper` 复用为全部纯白面（18 处 `color:#fff` 合并为 `--on-color`，其余重复值疑似处一律分立 token 不合并——子代理判断记录完整见其报告，cc 复核认可）｜Verified：`bun run verify` 127/127（新增 6 条 registry/漂移钉子/region-token 覆盖测试）；`bun run build` 干净单 CSS 产物（29.0KB，较前 +5.9KB=token 块与 var 间接）。**S3 等价性闸门（cc 亲自执行，双仪器）**：㋐计算样式哈希对照——改造前后两份构建在同一 python http.server:4321 前缀路径先后提供，同一协议逐视图驱动（清 LS→冻结动画→fetch 打桩(天气/短链零网络)→Math.random=0.42(扭蛋恒出长春)→种子点击 favs/cmp/trip→10 视图逐个 DJB2 哈希每元素全部计算样式属性，滤除 `--*` 自定义属性枚举噪声；扭蛋按票券真实渲染完成轮询后才采样，规避后台标签页 timer 节流），结果：v3 详情/v4 扭蛋/v5 对比/v6 行程/v7 路书/v9 分享 6 视图逐元素**完全一致**；v8 足迹地图 34 省 path+deco 按文档序配对后哈希**全部一致**（差异纯属 class 重构导致的签名改名）；v1 首页/v2 收纳展开/v10 桌面态仅 +🎨 按钮一个新元素、既有元素中唯一变化=hitCount 宽度（新按钮挤占 foot flex 行的几何后果，非 token 漂移）——**全站零意外计算样式偏移**；㋑逐视图截图对照——9 视图（首页/筛选展开/详情/扭蛋/对比/行程单/路书/足迹地图/分享）改造前后并排目检无差异。**机制功能验证**：boot 后 `data-theme="cream"` 同步已钉（无闪烁）；选择器开出 奶油(on)/🎲随机，点随机→toast+`nextstop_skin_v1`="random"+弹层留开+高亮移动，reload 后内联脚本把 random 正确解析回 cream；控制台全程零 error。子代理自检（var() 展开回填 diff 与旧 CSS 字节级一致）与 cc 闸门互为独立证据。**生产地址复验（push 3708983 后，Cloudflare 自动部署完成）**：HTML 连续 3 次轮询稳定引用新产物 `index-DhgtIa1i.js`/`index-BlQ8xKGF.css`，CSS 与本地过闸构建**字节级一致**（`diff` 通过），防闪烁内联脚本（`nextstop_skin_v1`）已在生产 HTML；Lab 首页 `/` 仍 200 零回归 [cc]

- M47 — 筛选台手机收纳 + 标签两字制 [R2 · S2] → sonnet · high · sonnet 子代理实现 + cc 复核（2026-07-19，按拨盘委派；cc 复核 diff 抓到两处修正：空徽章因有底色/内边距仍渲染成空蓝丸——补 `#filterBadge:empty{display:none}`；共享 `.search` 规则的 `min-width:180px` 未被窄屏覆盖导致收纳条过早折行——媒体查询内收窄到 140px）｜spec 见 design「筛选台呈现」。落地：①`src/ui/console.ts`——buildConsole 重构为 `.console-bar`（searchBox 原样移入 + 新「筛选」入口按钮带已选计数徽章 + 新「清空」按钮）+ `.console-body`（9 行 fgroup + console-foot，仅搜索框迁出、其余原样）；「抵达难度」标签收为「抵达」（data-key/过滤契约不变）；展开/收起=纯 `#console.open` class 切换，不碰 state、不触发 render——「展开/收起不丢已选状态」的机制保证；徽章计数规则（implementer 拍定，design 未指定）=9 个分组维度非空各计 1 + 三开关（避高海拔/只看收藏/隐藏去过）各计 1、搜索词不计（搜索框本身在收纳条上可见）——天花板组按维度计而非按 chip 计，避免点一档 ¥¥ 显示成 2 的误导；徽章在 updateChipCounts 内随每次 render 刷新；②`src/style.css`——桌面默认隐藏两个 bar 按钮、body 恒展开（承接原 `.console` 的 11px 行距，桌面视觉变化仅「搜索框从底行移到顶行」一处）；≤680px：body 默认 `display:none`、`.open` 展开，收纳 padding 收紧 12/14px ｜Verified：`bun run verify` 121/121（76 决策层 + 45 workerd；`src/logic/` 零触碰=过滤语义零变化的结构性证据）；`bun run build` 干净。**浏览器全链路复验**（claude-in-chrome，`vite preview` 生产构建于 `localhost:4321/next-stop-gacha/`，375px 窄屏）：收纳态 console 总高 69px=单行（≤2 行高过闸）、🎰 扭一个 fab 首屏可见；点「筛选」就地展开（按钮压下态）、真实点击江浙沪+¥¥ 后天花板语义正确（¥+¥¥ 双亮、命中 45/320、网格真实过滤到 45 卡）；收起后徽章=2、三 chip `.on` 全保留、命中不变；bar「清空」从收纳态直清成功（0 chip、徽章 `:empty` 隐藏、320/320 恢复）；桌面分支——窗口被系统锁在 606px 拉不宽，改用 CSSOM 把打包产物里真实媒体查询的断点从 680 改到 200px、让同一份规则在当前视口走桌面路径：两 bar 按钮隐藏、body 恒展开、fgroup 行排、搜索在顶、foot/hitCount 完整、九行两字标签截图目检与既有卡通风格一致；控制台全程零 error（含整页 reload）；上一会话遗留的真实 localStorage（行程杭州+成都、去过 1）全程原样存活。**生产地址复验（push 639bc00 后，Cloudflare 自动部署完成）**：`https://lab.medspiral.com/next-stop-gacha/` 引用新产物 `index-1lBTNJHX.js`/`index-BHo0Rji0.css`，CSS 与本地经浏览器复验的构建**字节级一致**（`diff` 通过，含 `:empty` 徽章修正与 console-bar 规则）；HTML 连续 3 次轮询稳定新版（传播期观测到分钟级新旧混 serving 与 hashed CSS 短暂 404，同 M38 已记录的边缘一致性现象）；Lab 首页 `/` 仍 200 零回归 [cc]

- M40 — 后端：短链分享 [R2 · S2] → sonnet · high · cc in-session（2026-07-18，R2·S2 且改动收敛在 worker+persist+两个 ui 模块，判断委派往返成本高于会话内直做，未拆子代理）｜spec 见 design「后端·短链分享」。落地：①`cloudflare/api.mjs`——`handleShareCreate`/`handleShareGet`：结构性校验 `{type:marks|trip, payload}`、payload ≤8KB（`TextEncoder` 字节数）、6 位短码（31 字符无歧义字母表，`crypto.getRandomValues`）、写前 `KV.get` 探测占用+碰撞重掷（≤5 次）、按 IP 每日计数简单限流（20/天）、180 天 `expirationTtl` write-once；KV 绑定未配置（`!env.APP_KV`）与超限/损坏输入分别映射 503/429/400/413/404，从不 throw。②`cloudflare/worker.mjs` 在前缀剥离后、代理 ASSETS 前插入 `/api/*` 分流（POST /api/share、GET /api/share/:code、其余 /api/* 一律 JSON 404），绝不把 API 请求转给 ASSETS。③前端 `src/services/shareApi.ts`（新，I/O 隔离，同 weather.ts 风格）：`createMarksShareLink`/`createTripShareLink`/`fetchShare` 任何失败（网络/非 2xx/解析异常）一律返回 `null`，调用方据此回退——与天气接入同一条「后端从不是可用性前提」哲学。④`src/logic/persist.ts` 抽出 `sanitizeTripItems`（从 `normalizePersisted` 原地提取，行为逐字不变，F14/F18 规则原样）供 localStorage 恢复与 M40 分享短链取回两处复用同一套 trip 校验信任边界。⑤UI 三处接入：`ui/share.ts` 新增 `generateShareCode()`（marks 短链，失败 toast 提示回退二维码/JSON）与 `checkShareCode()`（`?sc=` 打开页面时取回 payload，marks 走既有并集合并导入条，trip 走新的只读路书预览，任何失败静默不打扰，因为访问者可能压根没点过分享）；`ui/roadbook.ts` 重构——`roadbookHTML`/`fillRoadbookWeather`/`currentRoadbookText` 从隐式读 `state.trip/tripStart` 改为显式接收参数，新增 `openSharedRoadbook(trip, tripStart)` 走同一渲染路径但 `readonly=true`（banner 提示+不出「生成分享短链」按钮），新增 `shareCurrentRoadbook()`；**这一步是本模块最关键的正确性点**——若沿用旧版隐式读 `state.trip`，打开一条分享的行程链接会静默覆盖访问者自己正在编辑的行程状态，重构后分享打开的路书只是局部渲染的临时数据，绝不触碰 `state.trip`/`saveLS()`。`main.ts` boot 内新增 `checkShareCode()`（不 await，不阻塞其余启动）。⑥`index.html` 分享面板加「✨ 生成短链」按钮 + 提示行；`style.css` 加 `.rb-shared-banner`。**关键决策（implementer 拍定）**：KV 绑定名定为 `APP_KV`，M40 用 `share:` 键前缀、为 M41 预留 `sync:` 前缀——单一 namespace 覆盖两个后端模块，减少用户需要在 Cloudflare 创建的资源数量 ｜Verified：`bun run verify`（`tsc --noEmit && vitest run`）76/76（新增 3 条 `sanitizeTripItems` 直调测试）；`bun test tests/` 22/22（新增 `tests/cloudflare-api.test.mjs` 11 条：marks/trip payload 创建+取回逐位一致、未知短码 404、格式非法短码 404 且不查 KV、非法 type/JSON/超限 payload 400/413、trip 天数越界 400、同 IP 超日限 429 而跨 IP 不互相影响、短码碰撞正确重掷、KV 未绑定时 503 而非崩溃、未知 `/api/*` 路径 404 且从不转发 ASSETS）；`python3 tools/build.py` 两跑幂等 267+53 零漂移；`bun run build`/`bunx wrangler deploy --dry-run` 均干净通过（14 文件，dry-run 显式确认当前只有 `env.ASSETS` 绑定、无 KV——与「本轮刻意不接 KV」的决策一致，非遗漏）。**浏览器全链路复验**（claude-in-chrome）：`vite preview`（生产同形路径 `localhost:4321/next-stop-gacha/`）下——旧 3 通道（复制链接/二维码 164×164/复制 JSON）regression 通过无回归；行程路书「生成分享短链」按钮真实点击触发 `POST /api/share` 请求、因无 Worker 而失败、toast 正确显示「短链生成失败，用「复制路书文本」代替」；分享面板「生成短链」按钮同样失败后 toast「短链生成失败，用二维码或复制 JSON 代替」；控制台全程零 error。`vite dev`（`localhost:4322`，模块未打包，可用 `import()` 直接拿到 `openSharedRoadbook` 验证只读渲染）：`?sc=BOGUS1`（含数字 1，天然越过字母表校验）打开页面后 URL 静默清理、控制台零 error、无 toast 打扰；直调 `openSharedRoadbook([{hangzhou,2},{chengdu,3}], "2026-08-01")` 正确渲染只读 banner + 隐藏分享按钮，且渲染前后 `localStorage.nextstop_v2` 完全不变（浏览器里该 origin 遗留的真实旧行程数据原样保留，未被分享预览覆盖——直接证据证明「分享打开不写本机行程」这条不变式成立）。**生产地址复验（push c9f749a 后，Cloudflare 自动部署完成，KV 未绑定阶段）**：`https://lab.medspiral.com/next-stop-gacha/` 引用的 `assets/index-ae08VVH_.js`/`index-Cgoi8sZM.css` 与本地构建产物文件名逐字一致（含 tsc+vitest 门禁跑通的直接证据——**F45 残余项在此收尾**：这是 verify 门禁上线后第一次真实内容变更部署，远端确实把 vitest 一并跑过了，不是靠哈希不变侥幸绕过）；两资产 200，JS 的 `cache-control` 仍为 `public, max-age=31536000, immutable`（F42 不回归）；`data/manifest.json` 200；`POST /api/share` 与 `GET /api/share/ABCDEF` 均 503（`{"error":"unavailable"}`，`cache-control: no-store`）——KV 未绑定时的预期故障态，前端据此静默回退；`GET /api/whatever` 404（未知 API 路径，不透传 ASSETS）；Lab 首页 `/` 仍 200 不受影响。outage 类回归 = 零，M40 的「故障不影响核心功能」三件套自查在生产实测成立。

**2026-07-19 KV 绑定激活**：用户完成 `wrangler login`；implementer 全局安装 `wrangler`（`bun add -g wrangler`，连带 `brew install node`——wrangler 全局垫片脚本 `#!/usr/bin/env node` 硬依赖系统 node，纯 bun 环境跑不起来，此前项目内全部走 `bunx wrangler` 绕开了这个问题）；`wrangler kv namespace create APP_KV` 建出 namespace `f4933a0844864509a80fe3524b40b27a`，写入 `wrangler.jsonc` 的 `kv_namespaces` 块｜Verified（激活前）：`wrangler kv key put/get/delete --remote` 对新 namespace 直接读写验证通过（put→get 返回 "world"→delete→再 get 返回 404，证明 namespace 本身在 Cloudflare 侧真实可用，不是本地模拟——默认不加 `--remote` 时 CLI 实际操作的是本地模拟存储，这是本轮踩到的一个 wrangler 4.x 行为细节）；`bunx wrangler deploy --dry-run` 确认 `env.APP_KV` 绑定被正确读取；`bun run verify` 76/76、`bun test tests/` 22/22、`python3 tools/build.py` 幂等，均在改动前后无变化（此次改动只碰 `wrangler.jsonc` 一行配置，不碰代码）。**生产端到端验证（push 521da8d 后，Cloudflare 部署完成）**：`POST /api/share`（marks payload）首次返回真实 200 而非 503，拿到短码 `JD4JXH`；`GET /api/share/JD4JXH` 取回的 payload 与提交内容逐字一致；`POST /api/share`（trip payload，含 `tripStart`）同样 200 拿到短码 `FRFK2J`，`GET` 取回逐字一致；`GET` 未知短码仍 404；根路径/`data/manifest.json`/Lab 首页均 200，零回归。两个测试短码已用 `wrangler kv key delete --remote` 手动清理（不必等 180 天 TTL 自然过期）。**M40 至此完整闭环：代码、测试、生产部署、KV 绑定、端到端真实数据全部验证通过，短链分享功能正式对用户可用** [cc]

- M41 — 后端：同步码云同步 [R2 · S3] → sonnet · xhigh · cc in-session（2026-07-19，复用 M40 已建好的 `APP_KV` namespace + share 的校验/限流/重掷模式，改动收敛在同一个 `cloudflare/api.mjs` 与一个 UI 模块，判断委派往返成本高于会话内直做；S3 因为触碰用户最长命的收藏/打卡资产，故本模块验证明显重于 M40，见下方 Verified）｜spec 见 design「后端·同步码云同步」。**⚠️ 架构口径提示（2026-07-19 补，F53）**：本条目记录的是 M41 首次落地时的实现——同步码/限流计数存 KV。这套存储方案已在下方 ✅「M41·review 响应第二轮」条目里被 Durable Object 取代（codex 两轮 review 实测坐实 plain KV 没有跨请求原子 read-modify-write，并发同步会丢数据/限流可被绕开）。**当前实际架构以「review 响应第二轮」条目为准**；本条目的 12 位数字码/POST-only/`validateMarksPayload` 复用/TTL 400 天/`{favs,visited}` 8KB 上限等决策依然成立，只是存储后端从 KV 换成了 DO、PUT 从「全量覆盖」换成了「服务器端并集合并」，下文提到 `APP_KV`/`sync:` 前缀/`checkRateLimit(kv,...)` 的地方按此换算，不再逐处修改原文。落地：①`cloudflare/api.mjs` 新增 `handleSyncCreate`/`handleSyncPut`/`handleSyncGet`：同步码为纯数字 12 位（`crypto.getRandomValues`，10^12 空间，口述/手抄比字母数字混排顺手，碰撞重掷同短链逻辑）；payload 复用既有 `validateMarksPayload`（`{favs,visited}` 字符串数组）——**行程不同步**（design 拍定：行程是短命工作台，收藏/打卡才是长命资产），trip 形状的 payload 一律 400；POST 生成新码（种子=调用方当前数据）、PUT 全量覆盖且**只能覆盖已由 POST 创建过的码**（未知码 404，防止客户端凭空指定 key 把 KV 当免费无限存储）、GET 供前端取回；写入都带 `updatedAt`（`new Date().toISOString()`）。**TTL 决策（implementer 拍定，design 未强制指定同步码存活期）**：不同于短链的一次性 180 天 write-once，同步码给一个较长（400 天）且**每次 PUT 续期**的存活期——呼应 design「存储与精确性」把同步快照列为「可再生」资产，免费额度卫生优先于「永久」承诺，本机 localStorage 恒为真相源，过期后需重新走 POST 生成新码（**订正，2026-07-19**：这里原文错写成「同一个码过期后仍可重新 PUT 复活」，与代码实际的 POST-only 语义矛盾——codex F50 复核指出后已修正措辞，PUT 从不创建/复活任何码，只能覆盖已存在的）。**限流决策**：`checkRateLimit` 从固定 20/天泛化为 `(kv, ip, bucket, limit)`，短链创建/同步码创建/同步读写各占独立 bucket（`ratelimit:share:*`/`ratelimit:sync-create:*`/`ratelimit:sync-rw:*`），互不挤占；同步读写（GET+PUT 共享一个 bucket）限额比创建宽松（60/天 vs 20/天）——同步码是长期私有凭证，猜码即读到他人收藏/打卡，GET 也纳入限流（不同于短链纯只读不设限的读侧）。②`cloudflare/worker.mjs` 补 `/api/sync`（POST）与 `/api/sync/:code`（GET/PUT）路由，同 M40 一样先于 ASSETS 代理匹配。③前端 `src/services/syncApi.ts`（新，同 shareApi.ts 风格）：`createSyncCode`/`pullSync`/`pushSync` 任何失败一律返回 `null`/`false`。④`src/store.ts` 新增 `getSyncCode`/`setSyncCode`/`clearSyncCode`（新 localStorage 键 `nextstop_sync_v1`，只是「本机认哪个码」的指针，不是长命资产本身，丢了不影响 favs/visited）。⑤`ui/share.ts` 新增 `syncNow()`（留空点「同步」=生成新码；填码=GET 拉取→**复用 `parseShare`/`mergeUnion` 同一套并集合并信任边界**（与链接/JSON 导入同源）→本机合并写回→PUT 把合并后结果整份传回去，让远端逐步收敛为「见过的并集」）与 `forgetSync()`（清本机指针，服务器数据不受影响）；`syncNow` 是用户主动点击触发（不是被动打开别人发来的链接），故直接合并不再弹二次确认条——按钮点击本身就是确认。⑥`index.html` 分享面板新增「☁️ 同步码」`<details>` 区块（输入框+同步/解绑按钮+状态行）；`style.css` 补 `.share-import input` 样式｜**关键正确性点（同 M40 的 `state.trip` 教训同类）**：`syncNow` 只合并写 `state.favs`/`state.visited`，绝不触碰 `state.trip`/`state.cmp` ——已用浏览器直接验证（见下方）。Verified：`bun run verify`（`tsc --noEmit && vitest run`）76/76（无新增，纯函数侧无改动）；`bun test tests/` 34/34（**订正，2026-07-19**：这里原文错写成「新增 23 条」，实际是 `cloudflare-api.test.mjs` 当时总数达到 23 条——M40 原有 11 条 + 本轮 M41 新增 12 条，codex F50 复核指出口径错误后已修正；后续 review 响应轮又新增测试，见下方 review 响应条目的最新总数）：POST 生成 12 位数字码+GET 带 updatedAt 取回、PUT 覆盖+GET 反映新值、PUT 未知码 404、GET 未知码 404、GET/PUT 畸形码 404 且不查 KV、非 marks 形状/畸形 JSON/超限 payload 400/413、create 与 put 两次写入 TTL 均为 400 天、sync-create 限流与 share-create 限流互不挤占、sync-create 单独达日限 429、sync 读写共享 bucket 且上限高于创建（60 次触发 429）、码碰撞正确重掷、KV 未绑定时创建/GET/PUT 均 503）；`python3 tools/build.py` 两跑幂等 267+53 零漂移；`bun run build`/`bunx wrangler deploy --dry-run` 干净通过（14 文件，`env.APP_KV`/`env.ASSETS` 两绑定均正确读取）。**浏览器全链路复验**（claude-in-chrome，`vite dev` 于生产同形路径 `localhost:4325/next-stop-gacha/`，`window.fetch` 打桩模拟 `/api/sync` 三个方法的响应，真实点击走真实事件接线）：种子本机数据 `favs:[hangzhou] visited:[hangzhou] trip:[chengdu,hangzhou两站] cmp:[hangzhou,chengdu]`——①空输入点「同步」→ 真实 POST 触发、拿到桩码 `111122223333`、本机绑定+复制+toast 全部正确；②切换到新码 `222233334444`（桩 GET 返回 `favs:[chengdu,sanya] visited:[chengdu]`）点「同步」→ toast「已同步：♥3 收藏 · 👣2 打卡」，`localStorage.nextstop_v2` 验证 favs/visited 正确并集（`[hangzhou,chengdu,sanya]`/`[hangzhou,chengdu]`），**`trip`/`cmp`/`tripStart` 逐字节完全不变**（关键不变式直接证据，同 M40 roadbook 非破坏性证明同一等级），且真实触发了 PUT 把合并结果传回；③畸形格式码（非 12 位数字）→ toast 正确拒绝、**零网络调用**（用调用计数器验证客户端校验短路生效，没有发起任何 fetch）；④合法格式但未注册的码（桩 GET 404）→ toast「同步失败：同步码不存在或网络异常，本机数据未受影响」，本机数据与绑定码均保持不变（未误切换到失败的码）；⑤解绑本机 → 本机指针清空、按钮隐藏、状态行归位、服务器数据不受影响提示；⑥恢复真实（未打桩）`fetch` 对本地无 `/api` 实现的 dev server 发起真实网络请求（非模拟）→ 优雅降级为「同步码生成失败，请稍后重试」toast，无异常抛出。全程控制台零 error。**免费后端三件套自查**：Cloudflare Workers/KV 免费额度内（同 M40，无新绑定资源）；无绑卡；API 挂掉/未配置/网络异常时核心功能（本机收藏/打卡的增删查改）完全不受影响，仅同步这一增强功能优雅降级。**生产地址复验（push e5cfbe0 后，Cloudflare 自动部署完成）**：`assets/index-CQNwpyv1.js`/`index-BC-POuMk.css` 与本地 `bun run build` 产物 `diff` 逐字节一致（F45 门禁在本次真实内容变更部署再次跑通，不是巧合）；JS `cache-control` 仍 `immutable`、`data/manifest.json` 200、Lab 首页仍 200，零回归。`/api/sync` 对**真实生产 KV** 完整跑通：`POST` 拿到真码 `306135487050`、`GET` 返回种子 payload 与真实 `updatedAt`、`PUT` 覆盖后 `GET` 立即反映新值（`favs`/`visited` 均更新）；`GET`/`PUT` 未知码均 404（验证碼必须由 POST 创建，PUT 不能凭空生造 key）；畸形码 404；`/api/share` 回归测试仍正常（拿到真短码 `KMP64F`）；未知 `/api/*` 仍 404。两个测试用真实 key（`sync:306135487050`、`share:KMP64F`）已用 `wrangler kv key delete --remote` 清理，不留测试垃圾数据。**M41 至此完整闭环：代码、测试、生产部署、真实 KV 端到端全部验证通过，但按 design S3 定级（触碰用户最长命资产）尚待跨家族 review 通过才算最终关闭** [cc]

- M41·review 响应（F47–F50，2026-07-19 cc in-session）— codex 复核 baseline a896fda 提四条，逐条处置：**F47**（P1，限流在真实 KV 上不成立）codex 指出 `checkRateLimit` 的 GET→PUT 会在 1 秒内对同一 `ratelimit:sync-rw:*` key 写两次，撞上 Cloudflare KV「同 key 每秒限写 1 次」的官方限制会让第二次写抛错、把正常同步炸成 500；且 `handleSyncPut` 的 KV 存在性读发生在限流检查之前，未知码 PUT 能无限次白嫖 KV 读配额。采纳双修：①`checkRateLimit` 整体包 try/catch **fail-open**（计数读写出任何问题都放行而非把异常炸给调用方——限流是防滥用辅助层，不是核心功能前提，与全项目「后端从不是可用性前提」一致；代价是高并发下计数近似而非精确，判断这对个人项目体量可接受，未引入 Durable Object/协调器去做精确原子计数）；②限流检查挪到 `handleSyncPut` 的 KV 存在性读之前，未知码也受限流保护。**F48**（P1，并发下 last-write-wins 会丢数据，最严重的一条）：codex 指出客户端「GET 旧快照→本地并集→整份 PUT」在两台设备近乎同时同步时，plain KV 无 compare-and-swap，落败的写会丢掉对方独占的新增项。**部分采纳**（未做到 codex 建议的完全闭合，理由记录见下）：把并集合并挪到服务器端——`handleSyncPut` 收到 PUT 时先读现有值、与请求 payload 取并集再写回，把竞态窗口从「一次完整客户端往返（用户点按钮到网络返回，秒级）」收窄到「单次 Worker 调用内部 GET→PUT 的间隔（毫秒级）」，且落败的写本身已经是「旧值∪自己」而非被整体替换，下次任一方再同步即可自愈；PUT 响应现在带回服务器端真正落盘的合并结果，前端 `syncNow()` 据此再并集一次本机状态，不再只信自己那份局部合并（`pushSync` 返回类型从 `boolean` 改为 `SyncSnapshot | null`）。**未做**codex 建议的 Durable Object 方案：已查证 DO 在 Workers 免费计划可用（仅限 SQLite storage backend，非旧版 KV backend，语雀免费额度内），并非因为绝不绑卡约束而放弃，是判断这个功能是用户手动点按钮触发（非后台自动轮询），两台物理设备真在同一个毫秒级窗口内点同一个同步按钮的概率可忽略，为此再加一个 DO 绑定/迁移块/独立测试基础设施（现有 node:test+fakeKV 打法覆盖不了 DO，需要 `@cloudflare/vitest-pool-workers` 整套新基建）超出这个残余风险实际需要的复杂度——**这是一个记录在案的权衡，不是回避**，留给下一轮 codex 复核评估是否接受，若认为不够则升级为独立模块动 DO。**F49**（P1，8KB 上限只测投影后 payload，攻击者可用不影响存储的巨大多余字段绕过、让 Worker 白白读取解析大 body）：新增 `readBoundedJson(request, maxBytes)`，在 `JSON.parse` **之前**对原始请求体流式计数（`ReadableStream.getReader()` 边读边累加字节，超限立即 `cancel()` 中止并返回 413），Content-Length 头只做快速路径（不可信，可缺失/chunked），`handleShareCreate`/`handleSyncCreate`/`handleSyncPut` 三处统一改用。**F50**（P2，「POST-only 建码」与「过期后同码 PUT 复活」两条记录互相矛盾，代码只支持前者）：删除 `api.mjs` 里「同码可复活」的错误注释，明确码严格 POST-only、过期即永久失效；前端 `pullSync` 返回类型改为 `{ok:true,data}|{ok:false,reason:"not_found"|"error"}`（原来一律 `null` 吞掉了「码不存在」与「网络抖动」的区别），`syncNow()` 据此在**当前本机绑定的码被判 not_found 时自动解绑**并给出「已自动解绑，重新点同步即可生成新码」的明确提示，网络类失败仍走原来的通用提示；已顺带校正上一条 ✅ 记录里「新增 23 条同步测试」的口径错误（实际是 M40 原有 11 条 + M41 新增 12 条 = `cloudflare-api.test.mjs` 共 23 条，本轮又新增 6 条 F47–F49 回归测试，现总计 29 条）｜Verified：`bun run verify` 76/76（无改动）；`bun test tests/` 40/40（`cloudflare-api.test.mjs` 29/29，新增 6 条：限流 fail-open 不 500、真实 GET+PUT 序列不因同 key 秒级写限崩溃、未知码 PUT 也受限流保护、PUT 服务器端并集合并保留对方独占新增项、巨大多余字段被拒绝、无 Content-Length 时仍靠流式计数正确拒绝超限）；`python3 tools/build.py` 两跑幂等；`bun run build`/`bunx wrangler deploy --dry-run` 干净通过。**浏览器复验**（claude-in-chrome，`vite dev` 打桩 fetch）：本机绑定的死码同步失败后自动解绑+input 清空+status 归位（且 `trip`/`cmp`/`tripStart` 逐字节不变）；临时输入的未绑定死码同步失败后**不**触发解绑、input 保留原样便于用户核对是否打错；成功同步场景下 PUT 响应刻意造出「比本机知道的更全」的合并结果（模拟另一台设备插了一脚），验证前端正确把这份服务器合并结果并入本机状态（toast 与最终 `localStorage.nextstop_v2` 逐一核对，`trip`/`cmp` 依旧不变）；控制台全程零 error。**生产地址复验（push 952c081 后，Cloudflare 自动部署完成）**：`assets/index-DRp_S5m7.js` 与本地构建产物字节级一致，`manifest.json`/Lab 首页均 200 零回归；**在真实生产 KV 上重演 F48 场景**——建码后先模拟另一台设备 PUT 只含 `sanya`，本设备再 PUT 只含 `hangzhou,chengdu`（不知道 sanya 存在），响应正确返回三者并集 `["sanya","hangzhou","chengdu"]`，证明服务器端并集合并在真实 KV 上确实生效、未被覆盖；F49 的巨大多余字段 payload 真实返回 413；F50 的 PUT 到从未创建的码仍 404（无复活）；`/api/share` 回归测试仍正常。测试用 key 已清理 [cc]

- M41·review 响应第二轮（F47/F48 用 Durable Object 重做，2026-07-19 cc in-session）— codex 复核 baseline 1b98368 用受限频 fake KV 连续重放 100 次同 IP 未知码 PUT，实测「限流 fail-open 在真实 KV 写频约束下等于完全不设防」（非精度问题，是防线可被持续绕开）；又用并发 barrier fake 让两个 PUT 都读到旧值再写，证明「服务器端并集合并」在真并发下仍会 last-write-wins（`design.md` 当时也没同步这条实际语义，一并挑出）。**这次没有再选缓解/折中——两条建议的根因相同（plain KV 无跨请求原子 read-modify-write），已查证 Durable Object 在 Workers 免费计划可用（仅限 SQLite storage backend），改用 DO 正面解决**：新增 `cloudflare/durableObjects.mjs`——`RateLimiter`（一个 `(bucket,ip,day)` 一个实例，`checkAndIncrement()` 靠 DO 的 input/output gate 保证 get→put 对同实例其它并发调用不可交错，真原子；25 小时后 alarm 自毁存储，防止堆积占额度）与 `SyncCodeStore`（一个同步码一个实例，`create`/`read`/`merge` 三个 RPC 方法，`merge()` 内部读现值+取并集+写回全程无 await 外部 I/O，同一实例上真并发的两个 PUT 不会交错执行，`merge()` 超出 8KB 直接返回 `tooLarge` 不落盘；DO 没有 KV 的 `expirationTtl`，改用 `setAlarm` 实现「闲置 400 天自毁」，每次 create/merge 都续期，alarm 触发即 `deleteAll()`，之后 read/merge 都判定未创建——严格保留 F50 的 POST-only、不可复活语义）。`cloudflare/api.mjs` 的 `checkRateLimit`/`handleSyncCreate`/`handleSyncGet`/`handleSyncPut` 全部改用 DO stub（`env.RATE_LIMITER`/`env.SYNC_CODE_STORE`），`share:` 短链保留在 KV（write-once 从不 PUT，没有这类竞态）。`wrangler.jsonc` 加 `durable_objects.bindings` + `migrations.new_sqlite_classes`；`cloudflare/worker.mjs` 从 `durableObjects.mjs` re-export 两个类（DO 类必须从 `main` 入口导出）；`isGamePath`/`GAME_PREFIX` 抽到新的 `cloudflare/gamePath.mjs`（纯函数、零 cloudflare 专属导入），因为 `worker.mjs` 现在连带导出 DO 类，会拖 `cloudflare:workers` 这个虚拟模块，跑不进 build-assets 测试所在的纯 Bun 环境。**测试基建同步换血**：装 `@cloudflare/vitest-pool-workers`（与已装的 `vitest@4.1.10` peer 兼容，无需降级），新增 `vitest.workers.config.ts`（用当前版本 0.18.6 实际的 `cloudflareTest` 插件写法——技能文档里的 `defineWorkersConfig` 在这个版本已经不存在了，写之前先用真实 npm 包的 exports/README 核实过，没有照抄可能过时的文档），`tests/cloudflare-api.test.mjs`/`cloudflare-worker.test.mjs` 从 `node:test`+手写 `fakeKV` 整体改写为跑在真实本地 workerd 沙箱里的 vitest 测试（`env` 来自 `cloudflare:test`，是 wrangler.jsonc 声明绑定的真实本地模拟，不是手写 mock）；`tests/build-assets.test.mjs` 拆出单独文件承接需要真实文件系统/子进程的那一条（进不了 workerd 沙箱，保留 `bun test` 跑，`package.json` 新增 `test:build-assets` 脚本，`test` 脚本改指向新的 workers pool 跑法）。**关键新增回归**（用真实并发证明，不是顺序请求）：`F48 regression` 用 `Promise.all` 同时对同一个码发两个 PUT（分别加 `chengdu`/`sanya`），断言最终 GET 拿到三者并集——DO 场景下真的通过（KV 场景下这条会失败，此前只是没人这么测过）；`F47 regression` 用 `Promise.all` 并发发 25 个请求，断言成功数恰好等于限额 20（不多不少，证明并发下计数既不超放也不丢计数）；另外用 `runDurableObjectAlarm()` 直接触发某个同步码实例的 alarm，验证到期后 read/GET/PUT 全部判定为不存在——这是第一次真正验证 TTL 机制本身生效（之前 KV 版本只断言过 `expirationTtl` 选项被传了，从没证明过期真的会发生）。`design.md`「后端·同步码云同步/限流」两条按 codex 要求同步为服务器端并集合并 + DO 存储的实际语义，删除「PUT 全量覆盖」的过时描述 ｜Verified：`bun run verify` 76/76（前端无改动）；`bunx vitest run --config vitest.workers.config.ts` 44/44（worker 路由 11 + `/api/*` 33，含上述两条并发回归、TTL-alarm 回归、DO 直连单测 `SyncCodeStore`/`RateLimiter` 各自的碰撞安全/隔离性）；`bun run test:build-assets` 1/1；`python3 tools/build.py` 两跑幂等；`bunx wrangler deploy --dry-run` 干净通过，绑定列表正确显示 `env.RATE_LIMITER`/`env.SYNC_CODE_STORE`（Durable Object）与 `env.APP_KV`/`env.ASSETS` 四项。前端 `src/` 零改动，构建产物哈希与上一轮一致，故未重跑浏览器 UI 复验（后端存储实现换了，对外请求/响应契约不变，已有的浏览器复验仍然适用）。**生产地址复验（push 641a2e5 后，Cloudflare 部署几乎瞬间完成——`wrangler deployments list` 确认新版本创建时间与推送时间相差不到 30 秒）**：根路径/游戏路径/manifest 均 200 零回归；**在真实生产环境用两个后台 curl 进程真并发**（非顺序 await，用 shell `&`+`wait` 同时发出）对同一个新建同步码分别 PUT `chengdu`/`sanya`——两次响应 `updatedAt` 只差 2 毫秒（`23:54:19.990Z`/`.992Z`），后落地的响应体已经包含了先落地那个的新增项，最终 GET 拿到三者完整并集 `["hangzhou","sanya","chengdu"]`，两边都没丢——这是 F48 在生产 Durable Object 上被真实验证关闭的直接证据，不是本地模拟。F49 巨大多余字段真实返回 413；F50 PUT 到未创建的码仍 404；`/api/share` 回归正常。测试用短链 key 已用 `wrangler kv key delete --remote` 清理；测试用的两个同步码是 Durable Object 存储，没有等价的 CLI 单实例删除命令，留给内建的 400 天闲置 alarm 自然过期（本身也是这次修的功能，等于顺带验证了自毁机制的存在合理性——两个测试码不含真实用户数据，可接受） [cc]

- M41·review 响应第三轮（F51–F53，2026-07-19 cc in-session）— codex 复核 baseline c47f519 确认 F47/F48/F50 已关闭（DO 正常路径的原子限流与并发并集成立），新提三条：**F51**（P1，新写的 44 条 Worker/DO 测试没进任何自动部署门禁）——`bun run build` 之前只跑 `verify`＝`tsc + vitest run`（圈定 `src/**/*.test.ts`），`vitest.workers.config.ts` 那 44 条只能靠人工记得单独跑 `bun run test`，Cloudflare 的构建命令也只调用 `bun run build`，意味着哪怕以后 F47/F48 的并发保证真的回归，部署照样全绿放行——这是和 F45 同类、且这次直接盖住 M41 S3 数据完整性的门禁缺口。采纳：`verify` 脚本改成 `tsc --noEmit && vitest run && vitest run --config vitest.workers.config.ts`，`bun run build`/Cloudflare 部署自动继承；**用刻意改错一条断言验证门禁真拦**（`F47 regression` 断言改成错误期望值 → `bun run build` 退出码 1、`vite build` 未执行；改回后干净通过，同 F45 当年的验证手法）；顺带修正 `vitest.config.ts` 头注和 README「本地开发/构建」「部署」两节里残留的 `bun test tests/`/`node:test` 旧口径，`test:build-assets`（需要真实文件系统/子进程，进不了 workerd 沙箱）保持独立不进闸门，README 已注明原因。**F52**（P2，POST 建码与 PUT 合并用了不同的 8KB 计量口径）——`handleSyncCreate` 只量 `{favs,visited}`，`SyncCodeStore.merge()` 却把 `updatedAt` 也算了进去，两者相差约 30～40 字节，会出现「同样内容 POST 建档能过，随后 PUT 却因为多出的时间戳字节数被判 413」的死角。采纳：`merge()` 的大小校验改成只量 `{favs,visited}`（先算并集判定 tooLarge，再补 `updatedAt` 落盘），与 `create()` 共用同一套口径。**F53**（P2，design/state 仍有段落声称同步码存 KV、PUT 全量覆盖）——`design.md`「后端」prose 已经改对了，但「信息组织」表的「云端中转」行还整体写成 Workers KV；`state.md` 顶部 Status snapshot 与原 M41 落地条目也还是旧口径，两轮修复过程另起了段落，未回填最初的架构声明，容易让后续会话读到互斥描述猜不出哪条是当前事实。采纳：`design.md` 信息组织表拆成两行（短链=KV，同步快照/限流=DO）；`state.md` Status snapshot 的 M41 句子改写为直接陈述当前 DO 架构 + review 进行到第三轮；原 M41 ✅ 条目开头插入一段明确的「架构口径提示」，指向「review 响应第二轮」条目才是当前实际架构，不再逐处回改原文里散落的 `APP_KV`/`全量覆盖` 字样（历史记录保留，指针清楚就够） ｜Verified：`bun run verify`/`bun run build` 45+76=121 测试全绿（新增 1 条 F52 回归：卡在「量 `{favs,visited}` 刚好低于 8192 字节，加上 `updatedAt` 就会越界」这个临界点上的 payload，POST 建档 200，同内容 PUT 也 200，不再 413）；`python3 tools/build.py` 两跑幂等；`bunx wrangler deploy --dry-run` 干净通过，四项绑定不变。**生产地址复验（push 21403f3 后）**：根路径/游戏路径/manifest 均 200 零回归；**F52 场景在真实生产上重演**——构造一个按 `{favs,visited}` 计量刚好 8180 字节（低于 8192 上限，但加上 `updatedAt` 会越界）的 payload，POST 建档 200，同内容 PUT 也 200（旧 bug 会在这里错误 413），确认修复在生产 Durable Object 上确实生效 [cc]

- M38 — 前端工程化：逻辑组件化迁移 [R3 · S3] → fable · xhigh · cc in-session（2026-07-18，R3·S3 与会话模型同档，按规则会话内直做）｜spec 见 design M38 与「前端技术栈」。落地：①决策层 `src/logic/` 纯函数化——filter（含天花板/放宽候选纯描述符化）/gacha（rng 可注入）/itinerary（tripStops/最近邻/leg 整组/门户/tripLegs/bestInsertion/顺路彩蛋候选）/transport/budget/roadbook（模型+骨架+文本导出，天气行走注入）/share（序列化/解析/并集合并/JSON 导入）/map/persist（F13/F14/F18 全规则）/qr（sed 逐字提取加导出，未类型化改写）+ types/constants，全部显式传参、零 DOM/全局依赖，除三处必要改写（放宽 apply 闭包→action 描述符、路书天气行→注入函数、chip 点击与计数收敛到同一 simulateChipClick）外逐字迁移；②展示层 `src/ui/` 15 个视图模块（console/cards/render/dock/detail/compare/mapview/gacha/trip/roadbook/share/actions/events/toast/effects+clipboard/dom），模板字符串与事件接线逐字保留；`src/store.ts`（DATA/state/LS，键名结构不变）+ `src/services/weather.ts`（I/O 隔离）；main.ts 收缩为 40 行启动编排，`@ts-nocheck` 移除（仅 qr.ts 保留，冻结件不改写）；③**Preact 推翻**（见拍板档案）——preact/@preact/preset-vite 依赖删除，vite.config 去 preset，tsconfig 去 jsx 项；④Vitest 基线：vitest.config include 圈 `src/**/*.test.ts` 与 bun test（tests/ worker 用 node:test）互不误伤，73 测覆盖设计七不变式（leg 整组/门户/overland/显式交通/并集合并/容忍型天花板/空池放宽）+ 录案回归（F34 锚点后并列、F35 游轮句式、M28 二~四轮门控与开关、M30 江浙沪、F10 跨海方向、F13/F14/F18 持久化降级、F16 导入、F8 天数文案、F11 点亮口径、预算/投影/抽样/QR 冒烟），合成 fixture + 真实 chunk 双轨（真数据断言 320/46/独库/阿里/三峡/海南西线等录案场景）；⑤`tsc --noEmit` 进 `bun run build` 构建门禁（M37 遗留项闭环，Cloudflare 构建命令不变自动继承）｜Verified: `tsc` 零 error；vitest 73/73；`bun test tests/` 11/11（含 F39 资产前缀集成测试）；`python3 tools/build.py` 两跑幂等零漂移；`bunx wrangler deploy --dry-run` 干净（14 文件，custom build 含 tsc 门禁实跑）；产物体积与迁移前同量级（JS 185.03KB/CSS 22.15KB）；**浏览器全链路复验**（claude-in-chrome，`vite preview` 于生产同形路径 `localhost:4321/next-stop-gacha/`）：12 请求全部 `/next-stop-gacha/` 前缀 200、控制台全程零 error；筛选（江浙沪 46/320、天花板 ¥¥→[¥,¥¥] 288/320、取消恢复）、F34 真实点击链路（成都→乐山彩蛋→顺序 [chengdu, leshan-emeishan]、插到第 2 站 toast）、路书（D1 飞机进/D4 高铁自驾转场/D5 返程回家骨架）、独库整条装入（站间包车/自驾、leg 文案「进线到那拉提/沿线玩过去」、无「版改编」）、扭蛋（转出+票券卡+按钮复位；后台标签页 timer 节流会拖慢动画属浏览器行为非回归）、足迹地图（35 省 path+320 点）、分享（QR 164×164 与 M26 录案一致、hash 导入确认条并集合并且 visited 拒线路 id、F40 两清空按钮）、详情（真实 Open-Meteo 7 天填充）全部与旧版行为一致；localStorage 键结构不变，且上一会话遗留的旧 `nextstop_v2` 经新 persist 路径正确加载（额外的真实旧数据回归）。**生产地址复验（push e205e23 后，Cloudflare 自动部署完成，F39 口径）**：`https://lab.medspiral.com/next-stop-gacha/` 200 且引用新 hashed 产物 `assets/index-j9pL8bYk.js`；该 JS 200 且与本地经浏览器全链路复验的构建**字节级一致**（`diff` 通过——远端构建含 tsc 门禁跑通的直接证据）；CSS/manifest/chunk 全 200；cache-control 分层不回归（hashed JS/chunk `immutable` 长缓存、index/manifest `must-revalidate`）；Lab 首页 `/` 仍 200。部署传播期观测到新旧 index.html 短暂混serving（分钟级，must-revalidate 下的边缘一致性现象），连续 3 次轮询稳定为新版后才判定完成 [cc]
- M37 — 前端工程化：脚手架与数据外置 [R2 · S3] → sonnet · high · cc in-session（2026-07-18，R2·S3 但全链路强耦合——build.py chunk 格式/vite 输出/wrangler 构建命令/worker 资产服务任一环脱节即全站不可用，判断委派往返成本高于会话内直接做，故未拆子代理）｜spec 见 design「架构」「数据外置」「部署」。落地：①`package.json`+`vite.config.ts`+`tsconfig.json` 新增（Vite 8 + TS 7 + Preact 10，`@preact/preset-vite`；Preact 装好但本轮迁移代码未用 JSX，供 M38 组件化消费）；②旧单文件拆解——`<style>` → `src/style.css`；QR 编码器脚本 + 全部业务逻辑逐字迁移（零改写）→ `src/main.ts`（`@ts-nocheck` 标记，未加类型标注，M38 拆分建模块时再补类型）；`CN_MAP`（SVG 省界数据常量）→ `src/cn-map.ts`；根 `index.html` 收缩为 Vite 入口（保留原 body 标记 + `<script type="module" src="/src/main.ts">`）；③数据外置——`tools/build.py` 删除注入 `index.html` 的尾段，改为把校验通过的 `merged` 数组按固定批大小（40 条/批，纯 HTTP 并行考量、无地理语义）顺序切成 `public/data/chunk-N.json` + `manifest.json`（8 chunk，495KB，会随源数据一并提交——Cloudflare 构建侧不跑 python）；`main.ts` 顶部加 `loadData()`：`fetch` manifest 后 `Promise.all` 并行拉所有 chunk，**按 manifest 顺序（非 fetch 完成顺序）`flat()` 拼接**赋给 `DATA`，`boot()` 挪进 `.then` 之后异步执行——顺序不变式很关键：`filtered()` 未选排序方式时直接用 `DATA` 原始顺序展示，验证过拼接后首卡仍是杭州（与旧版一致）；④`wrangler.jsonc` 的 `build.command` 从「cp index.html」改为「`bun install && bun run build`」（`vite build`）；⑤`cloudflare/worker.mjs` 未改动——通用前缀剥离+ASSETS代理对多资产天然成立，`tests/cloudflare-worker.test.mjs` 新增两条请求级测试钉住 `data/chunk-N.json`/`data/manifest.json`/hashed JS 资产路径的前缀剥离（8/8 全过）｜**迁移中抓到一个真实 bug**：逐字搬运时漏抄了 QR 脚本与业务脚本之间的 `<canvas id="confettiCanvas">` + `<div id="toast">` 两个元素（原两个 `<script>` 标签间的窄缝，被两段独立 sed 范围各自跳过）——导致 `toast()` 因 `getElementById("toast")` 返回 null 抛异常，恰好在 `saveLS()` 之后触发已经问题不大，但当异常发生在 `saveLS()` **之前**的调用路径（如 `toggleTrip` 内 `toast` 先于部分场景）会让整条 delegated click handler 中断、后续状态和渲染全部不执行——加回两元素即修复，已用 `diff` 逐行核对整份旧文件重建版本确认无其它遗漏。｜Verified: `python3 tools/build.py` 两跑幂等（267+53→8 chunk，495KB，`public/data/` 内容字节相同）；`bun test tests/` 8/8；`bunx vite build` 零 error 零 warning（`dist/index.html` 6.64KB + JS 185KB + CSS 22KB + 8 chunk + manifest）；`bunx wrangler deploy --dry-run` 完整跑通自定义 build 命令、正确读取 dist 资产、ASSETS 绑定生效、干净退出（同 M36 流程；本次「读到 14 个文件」比本地 `find` 出的 12 个多 2，怀疑是 wrangler 内部簿记而非产物缺漏，未继续深挖，留意下次真实部署核实）；**浏览器端全链路人工点击复验**（claude-in-chrome，先 `vite` dev server 后 `vite preview` 生产构建各测一遍，控制台全程零 error/exception）：筛选（点「江浙沪」chip 命中 46/320，取消恢复 320/320）、扭蛋（转出有效目的地）、单/多站行程（成都+乐山 F34 顺路彩蛋点击验证顺序为 `[chengdu, leshan-emeishan]`，即 F34 在真实点击链路下成立而非仅 bun 断言）、路书生成（成都→乐山逐日速览含正确交通方式「高铁/自驾」「飞机」+ 完整 plans/美食/住宿文案）、收藏/打卡/足迹地图（35 个省份 SVG path 渲染）、分享统计+二维码 canvas（164×164）、天气预报（详情页真实 Open-Meteo 数据 + CC BY 署名）全部与旧单文件版行为一致；localStorage 键名/结构不变——`nextstop_v2` 仍为 `{favs,cmp,trip,visited,tripStart}`、`nextstop_wx_v1` 仍是天气缓存键，老用户本地数据无损。**未验证**：本机无法执行真实 Cloudflare 生产部署（同 M36 只能 dry-run），线上效果留待 push 后用户/下轮确认；`tsc` 未接入构建门禁（`src/main.ts` 零类型标注，M38 逻辑组件化时再建） [cc]
- F39–F43 修复（2026-07-18 cc in-session，codex 跨家族 review baseline 7f0d6b1 触发）[R2 · S3] → sonnet · high · cc in-session（生产 outage 直接修复，判断优先级高于等新会话）｜**F39**（P0，生产实际 404）：`vite.config.ts` 的 `base` 误判成「Worker 已剥前缀所以站内可用根路径」——剥前缀发生在请求已经命中这个 Worker **之后**，但路由匹配（`lab.medspiral.com/next-stop-gacha/*`）决定请求进不进这个 Worker 发生在**之前**；`base:"/"` 让 Vite 产物写出 host-root 的 `/assets/*`，这类请求根本进不了本 Worker，正式环境已实测 404。修复：`base:"/next-stop-gacha/"`；`loadData()` 的 fetch 路径改用 `import.meta.env.BASE_URL` 显式拼接（不再依赖相对路径隐式解析，虽然相对路径这次凑巧是对的）。**F40**（P1）：两处 `onclick="resetFilters()"`（静态空态按钮 + `buildConsole()` 动态生成按钮）在 module 化后不再能访问模块内函数（module 顶层声明不落 `window`）——改为 `id` + `addEventListener`，与本函数其余按钮同款。**F41**（P2，数据漂移）：`wrangler.jsonc` 构建命令补上 `python3 tools/build.py &&` 前缀，每次部署都从 `data/*.json` 重新生成 chunk，不再信任已提交的 `public/data/` 是新鲜的（假设 Cloudflare 构建镜像有 python3，未在真实远端验证，若不成立会直接构建失败而非静默用旧数据——好于当前的沉默漂移）。**F42**（P2，缓存策略）：chunk 文件名从固定 `chunk-N.json` 改为内容 hash 命名 `chunk-N-{sha256前10位}.json`（`tools/build.py`），`cloudflare/worker.mjs` 按路径模式显式设置 `cache-control`——hash 命名的 `assets/*` 与 `data/chunk-*-hash.json` 给 `immutable` 长缓存，`manifest.json`/`index.html`/其余入口给 `must-revalidate` 短缓存，不再信任 ASSETS 上游原始头。**F36 收尾**：M32 state 条目补 `→ sonnet · low` 完整拨盘。**F43**：见上方 Status snapshot 的教训记录，state 口径已同步生产现实 ｜Verified：`bun test tests/` 11/11（新增 hash chunk 前缀剥离、两档 cache-control 断言、以及**真跑 `vite build` 后解析 `dist/index.html` 提取真实 asset URL 并断言其落在 `/next-stop-gacha` 前缀内**的集成测试——直接对应 F39 教训，逐行代码核对无法发现这类"运行时请求形状"错误）；`python3 tools/build.py` 两跑幂等、旧 hash 文件正确清理（无残留）；`bunx vite build` 产物 `dist/index.html` 的 `<script src>`/`<link href>` 现为 `/next-stop-gacha/assets/...`；`bunx wrangler deploy --dry-run` 干净通过（含新 build 命令、14 文件读取同前）；**浏览器网络层复验**（claude-in-chrome，navigate 到 `http://localhost:4321/next-stop-gacha/` 这个与生产同形状的路径，而非 M37 首版验证时用的站点根路径）：`read_network_requests` 确认全部 12 个请求（HTML/JS/CSS/manifest/8 chunk）均以 `/next-stop-gacha/` 为前缀且状态码 200；筛选/行程/F34 顺路彩蛋顺序/扭蛋/路书/足迹地图全部复测通过，控制台零 error。**生产地址复验（push 092e097 后，Cloudflare 自动部署完成）**：`curl https://lab.medspiral.com/next-stop-gacha/` 返回的 `index.html` 引用 `/next-stop-gacha/assets/index-CPJbmgfd.js` 与对应 CSS，两者直接 `curl` 均 200（此前 F39 状态下同样操作是 404）；`data/manifest.json` 与 `data/chunk-0-e2e6c2a88e.json` 均 200；`cache-control` 分层生效——hashed JS 为 `public, max-age=31536000, immutable`，`manifest.json`/`index.html` 均为 `public, max-age=0, must-revalidate`；Lab 首页 `/` 不受影响仍 200。**F41 前提假设已验真**：Cloudflare 构建镜像确实带 python3——`python3 tools/build.py && bun install && bun run build` 已在其构建环境成功跑过。outage 解除是生产地址本身验证的，不是本地 dry-run 自证。`wrangler dev` 本地全链路服务器因环境 HTTP 代理疑似拦截而连接超时，未跑通（改用 `vite preview` + 网络请求核对前缀 + 最终生产地址直接验证，三重替代证据） [cc]
- F34/F35 修复（2026-07-18 cc in-session，M37 开工前 review 触发）｜codex 复核发现 [R2 · S2] → sonnet · high：**F34**（P1）单站行程「落脚顺游」在锚点前/锚点后两个缺口对同一候选给出完全相同的 add/extra（对称算式），`bestInsertion()` 严格 `<` tie-break 只留住先遍历到的锚点前一侧，把「到了成都顺便乐山」插成「乐山→成都」——加完全并列判据：同锚点同度量时优先锚点后的出站位（`a===anch`）；**F35**（P2）路书逐日速览把移动住宿（`stays` 含"游轮/轮渡/火车"）当地名套「游X一带」模板，三峡连住两晚吐出「游游轮一带」——`skeletonRows()` 加 `TRANSPORT_META` 命中分支，改输出「X上继续游览沿途景点」｜Verified: bun 断言 4/4（脚本未入库，临时校验）——成都单站插乐山得 [chengdu, leshan-emeishan]（回归：多站候选仍按几何解出、无 NaN/Infinity 异常）；三峡两晚游轮 D2 act="游轮上继续游览沿途景点" 不再含"游游轮"（回归：真实地名连住如"丽江/丽江"仍产出"游丽江一带"不变）；build 两跑幂等 267+53 零 warning；**M37 迁移后浏览器复验（新 Vite 构建）**：生产构建下空行程加成都→点乐山彩蛋 → `trip:[chengdu, leshan-emeishan]` 顺序正确（F34 修复在真实点击链路下成立，非仅 bun 断言） [cc]
- M28·四轮——彩蛋池尊重开关 + 插入位置保证复核 [R2 · S2] → sonnet · high · cc in-session（2026-07-17）｜用户拍板三条处置（①修②候补③维持不做）+ 要求确认插入位置：候选池过滤新增 `!(noAlt && d.alt)` 与 `!(hideVisited && visited含)` 两谓词（其余浏览筛选不约束行程建议）；插入位置经复核**本来就正确**——insertOnWay 与展示共用同一个 bestInsertion 的位置下标 splice，永不追加末尾，本轮补多城断言固化 ｜ Verified: bun 断言 17/17——丽江顺游基线含泸沽湖⛰/香格里拉⛰、开避高海拔后全排除、标记去过+开隐藏后都江堰不再被推（注：开关断言为等价复刻池过滤逻辑，非抽取原句）；杭州→成都→南京 + 点都江堰 → 插成 杭州→成都→都江堰→南京（at=2 紧邻锚定站）；build 两跑幂等；浏览器端仍待 codex 一并复验 [cc]
- M28·三轮扩展——落脚顺游 [R2 · S2] → sonnet · high · cc in-session（2026-07-17）｜用户拍板「到了成都顺便乐山完全可以」：彩蛋从单一廊道语义扩为两类同池混排（廊道顺路「+绕Nkm」/ 落脚顺游「距X约Nkm」，锚定非上海站点 ≤150km 且短驳非飞机），spec 见 design「顺路彩蛋」（二轮门控与三轮顺游已合并成文）｜Verified: bun 纯函数断言 11/11——直飞行程只出锚定目的地站的顺游（成都→都江堰56km/乐山·峨眉山128km、三亚→五指山59/乐东99/万宁111、拉萨周边无卡=诚实为空）、杭州+黄山廊道回归逐位不变、成渝混合行程廊道+顺游混排且插入位正确（都江堰插成都前=段0）、海口对陆路行程仍无候选；build 两跑幂等；**浏览器端点击链路未复验（同二轮），一并请 codex 下轮 review 过** [cc]
- M28·二轮修复——飞机段顺路门控 [R2 · S2] → sonnet · high · cc in-session（2026-07-17）｜[interrupt] 用户反馈「顺路彩蛋 +绕0km 问题超级大，直飞起点就不算绕路是吧」——复现坐实：上海⇄拉萨彩蛋推「神农架/苏州/宜兴 +绕0km」、⇄三亚推顺德、⇄成都推溧阳/宜兴/苏州，全是直飞大圆航线正下方的点；根因=bestInsertion 纯几何三边差对飞机段无顺路语义。修复（spec 已入 design「顺路彩蛋」）：段级门控——段本身或插入后两条子段任一 legInfo 判「飞机」则该段不可插，无可插段 add=Infinity 被 <200 过滤；展示下限 +绕1km 不再出现「0km」；insertOnWay 防御 Infinity ｜ Verified: bun 抽取页面纯函数断言 8/8——拉萨/三亚/成都直飞行程彩蛋为空、杭州+黄山陆路行程新旧算法逐位一致（莫干山 +0.82km 未被门控，被扩容后的周庄 +0.67km 挤出前三属排名事实非回归）、成都+重庆混合行程候选全部只锚进成渝陆路段且无江浙沪城市混入、海口(FLY_PROV)对陆路行程无可插段；build 两跑幂等 267+53、diff 仅 +14/-7 代码行 DATA 未动。**注意：仅 bun 纯函数级验证，浏览器端点击插入未复验**，建议下轮 codex review 顺带过一眼 [cc]

- M36 — Cloudflare 独立路径发布 [R2 · S2] → codex medium · high ｜[interrupt] 原游戏仓库继续作为唯一内容源；新增 Wrangler 构建/Route 声明、路径前缀 Worker 与请求级测试，构建产物只含自包含 `index.html`，不复制 `.agent/`、数据源或工具；目标正式入口为 `lab.medspiral.com/next-stop-gacha/`，不绑定整个 Lab Custom Domain。首次连接时 Cloudflare 自动生成 `cloudflare/workers-autoconfig` 分支，把仓库根目录直接作为静态资源，导致带前缀入口命中不存在的实体目录并返回 404；该自动配置未合入 `main`，已由本模块的前缀 Worker 配置取代 ｜ Verified: Node 请求级测试 6/6；Wrangler 4.110.0 官方 dry-run 真实执行构建并读到且仅读到 `dist/index.html` 1 文件；静态产物与源 `index.html` 字节一致；项目 build 零 error 零 warning且工作树无产品数据漂移；正确配置合入 `main` 后 Cloudflare 自动生产部署成功，根路径仍为 200，游戏路径由 404 恢复为 200 并带 `x-content-owner: next-stop-gacha-repo`，线上 HTML 与仓库 `index.html` 同为 769207 bytes、SHA-256 `2bf49ffa…05fb8` [codex]
- M35·review triage（F30–F33，2026-07-16 cc）— codex 复核 F29/M34/M35 提四条，全采纳修复入库：**F30**（P1）F29 的 overland 把所有完整 leg 线路都当公路，三峡游轮重庆→宜昌被标「包车/自驾」——leg 加可选 `transport`（游轮/轮渡/火车枚举，build 校验），`legInfo` 显式模式优先于距离启发式，`tripLegs` 从目标站 leg 读 transport；三峡宜昌段标 `游轮`（🚢，18.2h）；**F31**（P1）海南西线「三亚进海口出」只在文案、路书仍按乐东进/儋州出算，进出点/预算/里程全错——线路加可选 `entry`/`exit`=门户城市 id（引用现有 sanya/haikou），新增 `tripGateway()` 仅在行程恰为某线路完整整组时生效，改写首末大交通端点，行程单/逐日速览/详情/预算四处统一用真实进出点；**F32**（P2）东方卡漏调研拍板的海南铁路博物馆（官方线路确有、web 核实馆藏6499蒸汽机车）——补入 museums + 高亮 + 2/3 日方案，不越东方市域；**F33**（P2）乐东/东方/昌江误标「情侣周末」（长途一次中转，design M17 该档隐含短途）——三卡删除（base+patch），并机械审计既有海南卡确认无同类历史漂移 ｜ Verified: build 零 error 零 warning 两跑幂等 267+53；浏览器——三峡重庆→宜昌=游轮不再自驾、海南西线上海→三亚进/儋州经海口→上海出、D1「进线到乐东」D8「经海口返回上海」四处一致、总里程改用门户端点4625km；回归：破坏整组门户失效回退首末站（与F21一致）、青甘/独库无门户线路首末不变、站间陆路不变；三峡路书渲染🚢目检正常 ｜ review.md 四条待 codex 复核后删除 [cc]
- M35 — 海南西线（F27 收尾，2026-07-16 cc）— 照 `.agent/m33-hainan-west-survey.md` 用户拍板「4 新城 + 独立西线」落地（真环岛按调研仍另立、不做）：新增 4 张海南西海岸城卡（乐东·尖峰岭莺歌海 / 东方·鱼鳞洲俄贤岭 / 昌江·棋子湾霸王岭 / 儋州·东坡火山海岸，全 alt:false、difficulty 一次中转、纬度单调北上）+ route-hainan-west 海南西线（stops 乐东→东方→昌江→儋州 各2天、四站全写 leg、三亚进海口出、days[7,10] 默认8、可达区间[4,13]）；沿用 codex M33 惯例 base 全字段 + patch 镜像 7 后勤字段；口径 263→267 城、52→53 线（README/meta 同步）；含真实锚点尖峰岭雨林/莺歌海盐场/白查村船形屋/棋子湾/霸王岭/东坡书院/千年古盐田/峨蔓火山海岸，transit 保守化「以当期票务实查」不编造具体航班 ｜ Verified: build 零 error 零 warning 两跑幂等 267+53；浏览器整线装入 4 站无「版改编」、站间全陆路（乐东→东方打车/自驾、昌江→儋州包车/自驾、靠 F29 overland）、上海往返飞机、4 新城详情零异常、路线详情卡渲染目检正常 [cc]
- M33·实施跨家族审查 + F29 修复（2026-07-16 cc）— 按角色反转 cc 复核 codex M33（15 城 + 15 线）：15 城坐标逐个核对准确、transit 全部保守化无 F28 类编造、alt 标记正确（若尔盖/日喀则/普兰/札达=true）；15 线站序按坐标序列逐条核对**无 F19 类方向错误**（G318/青甘/G331/九寨甘南/阿里均正确推进），两处轻微折返（滇东南普者黑、呼乌鄂鄂尔多斯）是多点环线固有形态且无 leg 不产生错误叙事；用户已拍板的重叠（青甘/京承/九寨甘南/G318）符合预期。**F29**（cc 自查发现并修复，P1）：路书 `legInfo` 站间交通纯按坐标/省份判，西藏/新疆两远站必落「飞机」——阿里「日喀则→普兰 飞机3.6h」、G318「川西→林芝 飞机」、沙漠公路「和田→轮台 飞机」全是招牌自驾/穿沙段，根本无航班，与 leg 陆路叙事矛盾（命中用户「编造」红线）。修复：`legInfo(a,b,overland)` 加 overland 参数，`tripLegs` 用 `legEligibleIndices`+rid 匹配判定相邻两站同属完整 leg 组时置 overland=true → 60km 以上禁飞机、落「包车/自驾」（中途过夜由 leg.stays 承载）；上海往返大交通不受影响仍飞机 ｜ Verified: 六条带 leg 线路站间零飞机、上海往返仍飞机、包车时长按陆路里程（日喀则→普兰13.7h/G318川西→林芝12.8h/沙漠公路和田→轮台11.6h）；门控正确——非 leg 远城市（喀什+哈尔滨独立卡）仍飞机不误伤、破坏整组（阿里删日喀则）拉萨→普兰回退飞机与 F21 一致、独库等既有 leg 线路一并陆路化；青甘大环线路书渲染目检逐日速览全陆路无「版改编」；build 两跑幂等 [cc]
- M34 — UI 精致插画感升级 [R1 · S1] · cc in-session ｜ Claude Design「1a 精致插画感」方案落地（仅 CSS，零结构/交互改动）：天空改暖光三段渐变（天蓝→浅白蓝→近地平线暖米）+ 太阳双层光晕呼吸、三层山丘远浅近深错落景深、卡片/面板/弹层/扭蛋机硬阴影后叠柔和投影、主按钮/扭蛋 fab 内高光、ink 柔和藏蓝 `#37485a`、标题白高光；新增珊瑚/青绿/琥珀备用 token；REGION_COLOR 九区色与橙/紫/粉 UX 语义色系统保持不动 ｜ Verified: 首页/详情/扭蛋机/路书四视图截图目检卡通基调统一、通透感与景深到位；三主按钮内高光生效；build 两跑幂等 [cc]
- M33 — 内容扩容二期实施 [R2 · S3] → codex · high ｜先独立复核 3ef86ee，F24–F28 以正常构建、三类反例 fixture、jq 结构断言及平凉官方铁路信息复查全部关闭；新增 15 张完整 25 字段城市卡（长汀、珲春、延安、榆林、和田、轮台、武威、南宁、芒市、瑞丽、若尔盖、日喀则、普兰、札达、西昌）与构建器所需的等值 patch 镜像，新增 15 条联程线路并把河西走廊入口收束为武威，口径更新为 263 城 + 52 线；S3 复核中修正游轮登船日、九寨/黄龙顺序、错误标签与同行口径等语义问题，阿里南线因 10 日档无法诚实闭合而按 brief §3 拒绝该档、只保留 14 日拉萨闭环；海南西线/真环岛只产出 `.agent/m33-hainan-west-survey.md`，建议后续先做 4 新城 + 独立西线，真环岛需另立架构模块 ｜ Verified: 城市批两跑 263+37、SHA256 `347e13e9…4cee`；线路批两跑 263+52、SHA256 `aa22b7ea…f6b4`；文档/口径批两跑 263+52、SHA256 `9df8ecd6…db75`；15 城字段/patch 等值、52 线唯一 id、stops 存在与上限、F8 天数、海拔传播、必写 leg/stays、阿里闭环及旧线路仅河西一条预期变更均由机械断言通过，build 零 warning [codex]
- M33 — 内容扩容二期调研 [R2 · S2] · codex reviewer ｜复核 fcec443：F23 已修复，正常构建及 alt:true / alt:false 两类失败 fixture 均恢复 F8 拦截；只读盘点 248 城 + 37 线，产出 .agent/m33-expansion-survey.md（15 条线路候选、15 张城市候选与不建议项），未改数据；全量城市/联程复核的新问题 F24–F28 已写 review.md｜Verified: tools/build.py、全量 id/region/schema/线路站序与 plans 语义互查、官方来源交叉核验 [codex]
- F24–F28 triage（M33 调研随附复核，2026-07-16 cc）— 五条全采纳：**F24** 天山天池/五台山/乐山峨眉/神农架/长白山五卡 alt→true（patch 文件定点改，e 文件单行格式保留），北疆/晋北/东北雪国/宜昌神农架四线保守传播 alt→true；build 加 `ALT_TRUE_PIN` 人工海拔断言硬钉子 + F22 文本审计泛化到城市卡（原只查线路）；**F25** 青海湖祁连环线四站全补 leg（西宁-黑马河/茶卡/祁连×2/门源，大冬树山垭口 4120m 在 S204 已 web 核实）；**F26** 河西/呼伦贝尔/东北雪国三条 5 日档 plan 重写为覆盖全站且与 stops 同序，build 加 plan 文本顺序非阻塞警告（漏站判不了：海拉尔≠呼伦贝尔，靠评审）；**F27** 处置①改名「海南东线」（id 保留保收藏兼容，tagline/7日档标题去环岛口径；西线调研另派 codex）；**F28** 平凉 transit 重写——web 核实平凉至今仅宝中/西平线普速（2025-05 仅 K/6/7 字头），删「银西线高铁」编造，difficulty 一次中转不变｜Verified: build 两跑幂等；三个新闸门反例验证（五台山 alt=false→error、河西倒走文本→F26 警告、杭州塞垭口文本→F22 警告，复原后干净）；浏览器断言——青海湖环线整线装入路书含青海湖/黑马河/茶卡/大冬树山垭口/卓尔山/门源全部 leg 内容且无「版改编」、删茶卡站整组回退（leg 消失）、noAlt 筛选九个新 alt 记录全排除且杭州不误伤、海南东线名生效、平凉新口径生效 [cc]
- M32 — 详情页大按钮撞色修复 [R1 · S1] → sonnet · low · cc in-session ｜ 2026-07-16 用户反馈「打卡去过和加入行程同色怪怪的」：打卡按钮新增 `big-btn.green`（--spring 绿渐变，足迹语义），与行程橙、对比蓝三色区分；已打卡仍转 ghost 弱化、取消恢复绿 ｜ Verified: 浏览器断言三按钮 computed background 各异 + 打卡/取消类切换正确 + 截图目检卡通风格一致；build 两跑幂等 [cc]
- M27 — 行程单站点行移动端适配 [R1 · S1] · cc in-session ｜ Verified: Chrome 注入等效窄屏规则截图——站名整行、控件组第二行缩进（真机 680px 断点由用户手机复验） [cc]
- M28 — 顺路彩蛋真顺路化 [R2 · S2] · cc in-session ｜ Verified: 杭州+黄山行程彩蛋推莫干山/安吉/南浔 +绕1km（莫干山确在黄山→上海返程直线上，插入位置=几何最优）；线路卡不入候选、insertOnWay 拒线路 id、6 站上限保持 [cc]
- M29 — 路书逐日骨架 + 每晚落脚点 [R2 · S2] · 代码 cc，stays 数据 sonnet · medium 起草 + cc 复核（青甘大环线敦煌×2、伊犁十日分晚等主观点已核，标准节奏）｜ Verified: 独库3天+伊犁5天行程文本导出逐日行「20260707 D1 上海→独库公路（飞机 约8h），进线到那拉提 宿那拉提」全链正确；末日「当晚到家」；出发日期 localStorage 回环+垃圾输入拒绝；build 校验 stays 长度==days 两跑幂等 [cc]
- M30 — 江浙沪自驾 [R1 · S2] · legInfo 代码 cc；30 条 transit 文案 sonnet · low + cc 复核（7 条过保守时长收紧：徐州8→7h、连云港7.5→6.5h 等）｜ Verified: 沪杭段（208km）标注从「高铁」变「高铁/自驾」；杭州→黄山（皖）不受影响；build 幂等通过 [cc]
- M26 — 打卡/收藏跨设备分享备份 [R2 · S2] · UI/hash 合并/JSON cc；QR 编码器 sonnet · high（629 行零依赖 ISO 18004 实现）｜ Verified: 编码器与 python-qrcode 逐位交叉验证 6 组向量全一致（v1~v40，含 2331 字节 V40-M 容量边界与中文 UTF-8）+ 浏览器端真实迁移链接矩阵哈希与 python 参考一致（v5/mask2/hash 1967235024）；hash 导入确认条（并集合并、favs 收线路 id、visited 拒线路 id、垃圾 id 过滤、去重、导入后清 hash、hashchange 同页触发）与 JSON 导入导出全部浏览器断言通过；二维码默认收起、点开 230px；超 1200 字符降级提示走 JSON [cc]
- M31 — 城市卡包含关系排查 [R2 · S2] · codex 主审 + 2 个 GPT 只读分片复核｜产出 `.agent/m31-containment.md`：全量覆盖 249 城，确认 34 个包含配对（25 行政/地理 + 9 城市文件伪线路）及 5 个非严格包含的内容重叠项；逐条含证据与处置建议，未改数据｜Verified: 六区计数 42+34+54+29+53+37=249；全量 id/name/plan 互查、易误判组合卡反查、关键行政关系用政府资料交叉验证 [codex]
- M31·收尾修复第三轮（F23，2026-07-16 cc）— codex 复核第二轮修复时发现构建闸门回归：F22 新增的高海拔审计块把 F8「每个 days 档 ∈ [站数, Σ城市上限] 可达性」校验缩进吞进了 `if re.search(高海拔关键词)` 分支，导致该不变式只在「alt:false 且文本命中达坂/垭口」时才跑——当前 37 条命中数 0，等于对全部线路失效（alt:true 线路必跳过）。修复：F8 块移回 `len(ok)==len(st)` 通用校验层，F22 审计警告平行放在 `if "stops" in d` 层、不吞缩进；F8 注释加防漂移警示｜Verified: 正常 build 通过幂等；给 alt:true 独库注入不可达 days[2,5]→拦下（可达[3,9]）、给 alt:false 无关键词江南水乡注入 days[1,5]→拦下（可达[3,11]），两类 fixture 均如期拒绝，复原后干净 [cc]
- M31·收尾修复第二轮（F18复燃/F21/F22，2026-07-16 cc）— codex 复核指出上一版 leg 机制不抗行程编辑：**F18**（P1 复燃）旧版 `r:1` 持久化行程未迁移，老用户升级后仍看到「伊犁2天按5天版改编」——loadLS 加降级迁移：失效 `r`（非现存线路 id）去标记变独立城市站、天数夹到该城市合法方案档（旧独库 yili:2→5/bayanbulak:1→2），线路 id 已丢无法安全重建原线路，明确降级不假称已修；**F21**（P1）leg 只按单站判定，上移/删站/顺路排序/半条装入后仍吐固定顺序文案——改为**整组判定**：同 rid 全部 stop 齐全+连续+同序+天数=默认才整组启用 leg，任一破坏整组回退城市 pickPlan；**F22**（P1）独库翻哈希勒根达坂 3000m+ 却 alt:false，「避开高海拔」漏排——独库 alt:true，build 加非阻塞审计警告（文本含达坂/垭口/3000m+ 但 alt:false 时提示人工复核，不从自由文本硬判）｜Verified: build 两跑幂等（独库转 alt:true 后警告消失）；浏览器全套断言——旧 r:1 三站 payload 迁移为 yili:5/bayanbulak:2/kuqa:2 无改编、上移/删中站/半装三种破坏整组回退、完整装入 leg 正常、顺路排序后 leg all-or-nothing、独库 alt 徽章+详情高反警示+筛选/扭蛋池双双排除 [cc]
- M31·收尾修复（F18/F19/F20，2026-07-16 cc）— codex 复核暴露的「城市卡改联程票没认真改」根问题：**F18**（P1）线路装入行程后丢失线路方案、独库路书吐出「伊犁2天·按5天版改编」的错误内容——机制修复：线路 stop 可带 `leg={route,stays}` 线路视角逐日文案，路书优先于城市 plan（仅天数未改时生效，否则回退），trip 条目记 `r=线路 id`，build 校验 leg.stays 长度；**F19**（P1）独库北段哈希勒根方向错误——重构独库路线：起点乌鲁木齐（原伊宁在河谷西侧非线上）、stops 改乌鲁木齐·天山天池/巴音布鲁克/库车、三站写 leg 逐日文案、哈希勒根归入独山子→那拉提北段；**F20**（P2）西宁 3 天方案仍含跨州二郎剑整日——改市域内容（湟源丹噶尔/大通老爷山）+青海湖导流提示｜Verified: build 两跑幂等；浏览器断言独库整条装入路书无「改编」、哈希勒根在北段 D1、起点乌鲁木齐、河西走廊等无 leg 线路仍用城市方案降配（回归不变）、改天数回退城市 plan、旧 r:1 持久化不崩、西宁无二郎剑 [cc]
- M31·处置执行 — 2026-07-15 用户四组拍板（纯名义包含维持现名/A2 照做/环线归线路卡/C+D 都做），cc 执行：西宁·青海湖环线收窄为「西宁」纯城市卡（id 保留）、独库公路城市卡删除并重构 route-duku-highway（stops=伊犁/巴音布鲁克/库车）、ROUTE_STAY 收缩至 6 张、伊犁/喀什删越界内容并重写 stays、阿勒泰→布尔津·喀纳斯禾木、黄山→黄山风景区、武功山→萍乡·武功山（金顶）、6 张卡越界日程改「可接《某卡》」提示（sonnet · medium 执行 + cc 复核）；口径 248 城+37 线（README/meta 同步）｜Verified: build 两跑幂等含线路契约校验；浏览器断言独库线路展开装入=yili:2,bayanbulak:1,kuqa:2、伊犁 7 日路书无琼库什台宿昭苏、全站《卡名》引用逐一存在性校验通过；收录原则五条已写入 design [cc]

## 🔜 Next batch（五期开批 2026-07-19；spec 见 design 注册表 M45–M57 与「主题皮肤系统」——**代码轨道 M45–M58、M59、M60 全部落地**，本节只剩 M44 插画轨道续批 + M56 机制段候排）

轨道并行照旧：**插画轨道（codex，只动 `assets/illustrations/` 与工单）** ∥ **代码轨道（cc，动 src/）**，文件边界零冲突。四期 🔜 原第 1–3 条（M43 样张/M42 装饰批均已生成初筛完毕、M39 样稿三版）随皮肤化拍板改挂：水彩资产与样稿 v2 转手帐皮肤（P2）素材/施工图，生成与 QA 明细见 git 历史本节原文。 [cc]

1. 【画面】M44·分批铺量 — 目的地插画共享集 [R1 · S2] → codex 长期分批（2026-07-20 用户点名启动，从 P2 提前；与皮肤/代码轨道零文件冲突）｜完整约束=illustration-brief「M44 目的地插画分批铺量」：每城 1 版（✗ 带意见补 1 版）、开批去重硬闸、城市辨识度硬闸、区域回看闸、batch 1=10 城此后 20 城/批、每批停下等用户挑版。batch 0 三样张终审 + 共享集画风 A/B 比稿 B 胜出冻结（design「已冻结风格锁·目的地共享集」，原水彩锁降手帐皮肤专用）后开跑；**画不等接入**——接入走 M42 管线按批换上，卡顶个图属预期新增、代码零改动。进度： [cc]
   - **Batch 1 已终审转档**（2026-07-20）：用户明确 10 张全部通过（含初筛曾因匾额文字淘汰的 `nanjing`）；杭州采用本批 `v3` 新版替换既有母版，其余 9 张一起转 q90 WebP 入 `picked/dest/`。用户指出杭州已在 picked 却被重复生成，工单新增“开批去重”硬闸：picked 已存在 cityid 永久跳过、不占批次名额。 [codex]
   - **Batch 2 已完成生成与初筛，等待用户挑版**（2026-07-20）：去重扫描后按 `data-a.json` 顺序生成下一组 20 个未入选城市，范围 `dongji-island`→`chongming`，均为 1536×1024 RGB PNG；QA 总览=`raw/dest/qa/qa-m44-b2.png`（首行三基准），记录追加到 `raw/dest/qa/qa-m44-notes.md`。初筛 17 通过、3 淘汰：`xiandu` 主峰越出垂直中央 2/3 安全区，`qiandaohu` 船身与 `huaian` 城门匾额出现疑似文字样标记；全批并排未见“换画师”漂移。未启动 batch 3、未转本批候选入 picked、未接入代码。 [codex]
   - **江浙沪同质化返工 R1 已完成，等待用户挑版**（2026-07-20）：用户指出多张滥用“湖水 + 小船 + 塔/亭 + 远山”，工单已新增城市辨识度 prompt 硬闸、同区域母题分配、picked 区域回看例外与两项 QA（遮名认城 / 三项构图重合即返工）。复查用户点名的 5 张 picked：保留 `gouqi-island` / `putuoshan`，返工 `yangzhou` / `shaoxing` / `wuzhen-xitang`；复查 batch 2 后另返工 `nanxijiang` / `xiandu` / `qiandaohu` / `tonglu` / `zhenjiang` / `huaian` / `gaoyou` / `nanxun`。11 张最终候选初筛通过，QA=`raw/dest/qa/qa-m44-jzh-redraw-r1.png`；`xiandu-v2` 因安全区、`zhenjiang-v2` 因地标形制错误已淘汰，最终分别为 v3。旧 picked 未动，未启动 batch 3。 [codex]
   - **江浙沪返工 R1 已终审转档**（2026-07-20）：用户确认 11 张新版全部通过；q90 WebP 已入 `picked/dest/`——替换 `yangzhou` / `shaoxing` / `wuzhen-xitang`，新增 `nanxijiang` / `xiandu` / `qiandaohu` / `tonglu` / `zhenjiang` / `huaian` / `gaoyou` / `nanxun`。本次授权只覆盖新版 11 张；batch 2 回看保留的其余 12 张 v1 仍待单独终审。未启动 batch 3。 [codex]
   - **Batch 3 已完成生成与江浙沪区域闭环，等待用户挑版**（2026-07-20）：开批扫描确认 `data-a.json` 前 30 城均已有 picked/raw 且全部跳过，江浙沪只余第 31–42 条 12 个未生成 cityid，故本批按区域收口画 12 城、不跨区凑 20。初筛三张 v1 因主体进入上方 1/6 裁切带淘汰并定向补 v2（`zhouzhuang-jinxi` / `taohuadao-zhujiajian` / `shipu-xiangshan`），最终 12/12 通过。QA=`raw/dest/qa/qa-m44-b3.png`；42 城闭环总览=`raw/dest/qa/qa-m44-jzh-closure.png`，现状 18 picked + 24 raw、42/42 覆盖。独占母题复查未见继续套用“湖水 + 小船 + 塔/亭 + 远山”。未转本批候选入 picked，未启动其他地区。 [codex]
   - **Batch 4 已终审转档；Batch 5–11 已完成，等待 199 城统一审稿**（2026-07-20）：用户以“继续下一批”放行 batch 4，20 张最终版转 q90 WebP 入 `picked/dest/`；picked 从 44 增至 64 张。batch 5 完成 `data-b.json` 剩余 15 城华东闭环。随后用户拍板连续铺量、约 200 城再统一看 QA：batch 6–11 按数据顺序生成 120 个新 cityid（`beijing`→`yueyang-lou`），覆盖 `data-c.json` 全部、`data-d.json` 除已 picked 敦煌外全部，并推进 `data-e.json` 至岳阳楼；64 picked + 15 个 batch 5 raw + 120 个新 raw = **199 个唯一城市**。六张分批 QA=`qa-m44-b6.png`…`qa-m44-b11.png`，统一总览=`raw/dest/qa/qa-m44-total-199.png`。用户纠偏“传统建筑类别不等于撞型”：朱红墙/城门/寺塔/亭阁若为数据卡点名且形制可信的当地地标即放行，只有无出处通用建筑或地标形制错画才返工；据此 `fenghuang` 撤出，初筛现为 112 张可进入用户总审、8 张明确返工候选：`mohe`（可见文字）、`yanji`/`hulunbuir`（近景人物）、`qiqihar-zhalong`/`chenzhou`/`yichang`（主角错位或辨识弱）、`chaozhou`（广济桥核心启闭浮桥形制缺失）、`huangyao`（带龙桥被泛化为三孔廊桥）。batch 5–11 均未转 picked。 [codex]
   - **199 城统一终审转档完成；8 城待返工**（2026-07-20）：用户确认除返工清单外均通过。本次将 batch 5 的 15 城 + batch 6–11 的 112 城，共 127 个尚未入选 cityid，按 raw 最高版本转 q90 WebP 入 `picked/dest/`；已有 picked 去重跳过，三张 `*-sketch` 风格基准排除。转档后 picked 从 64 增至 **191 个唯一城市母版**，127 张均复核 1536×1024；`mohe` / `yanji` / `hulunbuir` / `qiqihar-zhalong` / `chenzhou` / `yichang` / `chaozhou` / `huangyao` 8 城未入 picked，保留 raw 待定向重画。未启动 batch 12，未改应用代码。 [codex]
   - **福州目的地图 40KB 预算修复完成**（2026-07-20）：191 城全量转档后 F60 唯一违规为 `dest-fuzhou.webp`——旧母版榕树叶片、瓦当、铺地与墙面碎纹过密，卡位在 q40 仍 43.3KB。按 M44 B 锁定向重画 v2，保留三坊七巷福州式马鞍墙主角、古榕与温泉井线索，减少重复屋面和高频纹理、增加留白；q90 母版由 643KB 降至 450KB，替换 `picked/dest/dest-fuzhou.webp`。真实全量运行插画管线零违规退出，`public/illustrations/dest/fuzhou.webp` 为 640×427 / 34.9KB（预算 40KB），其余 190 城亦全部过闸；未开预算例外、未改构建代码，8 城返工清单不变。 [codex]
   - **统一终审 8 城返工 R1 已完成，等待用户挑版**（2026-07-21）：按用户指示对 `mohe` / `yanji` / `hulunbuir` / `qiqihar-zhalong` / `chenzhou` / `yichang` / `chaozhou` / `huangyao` 各生成一个 v2，逐项修复文字、近景人物、主角错位及地标形制错误；8 张均为 1536×1024，文字/水印/风格/中央安全区与区域同质化初筛 8/8 通过。QA 总览=`raw/dest/qa/qa-m44-redraw-r1.png`，逐张结论已追加 `qa-m44-notes.md`。旧 v1 保留，新 v2 只在 raw；未转 `picked/dest/`、未启动 batch 12、未运行接入管线，停在用户终审闸。 [codex]
   - **挑版欠账清零 + 转档收尾**（2026-07-20）：用户在 codex 会话顺手完成全部待审（batch 2 剩余/回看 12 张 v1/batch 3），通过版由 codex 移入 `picked/dest/`——现 **44 城母版，江浙沪 42 城全闭环**。cc 收尾：核对 44 张母版均 1536×1024、24 张新母版入 git；cc 本地跑 M42 管线核出 44 张 dest 产物全部 640×427、≤40KB 预算内（最大恰 40KB），`bun run verify` 当时全绿（133+45+1，workerd 需沙箱外跑——EPERM 同 codex review 所记非产品问题）；**产物入库让位在途的 F58–F60 修复会话**（其正在改造 build_illustrations.py，产物随其重生成一并 commit 上线）。卡顶个图/详情头图为 M46 既有链路预期自动出现，代码零改动。**batch 4（华东）codex 同时段在批生成中**（raw 已见 16 城候选、QA/notes 未收口）——按规程停批后交用户挑版。**废弃清理**（用户指示）：raw/dest 58 张已 pick 城候选版本图 + 已收官批 QA 网格 5 张（notes.md 与三张 B 锁基准原图保留、在批华东候选不动）；raw/ink 37 张 A6 候选 + QA 网格/预览 html 9 件（style-ref-mock.png、qa-a6-notes.md、references/ 保留）；m39 质感样稿 v2/v3 补归档 git 历史后连同 v1 清出工作区（此前被 .gitignore 拦、「v3 已归档」口径从未成真——本次修正，ignore 规则保留） [cc]
**执行顺序（本轮 /loop，2026-07-20 用户指定）已全部完成**：[interrupt]✅ → M53✅ → M54✅ → M58✅ → M55✅ → M51✅ → M57✅，逐模块边界 compact，明细见各自 ✅ 条目。M56 机制段本批未含，留在本节候排。 [cc]

2. 【前端+内容】M56 — 交通方式修正：西部陆路语义 [R2 · S2] → **只剩机制段** sonnet · high（logic/transport+budget 守卫消费；schema/数据已全落库）｜⚠️ 2026-07-21 核实：批A 只落了数据段（noair/norail 111 城字段+build 校验），HEAD 的 transport.ts **零守卫**——字段无人消费，特克斯问题线上依旧，「机制段已完成」为误报。守卫 spec 见 design M56（含 2026-07-21 追加的 slowrail 第三守卫：仅普速城「高铁」降「火车」档，~65km/h+0.5h，6 城数据已由 cc 先行落库见 ✅）。transport.ts 当前被 M59 会话占用（④ 图标解析未提交）——**建议该会话收完 M59 批后同会话顺手接机制段**，文件域正好重合 [cc]

## ⏭ P1

- 【内容】M49 — 特色主题游扩容 + 筛选标签扩充（调研段）[R2 · S2 · 🌫️] → fable 编排、worker opus · high + 网查（开放式甄别；2026-07-20 用户拍板自 codex 划转，同日确认 worker 档位升 opus）｜ 2026-07-19 用户点名（诸暨珍珠城/横店影视城一族 + 「还能加什么筛选 tag」）；调研产出候选清单+标签提案交用户拍板，实施随拍板另立条目；新收录候选按 content-checklist 通用节预检；spec 见 design M49 [cc]
- 【前端+内容】M22 — 自选出发城市·北京首发 [R2 · S3 · 🌫️] ｜ 2026-07-20 用户拍板全模块划转 claude 侧（含原定 codex 的数据批）；2026-07-19 用户重排进 P1（原 2026-07-14「遥远将来」拍板作废）：先北京，其他城市再往后。两段式：**机制段**（行程/路书/彩蛋起点参数化=小头；difficulty 与 transit 均为上海视角、difficulty 还是筛选契约，per-origin schema 方案 🌫️ 需拍板）→ **北京数据批**（claude 侧，全量城市 transit/difficulty 北京视角重打+情侣周末类短途语义复审）。建议排在 M48/M49 实施之后——卡池稳定后再写北京视角文案，避免新增卡二次补写；spec 见 design M22（机制段开工时扩写） [cc]

## ⏭ P2

- 【前端】顺游半径西部放宽（150→按区域 200km） [R1 · S1] ｜ 2026-07-17 用户认可候补；gating：真实使用中觉得西部（拉萨类）彩蛋太冷清再动，动时注意东部不放宽 [cc]
- 【画面+前端】咔啦出场扩展 [R1 · S1] ｜ 2026-07-21 用户观察「设计的咔啦好像没咋用到」（明言无所谓=P2）；候选出场位：路书封面/toast/加载态/空态之外的彩蛋位，随后续皮肤批顺带 [cc]
- 【画面+前端】M39 — 皮肤：手帐（水彩剪贴簿）[R2 · S2] ｜ gating：M45 落地 + 水彩资产用户终审——2026-07-21 M60 后收窄为**扭蛋机/空态等 19 张**（原 28 张中九区 9 张随九区退出皮肤素材维度转素材库存档，不再待审）；审过 cc 转 picked/；另按成套清单需补工艺件批；施工图=质感样稿 v2（git 历史 m39-sample-v2.html，本地副本已清理） [cc]
- 【画面+前端】皮肤：青花（青花瓷）/ 画报（丝网印刷复古）[R2 · S2] ｜ gating：山水皮肤落地验证机制后按用户兴致排；spec 见 design 皮肤库表 [cc]
- 【内容】M11 — 海外版数据 [R2 · S2] ｜ 理由：goal 里的长期方向，用户未启动；2026-07-20 用户拍板执行侧划转 claude，开工前需 scoping 拍板（海外区域枚举/出发地假设/签证难度维度等） [cc]
- 【内容】高时效交通复卡清单（M56 批A 落库随附，定期回查，触发即回写文案/翻转标记）：上海轨交22号线（2026年内通车→chongming norail 翻转）、枣庄翼云（已校飞随时通航→taierzhuang noair）、红河蒙自（在建目标2026→jianshui-mengzi noair）、元阳机场（延期中→yuanyang-terrace）、丽水机场（2025-04 试飞→songyang/缙云片区文案）、冠豸山复航（→changting noair）、牡丹江海浪复航（或至2028→jingpohu noair）、雄忻高铁五台山站（2027-03→wutaishan）、甬石铁路（2027→shipu-xiangshan norail）、郑登洛城际（→dengfeng-songshan norail）、延榆高铁（2028）、兰合铁路（2027→gannan norail）、黑河高铁（→wudalianchi 文案）、漳汕高铁东山岛站（→dongshandao norail）、大瑞铁路保山以西（→ruili norail）、川青黄胜关以北（2028→ruoergai norail）、川藏铁路雅林段（2032→chuanxi-loop norail）、邵永高铁（2027-12→langshan-danxia norail）——共 18 项；**批B 新增 2**（2026-07-21）：云桂线阳宗站客运班次（2026-01 刚开、日约 3 班→chengjiang-fuxianhu 文案/norail 复议）、鄂州花湖机场客运航线存续（2025 收缩至约 1 条→huangshi-daye noair 复议）；**slowrail 批新增 2**（2026-07-21）：白阿线动车化改造（规划中→aershan slowrail 翻转）、滨洲线开行动车（电气化已成暂无动车→hulunbuir slowrail 翻转）——既列「黑河高铁」项触发时同步翻 wudalianchi slowrail [cc]
- 【内容】M22·其他出发城市 ｜ 北京批落地验证 per-origin 管线后逐城扩（2026-07-19 用户口径：北京先行、其余往后排）；spec 同 M22 [cc]

## 🟡 Pending decisions

- **默认皮肤**（2026-07-19 起待定；M46 落地但用户验收打回立 M52；**M52 已于 2026-07-20 落地，待用户终审山水新效果后拍板**）：奶油保守/山水尝鲜/「随机」，本轮 M46 交付刻意维持 `DEFAULT_SKIN="cream"` 不动（用户本次routing消息明确写死「默认给谁=用户终审拍板项」，不是 implementer 漏做）；～～选择器入口位置～～已随 M50 动作分区解决（页头全局动作区），M45「🎨 按钮位置临时」的注记随 M50 落地作废 [cc]
- **资产终审闸（皮肤化后口径）**：①~~A6 山水批用户挑版~~ 已收官（见 ✅ A6 条目）；②水彩资产终审拆两半（2026-07-20）：M43 三地样张终审**提前**为 M44 铺量 batch 0（见 🔜 第 2 条），扭蛋机/空态版本组合维持挂手帐皮肤（P2）gating——九区 9 张已随 M60（2026-07-21）退出待审转素材库；③~~风格锁经终审冻结后晋升 design~~ **山水锁已于 M46 落地时晋升**（design「已冻结风格锁·山水皮肤」），其余皮肤按同规则各自执行 [cc]
- 插画轨道执行细节：codex 若无图像 API 可用，降级为「整理批量 prompt 清单交用户手动生成」（工单已写明两种模式）；用哪个图像模型由 codex/用户按可用性定，cc 不锁定 [cc]

## 📋 拍板档案

- **2026-07-20 内容批三工单拍板**（用户）：**M48**=①difficulty 取整程复杂度口径+班次密度细则（<5对/日慎标直达），逐线：guilin→一次中转、siguniang-danba→折腾、jingcheng-bashang→一次中转、jiaodong-coast→直达，其余维持；②晋北改「可接《恒山悬空寺》《应县木塔》」；③太原主推直飞+高铁如实约9h；④警告6+建议23一揽子批准。**M56**=①本体粒度三档口径成立（<30km 同名近场通达/同市门户≤1h≥4班日 rail 通达/远端双标），四判例：五台山（品牌门户）、潮汕（共享机场）、崇武（随可接处理）均不标 noair、近郊未列项不追认；②不稳定态不硬置、复卡清单18项挂 state；③92组字段+29条文案+12城习惯区全批落库。**M49**=①T1 全收+陶瓷取龙泉/德化=9新卡，T2 余项留 backlog；②幻城类不收（廊坊/中牟出局）；③宜兴丁蜀走善卷洞补tag；④tag：实景演艺✓、观星✓（2026-07-20 二拍：用户指出「星空≠航天」动机混装，「星空航天」拆分——观星单立约5-7张、航天不立 tag 由文昌卡内容承载待复议；教训入 checklist「一个 tag 一种动机」）、工业遗产✓（用户点名推翻 cc 暂缓→已派补查深评定名单）、手作体验/市集未拍默认缓建、影视基地维持暂缓；⑤冷湖按观星+工业遗产双载体默认收（第10张，通达如实标注，用户可改）。拍板结果已回写三工单头部，实施批按其消费；**同日三拍**：工业遗产补查（opus 深评）回填后新晋 5 卡用户全收（唐山·开滦旗舰/六盘水避暑/攀枝花康养/黄石古今双遗产/大庆油城温泉，首轮误杀根因=忽略资源型城市转型第二支点）——**M49 入库终稿 15 新卡+3 tag（实景演艺/观星/工业遗产确信13张）**，存疑观察名单4（阜新/个旧/原子城/玉门），细节见 m49-findings 五之二附录 [cc]

- **2026-07-21 slowrail 拍板**（用户）：①「仅普速无高铁/动车」城增第三守卫字段 slowrail，启发式 leg「高铁」档降「火车」（源起用户问「普通火车还多吗」+ cc 池内筛查出哈尔滨→漠河类 8h 级时长错位）；②授权本 planning 会话**开代码例外顺手落地**（「做完的话就你这边顺手修一下，别忘了补到相应的规则里去」）——cc 执行时按文件归属收窄：只动无人占用的 data/build.py 数据段，transport.ts 守卫消费留给机制段（M59 会话在飞持有该文件）；③cc 核实并纠正「M56 机制段已完成」误报（批A 只落数据段，HEAD 零守卫），机制段列 🔜 待做 [cc]
- **2026-07-21 追加四条拍板**（用户，M59 立项后同日）：①票券氛围带修缮入批（M59 ⑪，根因=整券背景露缝实现形态，cc 取证）；②无个图城市卡位退大区题头（M59 ⑫）；③**九区题头晋升共享题头层**（立 M60）——用户预授权「画风差不多就直接进」，cc 比对确认同族（米纸底淡彩一体系，题头留白小品 vs 个图满幅=功能性密度差非风格冲突）；同日用户追认**九区彻底退出皮肤素材维度**：不设皮肤覆盖机制、新皮肤成套少画 9 版位、手帐水彩九区 9 张转素材库退出 gating（illustration-brief 成套清单/A7 预置画单已同步删该版位）；④氛围带/卡位题头随 cardPhotos 开关、详情兜底皮肤无关常显（cc 机制拍定）。附带观察：dest 产物目录 0 字节 suzhou/changting 为在飞 codex M44 返工批中间态，生产完好，不立项、让位在飞会话 [cc]
- **2026-07-21 共享集展示皮肤维度拍板**（用户）：①奶油卡位不展示 dest 个图（画风违和），详情位所有皮肤保留；②线路卡卡位启用九区题头图（兑现其原设计目的）——两条并入 M59（⑨⑩），机制入 design「目的地图展示语义」；cc 机制拍定=皮肤声明布尔开关、票券随卡位。随带资产使用审计（cc 盘点）：ink 15 槽位 + 6 工艺件全部在耗，九区图现役三处（线路详情头图/无个图城市详情兜底/票券氛围带），奶油零插画属设计（纯 CSS 卡通 + emoji），未消费的画作只有待终审的手帐水彩 28 张（P2 gating）与基准/参考图 [cc]
- **2026-07-21 视觉九条反馈立项**（用户）：三 bug + 六审美 → 立 M59 视觉修缮批（**已实现，见 ✅ M59**；咔啦出场少转 P2 观察条）。cc 拍定默认（用户复核可改）：详情区段=同构格式+每类一色；扭蛋弹层=机器 hero 化+??? 结果窗降级（DOM 冻结点按用户点名视为授权修订）；卡片交通图标=按 transit 文案词法解析主方式，条带/名下行共用。三 bug 根因均 cc 生产取证：山图=固定高条 cover 裁 70%、工艺框=细笔触母版+9-slice 五倍压缩非 404、图标=17px 尺寸设定（实现期另查出图标真正病因是 `.icon-btn`/`.btn` 选择器 specificity 撞车，17px 只是表症） [cc]
- **2026-07-20 目的地图展示语义拍板**（用户，44 城个图上线后）：卡位/详情位比例对调——卡片流=快滑浏览，卡顶个图收 **2:1 扁条**（浅裁，M44 安全区「主体压垂直中央 2/3」正好兜底）；详情页=单城深读，头图用**个图原生 3:2 完整不裁**；大区题头兜底与线路卡维持 2:1 原生不浅裁（题头契约不变），容器帧比随图源双态切换、缺图回退换源须同步换帧比。立 M58（design 增「目的地图展示语义」机制条） [cc]
- **2026-07-20 皮肤工艺件入画拍板**（用户，M52 审阅期）：用户问「哪些部分适合画出来而不是用代码实现」，cc 提五类工艺件（容器边框 9-slice/底材纹理 tile/印章/分隔线笔触/图位垫底——CSS 只能模拟质地、画才有真笔锋）+ 不入画边界（功能图标/带字按钮/交互态/标题字），用户拍板采纳并责成立册：illustration-brief 增常驻「皮肤资产成套清单」（第 0 件 mock+三层画单+开批规矩；A6 期「印章/分隔线由 cc 侧 CSS 自绘」口径向后作废、已收官批次记录不回改），design 增「工艺件画布契约」（含印章槽位对风格锁 No text 开例外、文生图汉字保真退路）与注册表 M57，A8 山水工艺件批立项。工艺件永远保留代码版做缺图回退 [cc]
- **2026-07-20 内容规矩三拍**（用户）：①M55 季节解析口径=**online** 渲染时解析、数据不加 tag（cc 拍：数据源保持人写单字符串、解析规则单点可单测、267 城零迁移；配离线审计扫出「含时间语义但解析不出月份」句子清单供补写）；②M56 表意口径修订=按当地习惯列自驾/包车主选项、**确实有航线的并列飞机**（组合标签「包车/飞机」式）、通达性网查严查；③开**常驻内容体检清单** `.agent/content-checklist.md`（活文件非工单，五节：通用/通达性/线路专项/季节时间/流程）——M48/M49/M56 数据段照此执行，以后新城市卡/线路卡入库过单，内容模块收工回写新规，design 增「内容质检规程」机制条；④本 planning 会话角色=只动控制面不写代码（src 让给在途 M52 fable 会话，避免冲突） [cc]
- **2026-07-20 内容侧五条反馈立项 + 执行侧划转**（用户）：①M48/M49/M22/M11 执行侧自 codex 划转 claude（插画/图像生成轨道仍归 codex）；②五条内容反馈溯源立项 M53–M56——对比池抽签采纳用户方案（cc 另拍：对比上限 4→6）、行程上限 cc 拍 6→10（最长线路 4 站+散选余量；后端/persist 均无站数约束已核）、路书季节文案 cc 拍「设出发日期后按停留月句级过滤，详情/对比保留全季节」、日序 cc 拍「路书渲染层重映射为全局 D 序，城市卡保留本地编号」、西部交通 cc 拍「同省 FLY_PROV 包车阈值 400→650 + noair/norail 守卫字段 + 267 城通达性网查体检」。四处默认值均 cc 拍定、用户复核可改；M56 范围拍定=只改交通判定与标注，不动天数模型 [cc]
- **2026-07-20 皮肤画风基准约定**（用户）：每皮肤画风基准=codex 整页 mock（水墨/青花两张已有并已按此执行 A6/预置 A7）；后续想做新皮肤一律先让 codex 画整页 mock、用户认可后才立项开批。已入 design「新皮肤诞生流程」。同时澄清双轨口径：目的地城市图共享集（水彩锁、基准=picked/dest/ 三样张）皮肤无关、不在此约定内；某皮肤将来要成套皮肤风城市图，走 design 成本模型既有的独立铺量决策条款 [cc]
- **2026-07-20 M50 落位修订拍板**（用户反馈四条 + AskUserQuestion 三答）：M50 首版「页头右上集中一簇」落地后用户指出：①足迹/备份应挂在足迹统计胶囊旁（备份备的就是这份数据）；②皮肤不配占一行（手机一屏内容珍贵，低频按钮进角落）；③「筛选」可展开无暗示；④收纳条「清空」与台底「清空筛选」重复。三答均选推荐项：足迹胶囊常显、本身可点开地图+旁挂 📤 备份钮；皮肤=右上角 absolute 图标钮；删收纳条清空（台底「清空筛选」为唯一入口）。折角暗示无分叉直接做。归属原则不变，「集中占行」落位方案废弃——design「界面动作分区」已改述为「挂靠语境、不集中占行」。对冻结点的第二次用户授权修订，落地后重新冻结 [cc]
- **2026-07-19 F54 处置拍板**（用户，AskUserQuestion）：皮肤声明契约的消费端验真选「边界修订，M46 验真」——M45 收敛为 token 体系+切换内核（design M45/M46/「皮肤声明」三处已改述），字体子集/懒加载管线与声明消费验真移入 M46 验收，M45 跨家族 gate 保持开放至 M46 该项过闸；不造一次性测试皮肤 [cc]
- **2026-07-19 动作分区拍板**（用户）：筛选台底部「排序+三开关+足迹地图+分享备份+皮肤+清空」异类混堆不可接受，责成按设计规范分区（同类同形、按归属安放，spec 见 design「界面动作分区」）→ 立 M50 并插进当前批次 M46 之前；冻结点结构随之做一次用户授权修订，M50 落地后重新冻结。M45 遗留的「皮肤按钮位置临时」待决项并入 M50 解决 [cc]
- **2026-07-19 五期皮肤化拍板**（用户）：①视觉采纳**皮肤可变**架构，不再单选画风，明示不计沉没成本；②首发皮肤=水墨（codex 整页水墨 mock 认可为方向参照），后续候选=水彩蜡笔/青花瓷/丝网印刷（皮肤命名 cc 拍定：奶油/山水/手帐/青花/画报）；③筛选台手机端收纳 + 行标签两字制（「抵达难度」→「抵达」）；④P1 排入线路全量体检、特色主题游扩容与筛选标签扩充（诸暨珍珠城/横店影视城一族）；⑤原「画风三选一」终审闸随本拍板关闭——样稿 v2 转手帐皮肤施工图、v3 归档 git 历史，水彩资产转手帐素材库不作废 [cc]
- **2026-07-17 四期开批拍板**（用户）：①解除单文件/零依赖/无后端硬约束——前端正规工程化（实现选型 Vite+TS+Preact 为 implementer 拍定，见 design「前端技术栈」，样稿/迁移启动前可推翻）；②允许简单后端，仅限 Cloudflare 免费额度（Workers+KV），**绝不付费**，用途=短链分享+同步码云同步，核心功能零依赖后端；③视觉转浅色手绘风——扭蛋机等核心元素手绘化排期靠前，目的地全量插画靠后但**本阶段必须出样例**；④插画文生图轨道交 codex（cc 出风格锁与 prompt 工单=illustration-brief.md），用户终审风格 [cc]
- **2026-07-17 吉祥物拍板**（用户）：角色=水豚（三候选中选定），硬要求**不得与现有水豚 IP 冲突**——工单 A2 已写入原创性红线（排除头顶柑橘/叼草/温泉毛巾等既有形象标志元素）+ 固定辨识三件套（黄渔夫帽/红背包/纸地图），初筛清单加对应检查项 [cc]
- **2026-07-17 吉祥物名字拍板**（用户）：**咔啦**（扭蛋机转动拟声）——首选「扭扭」被否（与用户小名相近），候选转转/咕噜/蛋蛋淘汰；名字入 design 装饰位清单，M42 接入文案用 [cc]
- **2026-07-17 咔啦空态执行口径**（用户）：空态必须引用已通过的水豚咔啦，并再次强调不得侵犯著名 IP；本轮以此前推荐的 `mascot-capybara-v4` 为同一角色参照，保留原创辨识三件套并执行工单 IP 排除清单。 [codex]
- **2026-07-17 控制面清理**（用户指示）：删除已消费的工单/调研文档 m31-containment / m33-expansion-{brief,survey} / m33-hainan-west-survey / m33-implementation-brief——git 历史为档案，既有 ✅ 条目中的文件引用自此指向 git 历史 [cc]
- **2026-07-18 Preact 推翻**（implementer 拍定，行使 2026-07-17 四期拍板①预留的「样稿/迁移启动前可推翻」权）：M38 迁移启动时废弃 Preact(+signals)，展示层改为按视图切分的模板渲染模块。理由：本应用无高频细粒度状态同步需求；等价重写全部渲染路径（含扭蛋动画/天气异步防串/overlay 命令式控制）只增回归风险、无用户可感知收益；视图模块已承载组件级样式（M39 换肤挂点）；M39–M44 均不需要框架。依赖/preset/tsconfig jsx 项一并移除，design「前端技术栈」已改述。供 codex review gate 与用户复核，如推翻此推翻则另立迁移模块 [cc]
- **2026-07-17 四期细项拍板**（用户，AskUserQuestion 三答）：①M39 字体方案=标题手写体（站酷快乐体档）+ 正文霞鹜文楷，与 design「质感层」既有 spec 一致，样稿按此做；②M41 同步范围确认=只同步打卡+收藏、行程不同步（与 design「后端」既有 spec 一致）；③GitHub Pages 旧入口用户已亲手取消发布——回滚依赖自此收敛为 git 历史 + Cloudflare 部署回退 [cc]
- （M26 起的历史拍板散记于各 ✅ 条目与 git 历史；Phase 1 拍板见墓碑范围） [cc]

## ❌ Explicit non-goals（dated triage 记录）

- 房价/余票实时数据：2026-07-15 复评维持不做；2026-07-17 理由随架构拍板更新为「无免费开放数据源，商业接口与绝不付费约束冲突」 [cc]
- 第三方瓦片地图：维持不做——GCJ-02 偏移 + 外部依赖/配额；SVG 足迹地图已交付够用 [cc]
- 用户账号体系（注册/登录/找回）：2026-07-17 原「用户系统/云同步不做」翻案收窄——云同步进 scope（M41 匿名同步码），账号体系维持不做 [cc]

## 📥 Review backlog（triage 结果）

- F30–F33（baseline eb33f58）：cc 已全采纳修复入库（见 ✅ M35·review triage），review.md 四条待 codex 下轮复核关闭 [cc]
- 2026-07-17 codex 跨家族复核：F30–F33 已关闭；M28 二至四轮已完成真实浏览器点击/开关复验，活动发现与四期控制面审计结论见 review.md baseline df8f485。 [codex]
- F14–F17、F18/F21/F22、F23、F24–F28：均已修复并经 codex 复核从 review.md 删除（历史见各 ✅ 条目） [cc]
- 7f0d6b1 review（F36/F39–F43）：已全部关闭——codex 确认性复核（baseline c4e65c1）含线上真实点击复验（两个清空按钮生产环境工作正常、控制台零 error、confettiCanvas/toast 存在）。复核只余 **F44**（P2 文档漂移）：F41 修复后 README/build.py docstring 仍写「Cloudflare 不重跑脚本」旧口径——cc 已同步三处文案（远端先跑 build.py 数据闸门；public/data/ 提交仅作本地预览与审计产物，部署真相源=data/），待 codex 下轮顺带关闭 [cc]
- **M38 gate review 已完成**（codex，baseline 6c3bce9，2026-07-18）：迁移保真度逐模块核对无语义回归、73 测断言来源确认非复制实现自证、Preact 推翻无异议、生产地址真实点击复验通过（线上产物与本地字节级一致）、F44 关闭——**gate 过闸**。留两条已由 cc 当轮 triage 修复入库：**F45**（P1）73 个决策层 Vitest 不在部署门禁上——新增 `verify` 脚本（`tsc --noEmit && vitest run`）并让 `build` 先跑 verify，Cloudflare 构建命令经 `bun run build` 自动继承；已按 codex 要求用刻意失败的单测验证门禁真拦（坏测试 → `bun run build` 退出码 1、vite 未执行；恢复后绿路径通过）。**残余项已收尾（2026-07-18，M40 部署时核实）**：M40 是 verify 门禁上线后第一次真实内容变更部署——生产地址引用的 JS/CSS 哈希文件名与本地 `bun run build` 产物逐字一致，证明 Cloudflare 远端确实完整跑过 `tools/build.py → bun install → bun run verify（tsc+vitest）→ vite build` 全链路且未失败，不是哈希凑巧不变绕过验证。**F46**（P2）README 旧口径——首段/依赖注释去 Preact、命令清单补 verify/test:unit 双测试栈、`src/main.ts` 单文件段落改写为 M38 后的 logic/ui/store/services 分层、部署段补「类型错误或单测失败会让部署失败」。两条待 codex 复核后从 review.md 删除 [cc]
- 2026-07-18 cc 开工 M37 前触发的状态流转规则（review.md 有未 triage 发现不开下一批）：F34/F35 已代码修复入库（见 ✅ F34/F35 修复），F36 已改 design/state 的 M32 标签为 `[R1·S1]`、补齐 M28 二至四轮/M43/M42·生成/M39·样稿的 `[R·S]→model·effort` 拨盘，F37 已删 design 信息组织表的硬编码「267 城+53 线」、把 `tools/build.py` 改述为「构建校验闸门」机制表达（`dist/`、`/api/*` 判断为架构级路由契约、非易变代码位置，保留），F38 核实已被 codex 自己的 170532d 提前修好（分工表已是 built-in imagegen 优先 + 现状用户职责），均待 codex 下轮复核后从 review.md 删除 [cc]
- **2026-07-19 M41 跨家族 review gate 已过闸**：codex 首轮复核（baseline a896fda）提 F47–F50；第二轮（baseline 1b98368）用受限频/并发 fake 实测证明 KV-only 缓解对 F47/F48 都不够，cc 改用 Durable Object 正面解决；第三轮（baseline c47f519）确认 F47/F48/F50 已关闭，新提 F51–F53（部署门禁缺口/8KB 计量口径不一致/design-state 口径漂移），cc 逐条处置。**codex 确认性复核（baseline c7bed5e）：F51–F53 全部关闭，review.md 当前无 Active findings，M41 `[R2·S3]` 跨家族 review gate 正式通过**——三轮下来，真正的架构问题（F47/F48：plain KV 无法承载真原子读写）被正面解决而非绕过，不是走过场。M41 至此完整闭环：代码、45+76 条自动化测试、部署门禁、生产地址真实并发/边界验证、跨家族 review 全部通过 [cc]
- **2026-07-21 内容批 gate（待开）**：批A（M48+M56 修复落库）+批B（M49 入库 15 新卡+3 tag+回标 51 条）已全部落库推送，**挂 codex 跨家族抽查 gate**——按 content-checklist「新增批由另一族模型 review 时按本清单抽查」执行，范围=批A/批B 两个 commit（9489819 起），重点：新卡通达标记依据抽查（noair/norail 111+8 处）、difficulty 口径一致性、回标 tag 语义边界（实景演艺/观星/工业遗产）、强季节卡 seasonNote 窗口。gate 未过前内容轨道不开新批 [cc]
- **2026-07-20 /loop 七项批次 review 响应**：codex 复核（baseline 883655a）新提 F61（P1）/F62（P2），cc 已逐条修复入库、带真实数据回归测试与浏览器端到端复验（见 ✅ M55/M57·review 响应）；review.md 未动（惯例同上，关闭权留给 codex 下轮复核确认后删除） [cc]

## 🪦 Sealed phases

🪦 Phase 1（M1–M25 + 三轮 codex triage）— sealed 2026-07-15 · commit 176512d
   Highlights: 单文件零依赖「下一站扭蛋」上线 GitHub Pages——249 城 + 37 条联程线路，筛选/对比/扭蛋/行程/路书全链路，天气接入（CC BY 合规署名）、打卡足迹 + 零依赖 SVG 中国足迹地图；模块号 M1–M25 永久保留

---
状态流转规则：会话结束同步本文件（署名）→ commit → push；review.md 有未 triage 的发现时不开下一个实现批次；后端相关模块合入前自查「免费额度内、无绑卡、降级路径存在」三件套。
